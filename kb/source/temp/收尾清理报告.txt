# 第二批 收尾清理报告

## 一、最终状态
  第二批（资料本体）      : 10233 个文件
  重复隔离区              : 22262 个文件（确认后可删除）
  可疑可执行文件隔离区    : 319 个文件（含毒载体，建议删除）
  与第一批重复            : 0 个  ✅
  剩余未解压压缩包        : 0 个

## 二、重复判定审计（重点：是否存在版本差异误判）

  A 类 · MD5 完全一致        : 9242 组
     → 二进制完全相同，不存在版本差异，判定绝对可靠。

  B 类 · 文本一致但字节不同  : 36 组
     → 已逐一核查：全部为 lp_solve 工具包的 C 源码
       （DEBUG.C / DEMO.C / LPGLOB.H …），分布在不同平台目录
       （lp20w95 / lp2djgpp / lpdos20），差异来源是
       **换行符 CRLF 与 LF 不同**（例：DEMO.C 均为 201 行，
       差 201 字节 = 每行 1 字节）。去空白后文本哈希一致。
     → 结论：属同一份源码的跨平台发行，判为重复正确，无版本差异。

  C 类 · 同名但大小不同      : 342 组
     → 这些**未被判为重复，全部保留**（系统按内容而非名字判定）。
       典型例：bp_neural_network.m 有 5272 字节（分类模型）与
       5642 字节（预测模型）两个版本，分属不同目录，均已保留。
     → 结论：版本差异场景已正确处理，未误删。

  **审计总结**：A 类属字节级相同，B 类属换行符差异，C 类未被合并；
  未发现「把不同版本误判为重复并删除」的情况。

## 三、不能正常解压的压缩包

  初次诊断：11 个「解压失败」。
  根因分析：
   1) 判定逻辑过严——tar 遇个别坏文件/中文名时返回码非 0，
      但实际**已释放其余文件**（如 chapter15.rar 出 4 个、
      chapter20.rar 出 18 个、lp_solve_3.0.tar.gz 出 51 个）。
   2) 文件名末尾带空格（如 'VISIO模板 .rar'）导致建目录失败。
  修复：
   1) 改为「只要解压出内容即视为成功（允许部分成功）」；
   2) tar/tar.gz 改用 Python tarfile（原生处理中文名）；
   3) 目标目录名去除末尾空格与点。

  **修复后**：11 个全部成功解压，多轮递归后
  **剩余未解压压缩包 = 0 个，失败 = 0 个**。

## 四、建议的后续动作
  1. 确认无误后可清空「第二批_重复隔离」与「第二批_可疑可执行文件隔离」。
  2. 第二批中 .bmp 等图片类占比较高，是否纳入知识库需你决策。
  3. 如需转 Markdown，建议优先处理文档类（docx/pdf/txt），代码类按需。