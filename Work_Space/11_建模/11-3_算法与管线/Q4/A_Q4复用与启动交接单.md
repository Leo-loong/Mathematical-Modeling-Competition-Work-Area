# A 题 · Q4 复用与启动交接单

> **用途**：Q4 求解 Agent **重新激活时的同步与纠偏**（其记忆停留在 09-11 08:09 之前，即其《求解思路与工作流》v2 定稿时）。
> **依据**：《A_外部资料复核实验报告》、《A_Q3创新项与外部核验·成果汇总与采纳裁定》、《A_数值口径总表》§十（含 §10.8）。
> **性质**：交接留痕，**不进论文**。

---

## 〇、一句话

**不要再等 Q3 的创新项；你真正缺的只是"约 15 分钟的口径移植 ＋ 3 分钟预测试复验"。**

---

## 一、三条纠偏（请自行核对，勿盲信）

### 纠偏 1｜Q3 的修正对象不是"表面 Robin 离散"，而是"控制体界面的物性系数取法"

你在 v2 的《继承关系警示》里写的是"**重点：表面 Robin 离散与质量核算口径**"——**猜偏了**。真实修正是：

| 项 | 内容 |
|---|---|
| **旧口径**（你在 7:55 复制进 `q4_core.py` 的那套） | `_iface_nb(f) = 2·l·r/(l+r)`，即**距离加权调和平均** |
| **新口径（M6）** | 界面系数取**沿 $C$ 的积分平均**（8 点采样），落地为 `q2_core_c._facek_nb(C)` 与 `_faceD_nb(C, T, d_fac, coupled, 8)` |
| **依据** | 调和平均**只对"界面处系数间断"成立**；Q3 尾部单个控制体内 $C$ 由约 0.15 降到约 0.05，$D$ 跨约两个数量级 ⟹ 调和平均严重**低估**胞内有效扩散系数 |
| **落地时间与位置** | `11-3/Q2/code/q2_core_c.py`（**8:44**，晚于你 7:58 的预测试 46 分钟） |
| **证据** | `Q3/q3_core.py` 现调用 `kf = _facek_nb(Ccur)`、`Df = _faceD_nb(Cit, Tcur, d_fac, True, 8)` |

**⟹ 你的 `q4_core.py` 目前仍是旧口径**，使用点共 3 处：

```text
  90: def _iface_nb(f):                       # 定义（调和平均）
 206:     kf = _iface_nb(kk)                   # T 方程主步
 251:     Df = _iface_nb(Dn)                   # C 方程 Picard 内
 296:     kf = _iface_nb(kk)                   # D6 校正步
```

### 纠偏 2｜你的 S2"机器级一致"结论**已失效**

S2 是 **7:58** 跑的，当时 `q3_core` **也还是旧口径** ⟹ 那是"**旧核 vs 旧核**"的一致（$1.6\times10^{-12}$），
**不能证明现在仍一致**。修正后两者已分岔。**S2 必须在移植后重跑。**

### 纠偏 3｜你的暂停理由已经解除

你标注的是 `⏸ 暂停等 Q3 收口`。**Q3 口径修正已完成并落地**（`q2_core_c` 8:44 改毕、`result3.xlsx` 已重生成、
《A_数值口径总表》§十 ＋ **M6 条款**已写入）。**剩下的只是文档层同步，你不再被阻塞。**

---

## 二、本轮实验给你的**直接证据**（新增，务必先读）

本轮完成了一组"**界面取法 × 网格**"的 $2\times2$ 对照实验（同一求解器、单变量）：

| 界面取法 | $\Delta r=1.0$ mm（$N=20$） | $\Delta r=0.25$ mm（$N=80$） |
|---|---|---|
| 两端点**调和平均**（你当前所用） | **559.31 h** | **88.43 h** |
| 沿 $C$ **积分平均**（M6） | **57.44 h** | **57.53 h** |

**三条对你有用的推论**：

1. **缺陷的量级远超 Q3 的观感**：Q3 在 $N=80$ 下该缺陷使 $t_{\rm end}$ 高估 **52%**（$87.49\to57.53$）；
   而在 $N=20$ 下高估达 **873%**（$559.31\to57.44$）⟹ **网格越粗，该缺陷越致命**；
2. **你的网格并不必然安全**：你用**物料坐标 $\xi$ 均匀网格**（$N=160$），$\xi$ 域内网格均匀 **≠** $C$ 的胞内跨度小；
   **体积收缩会使 $\xi$ 域的浓度梯度更陡**，而附录4 的 $D=4.2\times10^{-4}e^{-0.30/C}e^{-3850/T}$ 在低 $C$ 端同样急剧塌缩
   ⟹ **必须先做一次移植前后的对照实测，不得先验断定"影响小"**；
3. **移植代价极低**：Q3 侧同一单变量实验的实测成本仅 **约 2 分钟**（含两种取法、两种网格），
   故"先移植再复验"的总成本远低于"带着缺陷跑完再返工"。

> ⚠ **配套结论（同样适用于你）**：**"网格收敛"不能替代"系数取法正确"**——
> 在错误取法下，加密网格**根本不收敛**（$559.31\to88.43$，仍远离真值）。

---

## 三、当前全局进度（供你判断依赖）

| 项 | 状态 |
|---|---|
| Q1／Q2／Q3 | **全部完成**；Q3 最终 $t_{\rm end}=57.5314$ h，并已升级为 **$57.53\pm0.08$ h**（离散不确定度） |
| Q3 交付文档 | 已完成口径同步**并叠加增强项**（GCI／全局灵敏度／独立互验／外推 UQ）——**未完成前不要引用 Q3 结论性文字**这一禁令**已解除** |
| Q3 数值真值 | **唯一来源 =《A_数值口径总表》§十 ＋ §10.8** |
| Q3 创新项 | IN-1／IN-2／IN-3／IN-4a／IN-4b **全部完成**（详见《A_Q3创新项与外部核验·成果汇总与采纳裁定》） |
| **Q4** | **未启动正式求解**；口径移植待执行 |

---

## 四、你的下一步（建议三步，约 30 min）

### 第 1 步｜口径移植（必做，约 15 min）

在 `q4_core.py` 内新增 **Q4 专属**版本，替换上述 3 处调用：

- `_facek4_nb(C)`：界面 $k$ 沿 $C$ 的积分平均（8 点）；注意**用附录4 的 $k$**，且 `app3=True` 时切换回附录3；
- `_faceD4_nb(C, T, d_fac, coupled, nq=8)`：界面 $D$ 沿 $C$ 的积分平均；
  ⚠ Q4 的 $D$ 内 $T$ 用 **K**（保持现有 `_D_nb` 中 `Tc + CM` 的约定），公式为 $D=4.2\times10^{-4}e^{-0.30/C}e^{-3850/T}$；
- **实现请直接以 `q2_core_c._faceD_nb` / `_facek_nb` 为模板**（只读复制，**不回改**）；
- ⚠ `_iface_nb` **不要删**——S2 的"旧口径对照"仍需要它。

### 第 2 步｜预测试复验（约 3 min）

重跑 `q4_pretest.py`，**逐项重新裁定**（不要沿用旧结论）：

| 项 | 判据 |
|---|---|
| **S2** | `q4(fixR+app3)` vs **当前** `q3_core.step_imex_fast` ⟹ 应恢复 $\le10^{-9}$（通过即证明移植正确；这是你最强的自检） |
| **S4** | D6"方案 b（预估–校正）"的裁定是否仍成立（滞后偏差量级会变） |
| **S5／S6** | 收敛率是否变化 ⟹ $N=160$、$n\_sub=32$ 是否仍够 |
| **S7** | $N=80$ 的 $5.16\times10^{-5}$ 是否仍贴 L3-07 上限 |
| **新增** | **记录 $t_{\rm dry}$ 在移植前后的变化幅度** ⟹ 这就是你这一问的"偏差对照"，**务必留痕** |

### 第 3 步｜主求解（约 10 min）

- 移植 Q3 驱动范式：**事件驱动终止 ＋ 子步级二分 ＋ 熔断**（结构照 `Q3/q3_solver.py`，只读复制）；
- 参数（**待第 2 步复验确认后才锁定**）：$N=160$、$n\_sub=32$、$t\_{\rm corr}=1$；
- 熔断上界 150 h 保留；提交 §5.6 申请单后再 `--go`。

---

## 五、纪律（照旧，两处新加）

1. **只读** Q3 的**正式产物**（`q3_core.py`、`q3_solver.py`、口径总表、`result3.xlsx`）；**严禁读写 `Q3/innov/`**（实验专区，含半成品）；
2. **不改** Q2／Q3 任何文件（含 `q2_core_c.py`——那是修正后真值，只读复制）；
3. 新脚本首行打印 `[SCALE]`，默认只声明不执行，显式 `--go` 才跑；
4. **不被 Q3 旧文档结论污染**：任何"干壳阻滞／$C_\infty$ 是开关／表面列受限／解析差 12%／$S=+0.70$"之类表述**均已作废**，不得引用；
5. **（新）** 若你也要做创新项（自适应／GCI／全局灵敏度／外推 UQ），**优先复用 `Q3/innov/` 下的脚本模板**
   —— 它们已跑通并留有可复算的日志与数据，**不要在未对照的情况下另起一套**。

---

## 六、留痕

- 本交接单依据：本轮会话的 Q3 创新项执行结果、外部第一手资料复核实验、Q3 文档增强落地；
- 关联文件：《A_外部资料复核实验报告》、《A_Q3创新项与外部核验·成果汇总与采纳裁定》、《A_Q3检验与决策链》v2。
