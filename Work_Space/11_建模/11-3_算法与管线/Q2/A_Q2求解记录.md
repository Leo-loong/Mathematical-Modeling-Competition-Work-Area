# A 题 · 第二问（Q2）求解记录 · v2（★2026-09-11 全流程复核重跑完成）

> **定位**：Q2 的**执行记录**——逐阶段记"做了什么、得到什么、出了什么问题、如何处置"。
> **上游**：《A_Q2求解思路与工作流》（计划）、《A_数值口径总表》（数值真值）、《A_口径台账与裁定记录》（K 项）。
> **下游**：《A_Q2结果分析》《A_Q2检验与决策链》《A_Q2可靠性与方法学》（交付形态）。
> **纪律**：本文件是**过程记录**，数值一律以《A_数值口径总表》为准。

---

## 一、状态（★v2：全流程已重跑完成）

| 阶段 | 状态 | 产出 |
|---|---|---|
| W0 契约锁定 | ✅ 完成 | 《A_Q2求解思路与工作流》§一 |
| W1 三项设计 | ✅ 完成 | 同上 §二 |
| W2 步长/网格收敛预试验 | ✅ 完成（**并行重跑**） | `w2_foreground_log.txt`；**Δr=0.25 mm、内部步长 1/32 s**；9 算例墙钟 528.7 s → **91.3 s** |
| W3 实现求解核 | ✅ 完成 | `q2_core.py`（IMEX ＋ 退化模式） |
| W4 主力求解（3 h） | ✅ **完成** | `result2.xlsx`（10800×22×2 表）；耗时 273 s → **46.9 s** |
| W5 数值回填 | ✅ 完成 | 口径表 §九（**已按修订后数值更新**） |
| W6 检验 E1–E8 | ✅ 完成 | E2 ✓、E4 ✓、E6 ✓、E7 ✓（日志见 `code/`） |
| W7 结果分析 | ✅ 完成 | 《A_Q2结果分析》 |
| W8 收口与交付落地 | ✅ 完成 | 章节素材／图表规格卡／图数据 CSV／支撑材料 |

### 1.1 ★本轮（2026-09-11）全流程复核与重跑的**实质性发现**

| # | 发现 | 性质 | 处置 |
|---|---|---|---|
| **F-1** | **内层 Picard 迭代被上限截断**：日志"内层均 **256.0** 次/步"= 32 子步 × **8 次跑满**，而判据 `tol=1e-10`；诊断 A/B 实测（600 s 时程）相对差异 $1.6\times10^{-5}$，**随时程累积** | **实质缺陷**（数值可靠性） | `maxit` **8 → 30**（**交付口径**实测平均约 **9.3** 次/子步；早期诊断脚本曾记 16.4，系不同算例，见《A_Q2可靠性与方法学》§7.6） |
| **F-2** | 断言 `温度不超环境极值 <= 50.01` 为**硬编码**，而附件1 实测环境最高 **50.246 ℃** —— 仅"碰巧"未误报 | 判据缺陷 | 改为**动态取附件极值**（现输出 `50.2560 ℃`） |
| **F-3** | E4 互验**端点不一致**（显式 1.985 s vs 主力 2 s），且主力内部步长 1 s ≠ 显式 8.6e-2 s ⟹ 只能"量级校核" | 验证强度不足 | 改为**同步长 ＋ 端点严格对齐**（2.000000 s、24 步） |
| **F-4** | `A_Q2求解记录`（v1）状态表标"进行中"，而 W4–W7 实际已完成；待办栏写"W6 补跑 E7/E4/E6"但三者均已跑完 | **文档滞后** | 本文件升级 v2，状态表全部据实改写 |
| **F-5** | 交付包 `09_代码与复现/code/` 缺 3 个脚本（`q2_e5_sensitivity`／`q2_quick_check`／`q2_w2_prestudy`） | 交付不同步 | 见 §七 待办 |

### 1.2 ★性能优化（B ＋ C；用户指令）

| 版本 | 文件 | 内容 | 实测加速 | 数值差异 |
|---|---|---|---|---|
| **基线** | `q2_core_orig.py` | `solve_banded` 默认参数、每次新建数组、无 JIT | 1× | — |
| **B** | `q2_core.py` | numba JIT（物性／界面／装配／右端，`fastmath=False`），**保留 `solve_banded`** | **2.25×** | **逐位相同（0.000e+00）** |
| **B+C** | `q2_core_c.py` | 再以 **njit 手写追赶法**替换 `solve_banded` | **4.29×** | $\le1.8\times10^{-11}$ |

> `fastmath=False` 是硬性要求：JIT 后结果与 numpy 版**比特级一致**（B 版实测 `max|ΔT|=max|ΔC|=0`）。
> 核切换：环境变量 `Q2_CORE`——**默认已为 `c` 版**（`q2_core_c`，界面系数沿 $C$ 积分平均 ＝ M6）。基线对照脚本：`q2_solver_orig.py`（前台，输出 `result2_orig.xlsx`，与交付包隔离）。
> 一致性核对工具：`q2_verify_BC.py`。

---

## 二、设定与口径（求解开始前锁定）

| 项 | 取值 | 依据 |
|---|---|---|
| 物性 | 附录3：$\rho=650+128C$、$c_p=1450+2736\frac{C}{C+1}$、$k=0.21+0.38\frac{C}{C+1}$、$D=2.4\times10^{-3}e^{-0.45/C}e^{-3850/T}$ | N7–N10 |
| $h$、$k_m$ | 25 W/(m²·K)、$8\times10^{-7}$ m/s | N4／N5（H5） |
| 物性阶段切换 | **无**（全程附录3） | K10 |
| 边界 | $t\le14400$ s 用附件1 线性插值；$t>14400$ s 取 $T_\infty=50.00$ ℃、$C_\infty=0.04999$ | H6／N19／N20 |
| 时程与输出 | 0–10800 s（3 h）@1 s，10800 行 × 22 列 | K12／O3 |
| 空间 | $\Delta r=0.25$ mm（$N=80$ 区间／81 节点） | W2 |
| 内部步长 | $1/32$ s（T 与 C 统一） | W2 |
| 格式 | **IMEX 交替推进**（物性每内部子步更新） | W2 ＋ 文献背书 |

---

## 三、W2 预试验结果（摘要）

| 考核项 | 相对变化 | 判定 |
|---|---|---|
| 空间 $\Delta r$＝1.0／0.5／0.25 mm | $3.24\times10^{-5}$／$6.53\times10^{-6}$／基准 | ✅ |
| 时间 $h$＝1/2…1/64 s（温度） | $2.12\times10^{-5}\to6.83\times10^{-7}$ | ✅ |
| 时间 $h$＝1/2…1/64 s（含水率） | $1.23\times10^{-5}\to3.98\times10^{-7}$ | ✅ |

⟹ 定 $\Delta r=0.25$ mm、$h=1/32$ s（相对变化 ≤$6.8\times10^{-7}$，**比阈值严 70 倍**）。

---

## 四、过程中的问题与处置（★方法学留痕）

| # | 问题 | 性质 | 处置 |
|---|---|---|---|
| **Q2-α** | 外层迭代中 T 的时间推进被**重复叠加**（每轮从上一轮结果继续推进）⟹ 温度被过度推进（恒定 50 ℃ 边界下 1800 s 得 $T_0=49.99997$ ℃，物理正解应约 40.8 ℃） | **实质缺陷** | 改为**每轮都从 $n$ 层解出发**，仅物性随迭代更新（即 Q1 的"右端固定"纪律） |
| **Q2-β** | C 方程**每个子步的右端误取时步起点** ⟹ 各子步互相覆盖，有效推进量＝$\Delta t_C$ 而非 $\Delta t_{\rm out}$，制造"步长越小降幅越小"的**伪收敛陷阱** | **实质缺陷** | 改为**每子步从本子步起点出发**（子步间串行推进） |
| **Q2-γ** | 严格同层的外层耦合迭代**收敛因子≈0.91**（20 轮残差仅从 $4.9\times10^{-4}$ 降到 $2.0\times10^{-4}$）⟹ 需 100+ 轮，且未收敛解偏差达 **0.7 kg/kg** | **格式选择问题** | 改用 **IMEX 交替推进**（物性逐内部子步更新）：滞后量 ∝ 内部步长，**不损失精度**且无需外层迭代；文献中分区式强耦合慢收敛为**已知现象** |
| **Q2-δ** | v1 实现为**逐元素 Python 循环**，单步 0.63 s（900 步即 9 分钟） | **性能缺陷** | ①`iface`／`assemble_*`／`rhs_*` **全矢量化**；②三对角求解改用 `scipy.linalg.solve_banded` ⟹ 单步降至 0.0083 s（**19 倍**） |
| **Q2-ε** | 我**未做小规模计时即投长跑**，导致两次后台任务长时间无果 | **方法论失误** | 固化纪律：**任何长任务前必须先跑小规模计时**（已记入本节） |

---

## 五、主力求解（W4）—— ✅ 完成

**附件核验**：附件1 表头 `['时间','温度','水分浓度']`，241 点，0–14400 s 步长 60 s；
温度 28.0000–50.2460 ℃；水分浓度 0.01963–0.05025 kg/kg。

**求解设置**：$\Delta r=0.25$ mm（$N=80$ 区间／81 节点）、输出步长 1 s、内部步长 1/32 s、IMEX 格式、10800 步。

**耗时**：**273.0 s**（含并行任务带来的 CPU 竞争；纯计算约 190 s）。

**输出**：`result2.xlsx`，2.44 MB，工作表「温度」「水分浓度」，**10800 行 × 22 列**，4 位小数。

**6 项程序化断言（全部 PASS）**：

| # | 断言 | 结果 |
|---|---|---|
| 1 | 形状 10800×22 | **PASS** |
| 2 | 温度不超环境极值 | **PASS** |
| 3 | 温度不低于初值 | **PASS** |
| 4 | 含水率非负 | **PASS** |
| 5 | 含水率单调不增 | **PASS** |
| 6 | 含水率不超初值 | **PASS** |

**表3/表4 取用时刻的关键值（0.5–3 h）**：

| $t$/h | $T(0)$ | $T(R)$ | $C(0)$ | $C(R)$ | $\max_rC$ |
|---|---|---|---|---|---|
| 0.5 | 32.1893 | 35.4131 | 2.549932 | 1.648612 | 2.549932 |
| 1.0 | 40.3816 | 42.9977 | 2.525685 | 1.471059 | 2.525685 |
| 1.5 | 45.8468 | 47.1401 | 2.386029 | 1.347544 | 2.386029 |
| 2.0 | 48.4502 | 49.0033 | 2.170848 | 1.231075 | 2.170848 |
| 2.5 | 49.4670 | 49.6609 | 1.956653 | 1.116633 | 1.956653 |
| 3.0 | 49.8495 | 49.9664 | 1.766193 | 1.008113 | 1.766193 |

> ★上表为**修订后**真值（`maxit` 8→30，见 §1.1 F-1），并**已按 M6 口径（界面系数沿 $C$ 积分平均）更新**（v3，见 §1.2 附注）。
> 历史值：v1（`maxit=8`）为 `1.648628／1.471079／1.347566／1.231099／1.116659／1.766243／1.008141`；v2（`maxit=30` ＋ 旧**调和平均**界面）为 `1.648607／1.471056／1.347541／1.231073／1.116631／1.766196／1.008111`。
> **4 位小数层面**：M6 相对 v2 仅两格变化——(1.5 h, 0 cm) 温度 $45.8467\to$ **45.8468**；(2.0 h, 0 cm) 含水率 $2.1709\to$ **2.1708**；其余 28 格不变。
>
> ⚠ **★核口径（v3 补注，重要；本轮据实更新）**：交付 `result2.xlsx` **必须用 `q2_core_c`（界面系数沿 $C$ 积分平均 ＝ **M6**）生成**。
> `q2_solver.py` 的**默认核已改为 `c`**（见该文件第 53 行 `os.environ.get('Q2_CORE','c')`）⟹ **无需再设环境变量**即可按文档重出交付文件。`b` 版（`q2_core`，旧"调和平均"）**仅保留作口径对照，不得用于重出交付文件**。
> ✅ **原"待授权修改一行"已完成**（该项不再遗留）。

**观察**：$\max_rC$ **恒等于 $C(0)$** ⟹ 中心自始至终是最慢点（支持 O9 判据）；
3 h 末 $\max_rC=1.766\gg0.15$ ⟹ **远未达标**，与 Q3 需数十小时的先验一致。

---

## 六、检验（W6）

> ★**检验编号已与 Q1 统一**：E1 收敛 ｜ E2 解析对拍 ｜ E3 独立互验 ｜ E4 看门狗 ｜
> E5 灵敏度 ｜ E7 守恒核算（E6 数据预处理对照仅 Q1 需要，本问数据相同、不重复）。
> 历史文档中的旧编号对照：W2→E1、E4→E3、E6→E5。

| 检验 | 状态 | 结果（★v2 全部重跑完成） |
|---|---|---|
| **E1 空间/时间收敛**（工作流 W2 阶段） | ✅ | 空间**二阶**、时间**一阶**；$h=1/32$ s 时相对变化 $6.56\times10^{-7}$（温度）／$1.26\times10^{-6}$（含水率） |
| **E2 常物性退化对拍** | ✅ **通过** | 中心最大偏差 $3.336\times10^{-4}$ ℃、表面 $7.712\times10^{-4}$ ℃（与 Q1 同型对拍量级一致；与 v1 记录**一致**） |
| **E3 独立实现互验** | ✅ 完成（★已改进） | 显式 FTCS vs 主力 IMEX，**同步长 $\Delta t=8.333\times10^{-2}$ s、端点严格对齐 2.000000 s（24 步）**；末端场 $|\Delta T|=7.537\times10^{-3}$ ℃、$|\Delta C|=2.960\times10^{-4}$，属一阶时间格式量级、平滑无振荡 |
| **E4 物理看门狗** | ✅ 完成 | 6 项程序化断言全部 PASS（形状／温度极值／温度下界／含水率非负／单调不增／不超初值） |
| **E5 灵敏度** | ✅ 完成（并行） | 1 h 时程、10 组 OAT；$\max|S|=0.9520$（$T_\infty$ 主导）；墙钟 405 s → **57 s** |
| **E7 守恒性核算** | ✅ 完成 | 质量相对残差 $1.903\times10^{-3}$；能量相对残差 $1.803\times10^{-2}$（含"变物性下 $\int\rho c_p T r\,dr$ **非守恒量**"的方法学留痕）；极值与单调性 PASS；236 s → **111.3 s** |
| **★E3-b 强制解耦退化**（本轮补入） | ✅ **通过** | 单向耦合下改温度侧参数 $h$（±20%），含水率场 $\max\lvert\Delta C\rvert=\mathbf{0}$（**逐位不变**）⟹ 实现中无隐性的"温度→水分"依赖 |
| **★E8 潜热对照**（本轮补入） | ✅ 完成 | 计入潜热（$L=2.26\times10^{6}$ J/kg）后温度**系统性偏低 0.074–0.077 ℃**（峰值 −0.10 ℃）；潜热/对流 ≈ **0.64** ⟹ H3 简化**不可忽略**，其代价已量化 |
| **★E9 自适应时间步**（本轮补入） | ✅ 完成 | 内部子步降至 **18.8%**；墙钟 47.6 → **9.9 s（4.80×）**；偏差 $\le9.32\times10^{-6}$ kg/kg（**为 Q3 长时程备用**） |
| **★O8 耦合强度量化**（本轮补入） | ✅ 完成 | "温度→水分"通道对 $C(R)$ 影响 **18.1%**、"物性演化"通道 **5.98%** ⟹ 把"双向强耦合"变成**有数字的结论** |
| **★E5 补 $C_0$ 灵敏度**（本轮补入） | ✅ 完成 | $\lvert S\rvert\approx1.0$ ⟹ 最敏感参数，但属**"初值直传"的平凡敏感**（已如实标注，不可称"极度敏感"） |
| **★E5-b 全局灵敏度**（本轮补入） | ✅ 完成 | Morris ＋ Sobol：排序与 OAT **一致**；**交互占比普遍 <0.06** ⟹ OAT 足够。（$N=32$ 偏小，**仅可排序**） |

**E2 补充记录**：冻结物性 $\rho=976.4$、$c_p=3415.296$、$k=0.482958$、$D=5.64168\times10^{-9}$；
$\alpha=1.448282\times10^{-7}$ m²/s、$Bi=hR_0/k=1.035287$；级数解特征根 128 个；
自检 $\theta(Fo\to0)=0.99984\approx1$ ✓；外层迭代均 **1.00** 轮（frozen 模式预期）✓

---

## 七、待办（★v2 更新）

- [x] W5 数值回填《A_数值口径总表》**§九**（已完成；★已按修订后数值更新）
- [x] W7 写《A_Q2结果分析》（四层结构，已完成；★表值已更新）
- [x] W6 补跑 E7／E4／E6（★全部完成并改进）
- [x] W8 收口：交付包落地（模型叙述、可靠性与方法学、图表交付清单）
- [x] **性能优化 B ＋ C**（`q2_core.py`／`q2_core_c.py`）＋ 核对工具 `q2_verify_BC.py`
- [x] **交付包 9 张规格卡的越界数据源路径修正**（此前误指向工作区）
- [ ] **图件重绘（F-15～F-20）**：图数据 CSV 已刷新，参考图需按新数据重渲染
- [ ] **《A_Q2结果分析》§三 的 D 单调性对照表**需用新核复跑核对（当前沿用旧值）
- [ ] 交付包 `09_代码与复现/code/` 补齐 3 个脚本（`q2_e5_sensitivity`／`q2_quick_check`／`q2_w2_prestudy`）＋ 日志随包
- [ ] 把 B/C 优化的**方法学**写入《A_Q2可靠性与方法学》与 AI 登记
- [ ] W2 日志逐组行未落盘（改用统一 `say()` + 收尾一次性落盘，**已随并行化修复**）

