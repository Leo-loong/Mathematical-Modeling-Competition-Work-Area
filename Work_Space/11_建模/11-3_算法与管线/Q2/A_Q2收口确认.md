# A 题 · Q2 求解收口确认

> **定位**：Q2（整个烘干过程 0–10800 s，变物性双向强耦合）的**收口声明**——状态、文档地图、归档与清理记录、遗留与交接。
> **收敛范围**：**Q2 求解与全部检验（11 类）已收敛**；主力结果文件已完成全流程复核重跑。
> **给新 Agent**：想快速掌握 Q2，读本文件即可；想深入某一方面，按 §二 文档地图跳转。
> **对照范本**：本文件与《11-3_算法与管线/Q1/A_Q1收口确认》结构一致（五段式）。
> 按项目约定，本文件不引用、不记载资料中出现的日期。

---

## 一、状态声明

| 环节 | 状态 | 一句话结论 |
|---|---|---|
| W0 契约锁定 | ✅ | 口径与边界在求解前锁定（见《A_Q2求解思路与工作流》§一） |
| W1 三项设计 | ✅ | 物性更新粒度／耦合方式／线性求解器三项设计已定 |
| W2 步长／网格收敛预试验 | ✅ | 空间**二阶**、时间**一阶**；定 $\Delta r=0.25$ mm、内部步长 $1/32$ s（严于阈值约 40 倍）；**并行重跑** 528.7 s → **91.3 s** |
| W3 求解核实现 | ✅ | `q2_core.py`（IMEX ＋ 退化模式）；后经 B／C 两版等价优化 |
| W4 主力求解（3 h） | ✅ | `result2.xlsx`（10800×22×**2 表**）；**全流程复核重跑完成** |
| W5 数值回填 | ✅ | 口径表 §九（已按修订后数值更新） |
| W6 检验 | ✅ **11 类全部完成** | E1／E2／E3／E3-b／E4／E5／E5-b／E7／E8／E9 ＋ O8 |
| W7 结果分析 | ✅ | 《A_Q2结果分析》四层结构 ＋ Q2↔Q1 差异归因（三口径） |
| W8 收口与交付落地 | ✅ | 章节素材／32 张图表规格卡／图数据 CSV／支撑材料 |
| 一致性核验 | ✅ | 工作区与交付包 **25 个脚本逐哈希一致（差异 0）**；`README_日志索引.md` 交付侧为**全题索引**、工作区为**单问索引**（内容不同，属有意分层） |
| 红线自查（论文表 vs 交付文件） | ✅ | 口径表 §9.2 表值 与 `result2.xlsx` 逐值一致 |
| **计划缺口 G1–G6** | ✅ **全部补齐** | 见 §一.2 |
| **全流程复核（本轮）** | ✅ | 定位并修复 **5 项实质问题**（F-1～F-5），全部同步文档 |

### 1.1 本轮全流程复核的实质发现与处置

| # | 发现 | 性质 | 处置 |
|---|---|---|---|
| **F-1** | **内层 Picard 迭代被上限截断**：日志"内层均 256.0 次/步"＝32 子步 × **8 次跑满**，而判据 `tol=1e-10` | **数值可靠性缺陷** | `maxit` **8 → 30**（**交付口径**实测平均约 **9.3** 次/子步；早期诊断脚本曾记 16.4，系不同容限算例，已更正） |
| **F-2** | 断言"温度 ≤ 50.01"为**硬编码**，而附件1 实测环境最高 **50.246 ℃**，仅"碰巧"未误报 | 判据缺陷 | 改为**动态取附件极值** |
| **F-3** | E3 互验**端点不一致**（1.985 s vs 2 s）且步长不同 ⟹ 只能"量级校核" | 验证强度不足 | 改为**同步长 ＋ 端点严格对齐**（2.000000 s／24 步） |
| **F-4** | 《A_Q2求解记录》v1 状态表标"进行中"，而实际已完成 | **文档滞后** | 升级 v2，状态表据实改写 |
| **F-5** | 交付包缺 3 个脚本 | 交付不同步 | ✅ **已同步**：23 个脚本逐哈希一致 |

### 1.2 计划缺口 G1–G6 的补齐情况

| 缺口 | 内容 | 状态 |
|---|---|---|
| **G1** | 潜热对照（E8） | ✅ 完成：温度系统性偏低 **0.07–0.10 ℃**；潜热／对流 ≈ 0.64 |
| **G2** | 强制解耦退化检验（E3-b） | ✅ 完成：单向耦合下含水率场 $\max\lvert\Delta C\rvert=\mathbf{0}$（逐位不变） |
| **G3** | $C_0$ 灵敏度 | ✅ 完成：$\lvert S\rvert\approx1.0$（属"初值直传"的**平凡敏感**，已如实标注） |
| **G4** | RK-1（低 $C$ 端外推）声明 | ✅ 完成：已写入《模型评价与改进》§三 |
| **G5** | 全局灵敏度（Morris＋Sobol） | ✅ 完成：排序与 OAT 一致；交互占比 < 0.06 |
| **G6** | $D$ 单调性对照表用新核复跑 | ⏳ **未做**（差异应在 $10^{-11}$ 量级，不影响结论）——见 §四 遗留 |

### 1.3 口径判断

**检验体系由 6 类扩至 11 类**，覆盖并**超过**知识库《模型检验》对微分方程类模型的要求
（灵敏度：≥3 参数 ＋ 归一化系数；对比验证：含**消融实验**＝E3-b；鲁棒性：W2 收敛 ＋ E3-b 退化 ＋ E7 守恒）。
须在论文中如实标注的**三项表述边界**见 §四 第 3 项。

---

## 二、Q2 成果文档地图

### 2.1 建模与求解（`11_建模/11-3_算法与管线/Q2/`）

| 文档 | 职责 | 状态 |
|---|---|---|
| `A_Q2求解思路与工作流.md` | 求解思路与检验计划（含编号修订） | **计划态**（编号已与全题统一） |
| `A_Q2求解记录.md` | **执行记录**（状态、口径、发现与处置） | **结果态·权威**（v2） |
| `A_Q2结果分析.md` | 四层分析 ＋ 对比验证 | Q2 |
| `A_Q2方法学补检记录.md` | **方法学溯源**（IMEX／Picard／耦合策略的文献依据） | Q2 |
| `A_Q2缺口与优化评估.md` | 缺口盘点与优化分级（P0/P1/P2） | **已执行完毕**（v3） |
| `A_Q2收口确认.md` | 本文件 | Q2 |
| `code/` | 求解核、检验脚本、日志（**25 个 .py ＋ 日志 ＋ CSV**） | 见 §2.2 |

### 2.2 代码与日志（`11_建模/11-3_算法与管线/Q2/code/`）

**求解核（4 个版本，构成等价性链）**：

| 文件 | 职责 |
|---|---|
| `q2_core.py` | **主力核**（B 版：numba JIT ＋ `solve_banded`），2.25× |
| `q2_core_c.py` | **B+C 版**（再以 njit 手写追赶法替换 `solve_banded`），4.29× |
| `q2_core_orig.py` | **优化前基线**（供前台对照，与 B/C 版逐位比对） |
| `q2_core_bdf2.py` | 二阶时间格式（BDF2）探索，供 Q3 备用 |
| `q2_core_latent.py` | E8 潜热对照专用核 |

**主力与检验脚本**：`q2_solver.py`（主力求解 ＋ 6 项断言）、`q2_solver_orig.py`（基线前台）、
`q2_w2_foreground.py`（E1 并行收敛）、`q2_e2_frozen.py`（E2 常物性退化对拍）、`q2_checks.py`（E3＋E7）、
`q2_e3b_decoupled.py`（E3-b 解耦退化）、`q2_e5_sensitivity.py`（E5 OAT）、`q2_e5b_global.py`（E5-b 全局）、
`q2_e8_latent.py`（E8）、`q2_e9_adaptive.py`（E9 自适应步长）、`q2_verify_results.py`（结果核验）、
`q2_verify_BC.py`（双核一致性核对）、`q2_opt_verify.py`（优化等价性）、`q2_bench_ab.py`（A/B 基准）、
`q2_diag_picard.py`（Picard 截断诊断）、`q2_quick_check.py`、`q2_w2_prestudy.py`、`q2_figdata_export.py`

**日志**：见同目录 `README_日志索引.md`（**日志 ↔ 检验编号一一对应**；带"旧版／废弃"字样者仅留痕，**不得作为结论依据**）。

### 2.3 交付与图表

| 位置 | 内容 |
|---|---|
| `20_交付包/09_代码与复现/results/result2.xlsx` | Q2 交付结果文件（**10800 数据行＋表头**，×22 列，双表） |
| `20_交付包/09_代码与复现/code/` | 代码快照（**25 个 Q2 脚本，与工作区逐哈希一致**） |
| `20_交付包/09_代码与复现/README_日志索引.md` | 日志索引 |
| `20_交付包/01_模型叙述/A_Q2模型叙述.md` | 论文 5.2 模型建立素材（含 §2.1 $c_p$／$k$ 的双组分物理论证） |
| `20_交付包/01_模型叙述/A_Q2结果与分析.md` | 论文 5.2 结果分析素材 |
| `20_交付包/06_可靠性与方法学/A_Q2可靠性与方法学.md` | 论文"模型检验"节素材（11 类检验 ＋ 失败实验留痕） |
| `20_交付包/05_数值口径总表/A_数值口径总表.md` §九 | Q2 求解口径、关键数值、检验结论登记（**唯一真值**） |
| `20_交付包/04_图表包/` | F-15～F-32（**Q2 共 18 张**）规格卡 ＋ 图数据 CSV |
| `20_交付包/10_摘要与结论要点/模型评价与改进.md` | 模型优点／局限／改进（含 Q2 两条已量化局限） |

### 2.4 Q2 内容所在的跨问台账（**不按问拆文件**）

`05_数值口径总表` §九 · `03_假设清单`（分问适用性矩阵）· `02_符号表` · `04_图表包/04-0_图表总规划` ·
`11-1/A_口径台账与裁定记录` · `12_AI工具使用登记` · `13_论文可删减部分`（**Q2：S-09～S-18**）· `40_复核/02_差异台账`

---

## 三、归档、重组与清理记录

| 操作 | 对象 | 去向／结果 | 理由 |
|---|---|---|---|
| **并行化重写** | `q2_w2_foreground.py` | 原位（串行版保留为 `q2_w2_prestudy.py`） | 9 个独立算例可并行；**数值逐位不变** |
| **版本分层** | 求解核 | `q2_core_orig.py`（基线）／`q2_core.py`（B）／`q2_core_c.py`（B+C） | 构成**可对照的等价性链**，便于第三方复核 |
| **日志留痕** | 4～5 份"旧版／废弃"日志 | 原位保留，**在索引中显式标注"仅留痕、不得作依据"** | 保留过程可追溯性；避免误用 |
| **待清理** | `__pycache__/`（Q2 约 1.6 MB／83 文件；Q1 1 文件） | ✅ **已清理（2026-09-12 收口轮）**：全工作区 `__pycache__/`（Q1–Q4、kb/engine、机构对照、归档副本等 **9 处**）已删除 | 工具缓存，无内容价值 |
| **内容资产删除** | — | **0 项** | 除工具缓存外，一律"保留或标注"，不删内容 |

> **与 Q1 的差异说明**：Q1 收口时执行了"按问隔离"的结构重组与目录迁移；Q2 自建立起即在
> `Q2/` 目录内组织，**无需迁移**，故本节的"重组"项为 **0**。

---

## 四、遗留与交接

| # | 事项 | 归属 | 完成判据 | 影响面 |
|---|---|---|---|---|
| 1 | **G6：$D$ 单调性对照表用新核复跑** | 建模侧 | 复跑并与旧值比对 | **可忽略**：差异应在 $10^{-11}$ 量级（远低于 4 位小数输出精度），**不改变任何结论** |
| 2 | **F-21～F-32 共 12 张新增图未渲染** | 图表侧 | 按规格卡渲染并归入 `figures_reference/` | **不影响**数值与结论；仅影响论文配图丰富度（可删减项 S-09…S-18 中已注明"图可整段删"） |
| 3 | **三项须在论文中如实表述的边界** | 写作侧 | ① E2（常物性退化对拍）只证"**变物性框架下的离散与实现正确**"，**不等于**变物性本身的物理正确性；② E3（独立互验）只证"**代码路径独立性**"；③ E7 守恒核算与逐点断言**互补、不可互替** | 属**表述纪律**，违反则构成过度声明 |
| 4 | **$D$ 越大 ⟹ 表面浓度越高**的机理解释 | 写作侧 | 已在《A_Q2结果分析》§三 与 F-19 卡中改写（Robin 边界下**总失水由 $k_m$ 控制**，$D$ 只决定剖面平坦度） | 防"定性说反" |
| 5 | **交付包日志的位置** | 建模侧 | 交付包 17 个日志现位于 `09_代码与复现/` **根目录**，而工作区位于 `code/` 下 | ⚠ **结构不一致**（内容无差异）；是否统一由用户裁定 |
| 6 | **Q3 启动** | 待用户指令 | 须提交 T2／T3 **执行申请单**（依《工作约束》§12.3 六栏） | — |

---

## 五、纪律自查

- [x] 核心文档**无角色后缀、无日期**
- [x] 模板文件未覆盖（`00_*.md`、`00_规则/**` 未改动）
- [x] 同问文件已按 `Q2/` 隔离，**不散落**
- [x] 数值统一取自《A_数值口径总表》，论文数字禁止手抄中间输出
- [x] 每次实质 AI 工作**当场登记**
- [x] 冲突即停回询（本轮无语义冲突发生）
- [x] **不引入冗余编号**：检验／问题条目一律用小标题名称引用；检验编号与 Q1 统一（E1–E7）
- [x] 对外部核验成果**仅作自查**，论文与交付包中**不出现**任何外部来源与外部数值
- [x] **未运行任何未经批准的计算**（依《工作约束》§12）
