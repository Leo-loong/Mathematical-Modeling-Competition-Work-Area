# A 题 · 基线唯一性审计报告（Q1–Q4）

> **触发**：用户提出"**不同 Agent 互相口径不统一，一个改成一种说法、另一个改成另一种说法**"的怀疑，要求核查
> "每题是否存在**唯一的基线与标准**"。
> **性质**：审计留痕（**不进论文**）；**本轮只做核查与登记，未改动任何求解文件**。
> **方法**：以《A_数值口径总表》§二（M 系列）为**口径标准**，以"**各脚本实际 import 的求解核**"为**执行事实**，
> 逐文件比对（**不采信 docstring 的自我声明**）。
> **按项目约定，本文件不引用、不记载资料中出现的日期。**

---

## 〇、结论（先给答案）

| 问 | 唯一基线是否存在？ |
|---|---|
| **模型基线** | ✅ **存在且唯一** —— M 系列（尤其 **M6 界面口径**）已"定"，且明确"已全面替换 Q1／Q2／Q3 各核" |
| **数值基线** | ✅ **存在且唯一** —— 《A_数值口径总表》**§八（Q1）／§九（Q2）／§十（Q3）／§十一（Q4）** 四节齐备；§五 纪律"冲突以本表为准" |
| **交付数值** | ✅ **安全** —— `result1/2/3/4.xlsx` **全部由积分平均（M6）的核产出**（逐项已核实） |
| **执行层（脚本）** | ⚠ **不统一** —— **主力核一致，但检验／图数据脚本存在"旧口径孤岛"**，集中在 **Q2** |
| **文档层** | ⚠ **存在多处自相矛盾与过时表述**（见 §四）—— **这正是"一种说法 vs 另一种说法"的来源** |

**一句话**：**基线是唯一的，但"执行与文档"没有全部跟上基线** —— 你的怀疑成立，且问题的**范围与位置已精确定位**。

---

## 一、三层基线的核查

### 1.1 模型基线（M 系列）—— ✅ 唯一

《A_数值口径总表》**§二 M6**（状态"**定**"）：

> 控制体界面上的**变系数**（$D$、$k$）取**沿 $C$ 的积分平均** $\langle\phi\rangle=\frac{1}{\Delta C}\int_{C_{i+1}}^{C_i}\phi\,\mathrm dC$…
> **旧"两端点调和平均"仅对"界面处系数间断"成立**，在 $D$ 随 $C$ 跨数量级时显著低估 ⟹ **已全面替换（Q1／Q2／Q3 各核）**

⟹ **M6 即界面口径的唯一标准**，且**明文要求"各核"替换**。

### 1.2 数值基线（口径总表）—— ✅ 唯一且覆盖四问

| 节 | 覆盖 | 关键内容 |
|---|---|---|
| §八 | Q1 | 8.1 口径／8.2 结果／8.3 检验／8.4 待优化／8.5 有效位数 |
| §九 | Q2 | 9.1 口径／9.2 结果／9.3 检验／9.4 与 Q1 关系 |
| §十 | Q3 | 10.1–10.8（含 10.8 增强项真值） |
| §十一 | Q4 | 11.1–11.5 |

**§五 纪律**：*"本表与 `30_图表/03_图数据准备/*.csv` 冲突时，**以本表为准并回询**"* ✓

### 1.3 执行基线（脚本 → 核）—— ⚠ 分层判定

**判据**：**实际 import 的核**（不看 docstring）。

#### ✅ 五个主力核**全部**为积分平均（M6）

| 核 | 界面实现 | 证据（本次核实） |
|---|---|---|
| `q1_core.py` | **积分** | `faceD_int()`（L193） |
| **`q2_core_c.py`** | **积分** | **`step_imex` L691-692 显式 `kf=facek_of(Ccur)`；`Df=faceD_of(...)` L707/709** ⟹ **主推进不经调和分支** ✓ |
| `q3_core.py` | 积分 | L115 `kf=_facek_nb(Ccur)`；L131/133 `_faceD_nb` |
| `q3_core_nu.py` | 积分 | L266/271（同类） |
| `q4_core.py` | 积分 | `_facek4_nb`（L110）／`_faceD4_nb`（L141），L292/348/350/396 调用 |

> **本次的重要澄清**：`q2_core_c.py` **L450** 存在 `kf = _iface_nb(...)`（调和），一度触发告警；
> 经核实它是 **`assemble_T_var(kf=None)` 时的防御性回退分支**，而**两个推进函数（`step_coupled` L590、`step_imex` L691）
> 均显式传入 `kf=facek_of(...)`** ⟹ **主路径确为积分平均** ✓ **交付数值不受影响**。

**⟹ 推论：`result1/2/3/4.xlsx` 全部由积分平均核产出** ✓（且 `q2_run_log.txt` 首行明写"求解核：C 版"、终态与 §9.2 逐位一致）

#### ⚠ 旧口径孤岛（**Q2 最集中**）

| 脚本 | 走旧核 | 影响的结论／图 |
|---|---|---|
| **`q2_checks.py`** | `q2_core`（L40 `from q2_core import ... harm ...`） | **E4 独立实现互验** ＋ **E7 守恒核算**（质量／能量） |
| **`q2_e2_frozen.py`** | `q2_core` | **E2 常物性退化对拍** |
| **`q2_e5_sensitivity.py`** | `q2_core`（L40/75） | **E5 灵敏度** → **F-26 雷达图**（用 `q2_e5_S.csv`） |
| **`q2_e5b_global.py`** | `q2_core`（L43/81） | **E5b 全局灵敏度** → **F-31 Sobol 热力图** |
| **`q2_e3b_decoupled.py`** | `q2_core`（L41） | **E3b 耦合分解** → **F-27** |
| **`q2_w2_foreground.py`**／`q2_w2_prestudy.py` | `q2_core` | **W2 收敛性** → **F-20** |
| `q2_diag_picard.py`／`q2_quick_check.py`／`q2_opt_verify.py` | `q2_core` | 诊断类（不直接进论文） |
| `q2_bench_ab.py` | `q2_core` | **对照实验（本就该用旧核）** ⟹ **特性，非缺陷** ✓ |
| `q2_figdata_export.py` | `q2_core`（**仅借物性函数**） | **无害** ✓（数据实际来自 `result*.xlsx`；两核物性表达式一致） |

#### ⚠ 两个"旧口径派生核"

| 核 | 界面实现 | 影响的结论／图 |
|---|---|---|
| **`q2_core_latent.py`** | `_iface_nb`（**L364**），**全无** `_faceD_nb`／`_facek_nb` ⟹ **完全旧口径** | `q2_e8_latent.py`（**E8 潜热对照**）⟹ **"基线"与"对照"两侧同为旧口径**，其 $\Delta T$／$\Delta C$ **混有口径影响** |
| **`q2_core_bdf2.py`** | `_iface_nb`（**L357**），同 | **F-30 精度–效率帕累托图**的数据源 |

**⚠ 两者的 docstring 都声称"由 `q2_core_c.py` 派生，除本项外逐字相同"** —— 但**实际界面仍是调和** ⟹ **它们是"修正前的 C 版"的派生** ⟹ **该声明已过时**（见 §四）。

#### Q1（1 处）

* **`q1_e3_explicit.py`**：**自带**调和实现（L169 `2D_lD_r/(D_l+D_r)`），而其**参考解来自 `q1_core`（积分）**
  ⟹ E3 原设"空间口径与主力一致"的前提**已失效**，两轨现差**两件事**（时间格式 ＋ 界面取法）。
* 其余（E1／E2／E4／E5／E6／solver）全部走 `q1_core` ✓ **单一口径**；且 **E2 走 `dmode='const'`**（常数 $D$）**不受影响** ✓

#### Q3／Q4 —— ✅ **全部干净**

* Q3 全部脚本走 `q2_core_c`／`q3_core`／`q3_core_nu` ✓（含 `innov/` 全部）
* Q4 全部走 `q4_core`（积分）✓

---

## 二、影响分级

| 级 | 对象 | 判定 | 说明 |
|---|---|---|---|
| **A** | **Q2 的检验结论**（E2／E3／E4／E5／E5b／E7／W2，见 §9.3） | ⚠ **旧口径下得出** | 结论**方向**大概率不变，但**量化指标需复算** |
| **A** | **F-20／F-26／F-27／F-30／F-31** 图数据 | ⚠ **旧口径** | F-29（潜热）亦含旧口径基线 |
| **A** | **Q1 的 E3**（→ F-12 图数据） | ⚠ **两轨口径不一致** | 见上 |
| **B** | `result1/2/3/4.xlsx`、F-15～F-19 | ✅ **安全** | 均由积分平均核产出 |
| **C** | docstring／登记表述 | ⚠ **多处矛盾** | 见 §四 |

**量级先验（**不可当结论**）**：Q2 的 $C$ 区间为 $2.55\to1.008$，$D\propto e^{-0.45/C}$ 的**跨幅仅约 1.31 倍**；
而 Q3 尾部（$C\to0.053$）跨幅约 **243 倍** ⟹ Q2 的界面取法敏感度**预期远小于 Q3**。
**但这正是 Q3 教训禁止的推理方式**（"先验断定影响小"在 Q4 被打脸过）⟹ **必须实测**（见 §五 P1）。

---

## 三、★"两个 Agent 各改一半"的直接证据

### 3.1 口径总表 §九附注 A（L319）与代码事实**相反**

| 来源 | 表述 |
|---|---|
| **口径总表 §9 附注 A** | *"`q2_solver.py` 的**默认值仍为旧口径的 `b` 版**，**待授权改为 `c`**（见《A_复核差异台账》D-22）"* |
| **`q2_solver.py` L53 实际代码** | `_CORE = os.environ.get('Q2_CORE', **'c'**).strip().lower()` ⟹ **默认值已是 `c`** |

**⟹ 有人把代码默认值改成了 `c`，但**没有人回填口径表**。**这就是"一种说法 vs 另一种说法"的实证** ✓

### 3.2 检验编号在两处不一致

| 来源 | 同一实验的编号 |
|---|---|
| `q2_checks.py` docstring（L3） | **E4** 独立实现互验 |
| 《A_数值口径总表》§9.3 | **E3** 独立实现互验 |

### 3.3 `q2_core_c.py` docstring **自身**前后矛盾

* L28：*"界面系数一律**距离加权调和平均**"* ⟷ L140：*"应改用下方 `_faceD_nb`／`_facek_nb` 的**沿 $C$ 积分平均**"*
* L38：*"求解器仍是 `scipy.linalg.solve_banded`（B 版不替换）"* ⟷ L422：*"★C 版：改为 **numba 手写追赶法**"*

### 3.4 其他过时 docstring（同类）

| 文件 | 过时表述 | 实际 |
|---|---|---|
| `q3_core_nu.py` L12 | "界面系数仍用**距离加权调和平均**" | L266/271 用 `_facek_nb`／`_faceD_nb`（积分） |
| `q1_core.py` L163 | `assemble_C` docstring："Df…（**调和平均**）" | L226 用 `faceD_int`（积分） |
| `q2_core_latent.py` L19／`q2_core_bdf2.py` L15 | "与 `q2_core_c.py` **逐字相同**" | 界面**仍为调和** ⟹ 派生自**修正前**版本 |

---

## 四、整改方案

| 优先级 | 项 | 内容 | 成本 |
|---|---|---|---|
| **P0** | **修正口径总表 §九附注 A** | 删除"默认值为 `b`、待授权改为 `c`"，改为"**默认值已为 `c`**（M6 口径）"；同步销项 D-22 | 5 min |
| **P0** | **修正各核 docstring** | `q2_core_c`（L28/L38）、`q3_core_nu`（L12）、`q1_core`（L163）、两个派生核的"逐字相同"声明 | 20 min |
| **P1** | **Q2 检验脚本的口径复算**（★核心） | 照 Q3 的 E-DIAG-2 体例，把 6 个检验脚本（`q2_checks`/`e2_frozen`/`e5`/`e5b`/`e3b`/`w2_foreground`）**切到 `q2_core_c` 重跑**，量化 $\Delta$ | 2–4 h |
| **P1** | **Q1 的 E3 单变量对照** | 副本内把界面 D 换成积分，与现行比 $\max\|\Delta T\|$／$\max\|\Delta C\|$；若 ≪ 判据则 E3 结论成立、仅需声明边界 | 0.5 h |
| **P1** | **F-20／F-26／F-27／F-30／F-31 图数据复核** | 待 P1 复算后按结果决定是否重出 | 依 P1 |
| **P2** | **统一检验编号** | `q2_checks.py` 的 E4 → E3（或反之），全库同步 | 15 min |
| **P2** | **派生核升格或标注** | `q2_core_latent`／`q2_core_bdf2` 建议**同步升为积分口径**（否则其结论需附口径声明） | 30 min |

**建议执行顺序**：**P0（30 min，零风险）→ P1-Q1-E3（0.5 h）→ P1-Q2 复算（主要工作量）**。

---

## 五、审计方法自查（防"我以为"）

| 检查 | 做法 |
|---|---|
| 是否采信了 docstring 的自述？ | **否** —— 一律以 **`import` 语句 ＋ 调用点实参** 为判据 |
| 是否只看了"文件开头"？ | **否** —— `q2_core_c` 的风险点在第 450 行，正是"往下读"才发现的 |
| 是否区分了"特性"与"缺陷"？ | **是** —— `q2_bench_ab.py`（口径对照实验）用旧核是**设计意图**，未计为缺陷 |
| 是否把"预期影响小"当成结论？ | **否** —— 已明确标注"**必须实测**" |
| 是否改动了求解文件？ | **否** —— 本轮**只读** |

---

## 六、变更记录

| 版次 | 变更 |
|---|---|
| **v1** | 首次建立：以"**不看自述、只看 import 与调用点**"为方法，对 Q1–Q4 做基线唯一性审计。**结论**：模型基线（M6）与数值基线（口径总表 §八–§十一）**唯一**；**五个主力核全部为积分平均**（含对 `q2_core_c` L450 告警点的**排除性核实**）；但 **Q2 的检验与图数据脚本存在 12 处旧口径孤岛**（含两个旧口径派生核），**Q1 的 E3** 为两轨口径不一致。**并给出"两个 Agent 各改一半"的直接证据**（口径表 §九附注 A 与 `q2_solver.py` L53 相反、检验编号 E3/E4 冲突、多处 docstring 自相矛盾）。整改方案 P0–P2。 |
