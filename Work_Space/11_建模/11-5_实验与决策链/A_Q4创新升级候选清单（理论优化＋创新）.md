# A 题 · Q4 创新升级候选清单（理论优化＋创新）· v1

> **性质**：**计划态文档（不产生数值结论、不改代码）**。按用户指令"文献参考与创新方法一并执行，减少一轮代码修改"——本清单把 Q4 可做的**理论优化**与**创新项**全部列出并分级，供裁定后**一次性**执行。
> **上游**：kb 检索（本轮 4 式）、Q3 创新资产（《A_Q3创新项与外部核验·成果汇总与采纳裁定》v3）、Q4 现状（《A_Q4检验与决策链》v1、《口径总表》§十一）、交接单纪律（"优先复用 Q3/innov 模板，不另起一套"）。
> **合规基线**：沿用 Q3 采纳裁定 §五 全部边界（外部内容零出现、对照轨不得作结果、全部数字可复算、不利事实如实登记）。

---

## 一、检索与资产盘点

### 1.1 kb 方法论锚点（与 Q4 创新直接相关者）

| 锚点 | 位置 | 支撑的候选项 |
|---|---|---|
| IMEX-BDF2 后验误差估计与自适应时间步（JCAM/Calcolo 2025）＋最优自适应隐式时间步（arXiv 2025）＋IMEX-RB 一阶无条件稳定（arXiv 2025） | `kb/source/联网搜索/` | 自适应步长（B2）的正当性论证 |
| 网格收敛性与 GCI（Richardson 外推） | 同上 | A1 误差带 |
| 灵敏度方法学（OAT／Morris／Sobol／FAST） | 同上 | A3 全局灵敏度 |
| 边界固定化 BIM vs 变空间网格 VSGM（Sadoun 2012，#36 新入库） | 同上 | 方法族定位＋"能量守恒残差作检验指标"（已由 E7 落实） |
| 干燥降速段划分／内外传质阻力／传质 Biot 数判据 | 同上 | A5 解析夹逼（下界构造）＋结果分析机制段 |
| Adrover 移动边界干燥系列（#32/#33/#34）；Crank/Murray–Landis/Gibson（#35） | 同上 | A5（渐近上界）／B5（长径比判据 L₀/R₀>15 vs 本题 12.5） |
| 多孔物料收缩特性／苹果切片孔道网络／苹果丁收缩机制（#15/#16/#17/#19） | 同上 | B3（非均匀收缩）的机理边界声明 |
| 食品等温干燥收缩移动边界模型（Adrover 三篇同族） | 同上 | 同上 |

### 1.2 Q3 创新资产可复用映射（`11-3/Q3/innov/`，只读复制）

| Q3 资产 | 内容 | Q4 移植形态 | 改动量 |
|---|---|---|---|
| IN-1 `q3_solver_INV1.py` | 一阶＋Richardson 半步后验误差自适应 | B2（建议不做，见 §二） | — |
| IN-2 `q3_e3_INV2_cn.py` | CN＋Rannacher 独立实现互验 | B1：物料坐标下 CN 的几何项需重推导 | **大** |
| IN-3 `q3_e5b_INV3_global.py` | Morris＋Sobol（48 点＋严格轨抽查） | A3：换参数范围与输出量（t_dry） | 小 |
| IN-4a `q3_gci_INV4_tend.py` | t_end 离散误差带（空间 GCI＋时间三档） | A1：换 Q4 驱动 | 小 |
| IN-4b `q3_uq_INV4_lowC.py` | 低 C 端 D 截断族 UQ＋LHS 分位 | A2：Q4 的 D 公式（附录4）需加 C_fr 钳位 | **中（唯一实质改动）** |
| XCHK-1/2 | 分辨率排除性／界面取法×网格 2×2 | B7（可选） | 小 |
| Q3 postcheck `q3_postcheck.py` | 解析两界夹逼 | A5：Q4 版下界/上界推导 | 小（纯理论＋级数解） |

### 1.3 Q4 已有基础（本轮检验闭环后）

- `q4_w6_lib.solve_to_dry` 已支持 **N/n_sub/t_corr/d_fac/hcv/kmv/dT/dC/L/fixR/hist_every** 全参数 ⟹ A1/A3/A4/B4/B6/B7 **零核改动**；
- 交付口径已含 E1（24h GCI）、E5（OAT）、E7/E8；主力 t_dry=50.6523 h（50.6523±? 待 A1 升级为带误差带）。

---

## 二、候选清单（A 建议执行｜B 可选｜C 不建议）

### A 级（建议本轮一次性执行；合计预计 60–80 min，Pool(5) 并行）

| # | 名称 | 内容与产出 | 依据／复用 | 代码改动 | 预计成本 |
|---|---|---|---|---|---|
| **A1** | **t_dry 离散误差带（GCI）** | 空间 N=160/320 全量事件求解（t_dry 对照）＋时间 n_sub=32/64 两档 ⟹ **答案升级为 50.6523±δ h**（合成口径＝两者取大，Q3 同款） | IN-4a 模板；GCI 方法学 | 零（solve_to_dry 已支持） | ~30 min（N=320 全量 ~17 min 为大头） |
| **A2** | **低 C 端 D 外推 UQ（RK-1 闭环）** | 附录4 的 D 加 C_fr 截断（C<C_fr 冻结），截断族 8–10 档（0.1–0.5）＋LHS 分位 P5–P95 ⟹ 量化"外推段贡献 x%–y% 时长"，**模型评价核心局限定量化** | IN-4b 模板；RK-1；Q3 结论"外推段贡献 43%–66%" | **唯一实质改动**：`_D_nb`/`_faceD4_nb` 加 c_fr 钳位形参（默认 None＝交付口径不变，回归零风险） | ~25 min（截断使 D 变大、t_dry 变短，可用 n_sub=16） |
| **A3** | **全局灵敏度（Morris＋Sobol）** | 参数域 {D₀ 缩放, h, k_m, T∞, C∞}（＋可加 C_fr 不确定度作为第 6 维与 A2 联动）；输出 t_dry；Sobol S_T＋交互项 | IN-3 模板；灵敏度方法学锚点 | 零（采样驱动调 solve_to_dry） | ~30 min（Morris 24 轨＋Sobol 抽查，n_sub=16，Pool(5)） |
| **A4** | **判据敏感度** | C_crit ∈ {0.18, 0.13} 两档全量 ⟹ "判据收紧 x% 使 t_dry +y%" | Q3 E-P3（38.33/57.53/85.09 h） | 零（改 CC） | ~18 min |
| **A5** | **解析两界夹逼（理论）** | 下界：集总/均匀梯度极限（或 Adrover 型等温收缩解的量级）；上界：固定半径 R_end 的圆柱渐近解（Fo→∞ 渐近，级数解首项）⟹ t_dry ∈ [下界, 上界] 夹逼表，与 50.6523 h 对照 | Q3 E2-替代（26.8/57.53/97.7 h）；降速段/Biot 判据锚点 | 零求解（纯推导＋级数解脚本） | ~5 min |
| **A6** | **压缩温升解析包络（理论）** | 由守恒形式推导 T 上界：绝热极限 d(ρcpTR²)=0 ⟹ T·R² 增长包络 vs 对流散热时标比较 ⟹ 给 E4 的"T≤压缩包络"一个**解析上界**，并与实测 Tmax=54.63 ℃ 对照 | MB-11；F-Q4-2；绝热判决实验 | 零求解（推导＋一次核验） | ~5 min |
| **A7** | **收缩–物性双效应分解** | fixR＋附录4 对照档（R≡R₀、其余同交付口径）⟹ 定量分解"收缩加速 vs 附录4 扩散减慢"两效应（F-Q4-5 目前只有定性归因：50.65 vs 57.53 h 混合了两问物性差） | 现有 fixR 旗标；Q3 XCHK 思路 | 零 | ~10 min |

### B 级（可选；默认不执行，如需可并入同一批次）

| # | 名称 | 内容 | 价值 vs 代价 | 判 |
|---|---|---|---|---|
| B1 | 独立实现互验（CN＋Rannacher 移动域版） | 物料坐标下 CN 的几何项重推导＋独立实现 | 验证价值已被 E3-g-②（3.7e-8 vs Q2 生产核）＋跨驱动逐位一致**大部分覆盖**；改动大 | **C（不建议）** |
| B2 | 自适应时间步（E9/IN-1 移植） | 效率主张 | 主力仅 487 s，无瓶颈（Q3 决策 7"有意精简"同款） | **C** |
| B3 | 非均匀收缩律对照（RK-5） | 需引入内部收缩映射假设（改方程几何项），文献 #16/#19 表明机理复杂且无附件信息 | 改动中等偏大＋假设主观 ⟹ 建议**理论声明级**（列入局限，引 #16/#19/#15） | **C（代码）／理论级已写入结果分析** |
| B4 | 潜热中段 ΔT(t) 曲线 | 潜热轨加 hist_every=60 再跑一次 ⟹ 中段温降曲线（E8 目前只有末场差与 t_dry 偏移） | 补全 E8 呈现；~10 min | **B（可并入）** |
| B5 | 长径比端部效应 | 2D 对照成本巨大；#34 判据（12.5<15 临界）只支持**边界声明** | 理论声明级（已具备） | **C（代码）** |
| B7 | 界面取法 × 网格 2×2（Q4 版） | 用 `_iface_nb`（调和平均，保留作对照）× N∈{40,80} 对照，量化"取法缺陷在本问的量级" | 复现 Q3 方法学结论于 Q4；~6 min；注意其数值**不得作结果呈现** | **B（可并入）** |

### C 级（不建议，防范围蔓延）

- 优化/评价类"创新型算法"套件（遗传/CRITIC 等，kb 速成资料命中）——本题非优化/评价题，硬套属"为创新而创新"；
- 2D/CFD 全维模拟（成本与收益严重不匹配；长径比判据可声明）；
- 任何改动**交付口径**（M6 积分平均、D6 方案 b、N=160）的"升级"——均已被检验闭环背书，重开口径违反收口纪律。

---

## 三、建议的一次性执行包（Bundle，待裁定）

- **范围**：A1–A7（A5/A6 为纯理论，A1/A2/A3/A4/A7 为求解档；B4/B7 如裁定纳入则 +2 档）。
- **目录**：`11-3/Q4/innov/` 副本专区（**不动任何正式产物**；交接单纪律：只读复用 Q3/innov 模板）。
- **唯一代码改动点**：`q4_core` 副本中 `_D_nb`/`_faceD4_nb` 增加可选 `c_fr` 钳位形参（默认 None＝与交付口径逐位一致，先跑回归对照再用于 UQ）。
- **新脚本（4 支，均带 [SCALE]＋--go）**：
  1. `q4_INV1_gci_tend.py`（A1）；
  2. `q4_INV2_uq_lowC.py`（A2，含 LHS）；
  3. `q4_INV3_global.py`（A3，Morris＋Sobol＋严格轨抽查）；
  4. `q4_INV4_aux.py`（A4 判据档＋A7 fixR 档＋B4 潜热 hist＋B7 取法对照，Pool 并行）；
  A5/A6 并入 `q4_INV4_aux.py` 的理论段或单独 `q4_INV5_theory.py`（零求解）。
- **编排**：Pool(5) 并行，一批 `--go`；总墙钟预估 **60–80 min**。
- **判据（沿用 Q3 口径）**：A1 合成口径取大；A2 分位区间须声明先验主观性；A3 μ* 与弹性不可混读、S₁ 负值如实登记；对照轨数值不得作结果呈现；全部产物落 `innov/logs/`＋图数据 `fig_q4_INV*.csv`（图表总规划自 **F-44** 起编）。

## 四、理论级产出清单（零代码，可先行写入文档）

1. **两界夹逼表**（A5）：t_dry ∈ [解析下界, 解析上界]；
2. **压缩温升解析包络**（A6）：T ≤ T(R) 解析上界（绝热极限＋对流时标）；
3. **长径比边界声明**（B5 理论级）：L₀/R₀=12.5 与 #34 判据 15 的差距 ⟹ 一维圆柱近似的适用边界；
4. **两类不确定度分列框架**（Q3 发现 2 延续）：离散（A1）与模型外推（A2）不得相加。

## 五、裁定记录（★已裁定，即刻生效）

> 用户指令（本轮）："我都接受。你可以自己具体判断如何选择具体需要落地的修改。现将约束落地到相关建模文档，然后开始修改代码并验证，完成后再将结果落地文档。"

1. **Bundle 范围**：**A1–A7 全做 ＋ B4、B7 纳入**（B1/B2/B3/B5/C 级维持不做）；
2. **A2 先验**：C_fr ~ **U[0.15, 1.0]**（沿用 Q3 同区间）；
3. **A3 维度**：**6 维**（D₀、h、k_m、T∞、C∞、C_fr），与 A2 联动；保真度策略自定（Morris 筛选轨用低保真代理＋严格轨抽查，沿用 Q3 IN-3 模式，如实登记保真度）；
4. **执行细则授权**：脚本划分、并行编排、保真度与档位由 Agent 按成本/判据自行落定；全部在 `Q4/innov/` 副本专区，正式产物零改动；[SCALE]＋--go 闸门；结果完成后落地文档（innov 成果汇总＋口径总表增强项真值＋检验与决策链同步＋AI 登记＋交付包同步）。

> **★ 执行状态（本轮闭环）**：A1–A7＋B4＋B7 **全部执行完成**（IN-1～IN-5，详见《A_Q4创新项与成果汇总与采纳裁定》与《口径总表》§11.7）。
> 执行中修正两处实现缺陷：INV1 汇总单位 bug（未重跑求解，仅重算汇总）、IN-3 v1 的 LHS 分层缺陷（**v1 结果作废**，v2 重跑有效）＋INV4 首跑缺 fixR 形参（补后重跑）。
> 采纳裁定与"不得作为结果呈现"清单见汇总文档 §二。

---

## 六、变更记录

| 版次 | 变更 |
|---|---|
| v1 | 首次建立：kb 4 式检索＋Q3 资产盘点 ⟹ A/B/C 三级候选 13 项；给出一次性执行包（4 支新脚本、唯一代码改动点为 C_fr 钳位）与理论级产出清单；待用户裁定范围。 |
