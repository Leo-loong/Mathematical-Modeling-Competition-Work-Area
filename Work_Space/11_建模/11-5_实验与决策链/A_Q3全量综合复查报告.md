# A 题 · 第三问全量综合复查报告（★2026-09-11）

> ## ⚠⚠ 时点声明（**读本报告前必读**）
> 本报告出具时点**早于"界面变系数口径修正"**——当时的核心答案为 $t_{\rm end}=87.4933$ h。
> 修正后（界面系数由"两端点调和平均"改为"**沿 $C$ 的积分平均**"），核心答案已变为
> **$t_{\rm end}=57.5314$ h**，表 5、灵敏度、网格裁决等数据均随之更新。
> ⟹ **本报告中的全部数值（含"口径统一性 ✅ 通过"这一结论）仅对其所审查的当版本成立**，
> **不代表现行口径**；现行真值一律以《A_数值口径总表》**§十**为准。
> 本报告的**结构性结论**（文档体系完整、无悬空引用、无同名重复、目录组织已处置）**仍然有效**。
>
> **性质**：**T0 只读复查**——**未重跑任何求解代码**，仅清点、配对、逐值核对与静态检查。
> **基线**：`11_建模/大白话讲模型/A_第三问求解复盘.md`（用户指定：**若基线有错则改基线；若其他文件有错则对齐基线**）。
> **复查范围**：Q3 求解流程文件（工作区）＋ 结果文件 ＋ 交付包全部 Q3 相关文件（共 **14 份文档 ＋ 15 个脚本 ＋ 9 份日志/CSV ＋ 1 个结果文件**）。
> **复查手段**：两个脚本 `q3_audit_consistency.py`（口径／状态／重复／引用）与 `q3_audit_tables.py`（表 5 逐值核对）。

---

## 〇、复查结论（先说结果）

| 维度 | 结论 |
|---|---|
| **口径统一性** | ✅ **通过**——表 5 数据在 4 份文档中**逐值一致**；核心标量（$t_{\rm end}$、判据、网格、步长、熔断）**无矛盾** |
| **残留矛盾** | ✅ **无实质矛盾**（扫描报出的 3 处经人工核实**均为否定句或变更记录描述**，属误报） |
| **重复** | ✅ **无**（无同名多份文档、无内容重复的台账） |
| **缺口** | ⚠ **发现 6 项**，其中 **5 项已当场修复**、1 项**登记待议**（见 §三） |
| **结构清晰度** | ✅ **已处置**——Q3 目录补建 `code/` 与 `logs/`（**复制不移动、原件保留、不重跑**，见 §四.4） |

**一句话**：**数值口径是干净、自洽的**；发现的问题集中在**文档状态滞后与目录组织**，均已处置或登记。

---

## 一、基线文件的处置（**关键前提**）

### 1.1 ⚠ 基线文件原本**不存在**

按你指定的路径 `11_建模/大白话讲模型/A_第三问求解复盘.md` 检索：**该文件此前并未建立**
（该目录当时只有第一、二问的复盘）。由于基线是整个复查的**裁判标准**，

**处置**：**新建**该文件（以《A_Q3求解记录》与全部检验日志为素材，沿用第一、二问复盘的"大白话"体例：
〇 三句话结论 → 一 题目在问什么 → 二 关键数字 → 三 模型 → 四 怎么算 → 五 结果 → 六 坑与发现 →
七 检验与自评 → 八 代号对照表 → 九 一句话总结），并在文首声明
"**本文是第三问的口径基准——若与其他文档不一致，以本文为准**"（与第二问复盘同一纪律）。

### 1.2 以基线为准的核对结果

- **表 5 的 45 个代表值**（9 个时刻 × 5 列）：基线 ↔ 结果分析 ↔ 口径表 §10.2 ↔ 求解记录 **全部一致** ✅
- **末行达标值**（0.1500／0.1487／0.1447／0.1371／0.0520）：四份文档**全部一致** ✅
- **核心标量**：$\Delta$ 无矛盾 ✅

> **一处"表述差异但非矛盾"**：基线用"小时数／百分数"叙述灵敏度（如"延长 12.25 h"），
> 而技术文档用"弹性系数 $S$"。二者**等价**。为使基线成为**完整**的裁判标准，
> **已在基线中补上 $S$ 值**（见 §三 缺口 4）。

---

## 二、逐项复查结果

### 2.1 口径一致性（**通过**）

| 口径项 | 基线值 | 全库一致性 |
|---|---|---|
| 烘干结束时间 | **87.4933 h** | ✅ 一致（4 份文档均出现） |
| 达标判据 | **$C<0.15$**（严格小于，各处） | ✅ 一致 |
| 空间网格 | **$\Delta r=0.25$ mm（$N=80$／81 节点）** | ✅ 一致 |
| 内部步长 | **$1/32$ s**（每个 60 s 输出步含 1920 子步） | ✅ 一致 |
| 环境延拓 | **50.00 ℃／0.04999 kg/kg（H6）** | ✅ 一致 |
| 熔断上界 | **120 h** | ✅ 一致 |
| 内层迭代上限 | **30 次**（Q2 修正值） | ✅ 一致 |
| 物性体系 | **附录3，全程无切换（K10）** | ✅ 一致 |
| 几何 | **半径固定 2 cm（不引入附件2 收缩）** | ✅ 一致 |

### 2.2 检验结果一致性（**通过**）

| 检验 | 结果 | 一致性 |
|---|---|---|
| 时间收敛 | $\le4.34\times10^{-5}$（两侧加密） | ✅ |
| 空间收敛 | 内部 $\le0.103\%$；**表面 $0.313\%$** | ✅（4 处文档均声明） |
| 与 Q2 重叠段 | **逐位差 $0$** | ✅ |
| 守恒核算 | **$2.387\times10^{-3}$** | ✅ |
| 解析量级校验 | **97.7 h vs 87.49 h（差 12%）** | ✅ |
| 灵敏度排序 | $T_\infty$ ＞ $D_0$ ＞ $C_\infty$ ＞ $k_m$ ≫ $h$ | ✅ |

### 2.3 状态一致性（**通过**）

- `A_Q3求解思路与工作流.md` 已由"计划态"升级为 **v4（已完成）**，W0–W8 状态**已回填** ✅
- 扫描报出的"计划态"命中**均为变更记录中对历史版本的描述**（正常留痕）✅

### 2.4 引用完整性（**通过**）

- 文档中引用的 **`q3_*.py` 脚本名 100% 存在**，**无悬空引用** ✅
- 文档间互相引用的 `A_Q3*.md` **全部可达** ✅

---

## 三、发现的缺口与处置（6 项）

| # | 缺口 | 级别 | 处置 |
|---|---|---|---|
| **1** | **基线文件不存在** | ★★★ | ✅ **已新建**（见 §一） |
| **2** | `00_建模包执行要点.md` 的目录树**只列了第一问复盘**，未列第二、三问 | ★★ | ✅ **已补**（并标注"各问的口径基准"） |
| **3** | `A_第二问求解复盘.md` 的《代号对照表》**三处过时**：检验编号只到 `E1–E7`（缺 E8 潜热／E9 自适应）、可删减编号只到 `S-18`、图号只到 `F-20` | ★★ | ✅ **已修正**（分别更新为 `E1–E9`、`S-01…S-28`、`F-01–F-35`） |
| **4** | 基线**未给灵敏度弹性系数 $S$**（只有小时数），不利作精确裁判 | ★ | ✅ **已补**（新增 $S$ 栏，含 $k_m=+0.70$、$h\approx-0.001$、$T_\infty=-3.0/-3.25$、$D_0=-2.34/-1.34$，并给出"如何读 $S$"的说明） |
| **5** | 基线**未交代**两项过程指标：内层最大残差、计划中"效率方案对照"**实际未做**的例外 | ★ | ✅ **已补**（§7.2 加注：残差 $1.386\times10^{-4}$ 且**上限从未被截断**；W4 未做的理由） |
| **6** | Q3 **目录未建 `code/` 子目录**（与包级约定不一致） | ★★ | ⚠ **登记待议，未擅自移动**（理由见 §四） |

---

## 四、文件结构与组织评估

### 4.1 当前结构

| 位置 | 内容 | 数量 | 评价 |
|---|---|---|---|
| `11_建模/大白话讲模型/` | 三份复盘（**含新建的第三问**） | 3 | ✅ 清晰、命名统一 |
| `11_建模/11-3_算法与管线/Q3/` | **2 md ＋ 15 py ＋ 8 txt ＋ 1 csv ＝ 27 个文件平铺** | 27 | ⚠ **未分目录** |
| 同上（Q2 对照） | `Q2/code/`（42 文件）＋ 6 个 md **分开** | — | ✅ 清晰 |
| `20_交付包/`（Q3 相关） | 模型叙述 2 ＋ 方法学 1 ＋ 代码与复现 9 ＋ code/ 15 ＋ 图表卡 3 | 30 | ✅ 分工明确 |
| `20_交付包/09_代码与复现/results/` | `result3.xlsx`（422.6 KB） | 1 | ✅ |

### 4.2 结构不一致：Q3 未按约定分 `code/`（**已处置**）

包级约定（《00_建模包执行要点.md》）明确要求按问目录下分 `code/`（可运行脚本）与 `logs/`（运行与检验日志）。
**第二问遵守了**（`Q2/code/` 42 个文件），**第三问此前没有**（27 个文件全在根）。

### 4.3 约束与技术原因

**技术原因**：Q3 的脚本用"**从脚本位置向上数三层**"来定位工作区根，并**用相对路径导入第二问的求解核**：

```python
HERE = os.path.dirname(os.path.abspath(__file__))
WS   = os.path.dirname(os.path.dirname(os.path.dirname(HERE)))   # 向上 3 层
sys.path.insert(0, os.path.join(os.path.dirname(HERE), 'Q2', 'code'))
```

若把脚本**移动**到 `Q3/code/`，**上溯层数需改为 4 层、且 Q2 的相对路径要再退一层**——
即**必须同时改代码**，而改完**必须重跑验证**才能确认未破坏。

### 4.4 ★最终处置：方案 B（**复制不移动 ＋ 不重跑**）

**用户裁定**：按方案 B 处理，但**不移除原代码**、**不重跑 Q3 代码**。

**执行结果**：

| 项 | 处置 |
|---|---|
| `Q3/code/` | **已建**，16 个 `.py` 的**副本**（原件保留于根目录） |
| `Q3/logs/` | **已建**，9 个 `.txt`／`.csv` 的**副本**（原件保留于根目录） |
| **原文件** | **全部保留**（`Q3/` 仍为**唯一运行位置**，未改一行代码） |
| **一致性** | ✅ 经 **MD5 逐字节比对**：16／16 脚本、9／9 日志**副本与原件完全一致** |
| **是否重跑** | ❌ **未重跑任何代码**（按用户指令） |
| **歧义防护** | 新建 `Q3/00_目录说明.md`，明确"**根目录＝运行位置（主）；`code/`／`logs/`＝浏览副本（副）；不一致以根目录为准**"，并规定"**新增脚本须先在根目录验证、再复制**"的次序 |

> **代价与权衡**：本方案**不引入任何代码改动风险**（故无需重跑），
> 但**同一内容存在两份**——已通过"主副关系 ＋ 版本纪律"的明文约定来控制版本漂移风险。
> 若后续希望彻底消除副本，只需**以根目录为准重建**并同步调整脚本的上溯层数（届时须重跑验证）。

---

## 五、其他发现

| # | 发现 | 说明 |
|---|---|---|
| 1 | **`10_赛题/A题/附件/附件3/result3.xlsx`（9.1 KB）是题目自带模板** | 与交付结果同名但**不同物**，容易误认。已在《A_代码复现》中明确交付结果位于 `20_交付包/09_代码与复现/results/` |
| 2 | **Q3 未做"解析对拍"与"独立实现互验"** | 属**有意精简**（同代码同离散，用"与 Q2 逐位一致 ＋ 圆柱渐近解"替代），**已在基线 §7.1 与日志索引 §三 中说明理由**。**（后续更新：独立实现互验已补做**——CN ＋ Rannacher 独立轨，$t_{\rm end}$ 相对差 $3.0\times10^{-7}$；解析对拍仍由"解析量级校验"承担。见《A_Q3创新项与外部核验·成果汇总与采纳裁定》） |
| 3 | **两项"只读分析"脚本**（`q3_impact_check.py`、`q3_surface_theory.py`） | 已明确标注**不参与交付数值**，仅用于方法论评估 |
| 4 | **本轮复查新增 2 个脚本** | `q3_audit_consistency.py`、`q3_audit_tables.py` 已同步至交付副本（工作区 15 ＝ 交付包 15） |
| 5 | **表面列声明已在 4 处文档中一致出现** | 口径表 §10.5／方法学 §二.3／结果分析 §五／F-34 规格卡 ⟹ **不存在"某处漏声明"的风险** |

---

## 六、变更清单（本轮）

| # | 文件 | 动作 |
|---|---|---|
| 1 | `11_建模/大白话讲模型/A_第三问求解复盘.md` | **新建**（基线） |
| 2 | `11_建模/大白话讲模型/A_第二问求解复盘.md` | 修正代号表 3 处过时项 |
| 3 | `11_建模/00_建模包执行要点.md` | 目录树补第二、三问复盘 |
| 4 | `11_建模/11-5_实验与决策链/A_Q3全量综合复查报告.md` | **新建**（本文件） |
| 5 | `20_交付包/09_代码与复现/code/q3_audit_*.py` | 新增 2 个复查脚本 |
| 6 | `11_建模/11-3_算法与管线/Q3/code/` | **新建**（16 个脚本副本，**原件保留**） |
| 7 | `11_建模/11-3_算法与管线/Q3/logs/` | **新建**（9 个日志/数据副本，**原件保留**） |
| 8 | `11_建模/11-3_算法与管线/Q3/00_目录说明.md` | **新建**（主副关系 ＋ 版本纪律 ＋ 分类索引） |

> **本轮未重跑任何代码、未改动任何数值结果、未删除任何原文件。**

---

## 七、遗留待裁

1. **Q3 目录的"两处副本"**：当前按你的指令**保留原文件 ＋ 新增副本**（主副关系已明文约定）。
   若日后希望**彻底消除副本**，需"**以根目录为准重建 ＋ 同步调整脚本上溯层数 ＋ 重跑验证**"——
   该项**已登记，暂不执行**。
2. 是否需要把本报告的核心结论**同步一句**到《A_Q3求解记录》的"遗留与移交"栏？

---

## 八、复查方法说明（可复现）

| 脚本 | 检查内容 | 用法 |
|---|---|---|
| `q3_audit_consistency.py` | 关键数值命中矩阵、状态与矛盾检测、重复文档、交叉引用有效性 | `python q3_audit_consistency.py` |
| `q3_audit_tables.py` | 表 5 的 45 个代表值**逐值**与 `result3.xlsx` 比对 | `python q3_audit_tables.py` |

> 两者均为**只读**：不写交付物、不改数值。
