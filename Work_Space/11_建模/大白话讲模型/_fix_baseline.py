# -*- coding: utf-8 -*-
import io, os
p = "A_第三问求解复盘.md"
t = io.open(p, encoding="utf-8").read()
a = t.index("### 坑三：")
b = t.index("## 七、效果如何")
NEW = """### 坑三：第二问那条"全域单调不增"的检查规则在长时程下的适用性

第二问只有 3 小时，含水率全域单调下降，所以断言里写了"全域不增"。
长时程下，表面**可能**出现"局部增湿"式回升（内部水分补充快于表面脱除）。

**修正口径后的实测**：全程 3452 个输出步中，表面列回升 **0 步**（最大单次 $-1.3\\times10^{-6}$，即无回升）。
为表述更稳妥，断言仍保留四条——**① 中心单调不增；② 全场上界＝初值；③ 非负；④ 达标后末行全域 < 0.15**。

### 坑四：算出来 57.5 小时，比我们估的 26 小时长得多——**是估算方法错了**

**现象**：动手之前我们估"大约 26 小时"（区间 20–45 h），实测算出 **57.5 小时**。

**排查**：不是算错（有多重检验支撑，见 §七）。**是我们原来的估算方法有缺陷**——
原来用"空间均匀假设下的分段积分"，本质上**把扩散方程当成了单点的常微分方程**，
忽略了"内部还有浓度梯度"这件事，于是**系统性地低估**了时间。

**改进**：改用**圆柱渐近解**（这一类问题的标准解析近似）重新估，得到 **97.7 小时**
（该式取 $D(C_{\\rm crit})$ 为常数、忽略"早期 $D$ 更大"，故为**上界**）。

> **论文写法**：解析估计给出 **下界 25.8 h（空间均匀假设）／上界 97.7 h（圆柱渐近解）**，
> 数值解 **57.5 h 落在其间** ⟹ **量级自洽**，构成独立于数值方法的旁证。

### 发现一：★表面扩散边界层的"网格限制"与**界面口径修正**（**必须写进论文的边界声明**）

第三问要算到含水率很低（中心 0.15、表面 0.05）。这时 $D$ 已**降到初始的几千分之一**，
**表面扩散边界层厚度 $\\delta=D(C_R)/k_m$ 远薄于网格步长**：

| 工况 | $\\delta/\\Delta r$（$\\Delta r=0.25$ mm） | 结论 |
|---|---|---|
| 第一问 | **约 60** | 网格远远够细 ✓ |
| 第二问 | **约 51** | 网格远远够细 ✓ |
| **第三问（表面）** | **约 0.03** | ⚠ 薄层跨不过，**须查格式是否失准** |

**复检的关键认识（重要）**：薄层未分辨**本身不会**让有限体积格式失准——
用**有解析解**的"常数 $D$＋Robin 圆柱"问题（Carslaw-Jaeger 级数）对拍，
$\\mathrm{Bi}=2\\to20000$（即 $\\delta/\\Delta r=40\\to0.004$）下误差**仍 $\\le3\\times10^{-3}$ 且与网格无关**。
**真正的根因是"界面扩散系数取法"**：

> **根因**：原代码对界面 $D$ 取**两端点的调和平均** $2D_lD_r/(D_l+D_r)$——
> 该式**只对"界面处系数间断（分段常数）"成立**。而尾部单个网格胞内 $C$ 由 $\\sim0.15$ 掉到 $\\sim0.05$，
> $D$ 随之**跨约 285 倍**；此时调和平均 $\\approx2\\min(D)$，**严重低估**胞内有效扩散系数
> （代表单元估算低估约 $73$ 倍）。
> 正确取法（单元内稳态通量 $J=\\frac1{\\Delta r}\\int D\\,\\mathrm dC$）是**沿 $C$ 的积分平均**
> $\\langle D\\rangle=\\frac1{\\Delta C}\\int D\\,\\mathrm dC$。

**修正后的实测（同一 $\\Delta r=0.25$ mm，仅换界面取法）**：

| 界面 $D$ 取法 | $t_{\\rm end}$/h | 24 h $C(R)$ |
|---|---|---|
| 调和平均（旧） | **87.4933** | 0.0567 |
| **沿 $C$ 的积分平均（本问采用）** | **57.5314** | 0.0665 |

且**表面加密网格**（口径不变、仅加密）亦给出 $57.53$ h ⟹ **两条独立路线相互印证**。

> **论文写法（边界声明）**：
> ① **界面变系数一律取"沿 $C$ 的积分平均"**（本问采用）；
> ② 修正口径后，表 5 各列（含 2.0 cm 表面列）均达 **4 位小数稳定**；
> ③ 低 $C$ 端 $D$ 的**外推**仍是**最大不确定性源**（见 §七.3）。

> **附带结论**：该口径**对 Q1／Q2 无影响**——二者的含水率区间内 $D$ 的胞内变化极小
> （对照实验：同一 3 h 问题下 $C(0)$ 六位小数完全相同、$C(R)$ 差 $\\le10^{-6}$）。

### 发现二：内部步长与网格的收敛性

| 维度 | 结果 | 判定 |
|---|---|---|
| **时间步长 1/32 秒** | 与相邻档（1/16、1/64 s）相对变化 $\\le1.0\\times10^{-4}$（**绝对** $\\le2\\times10^{-5}$） | ✅ **低于 4 位小数输出精度** |
| **空间网格（$N=20$–$160$）** | 相邻档相对变化 $\\le2.9\\times10^{-4}$（$N{=}80\\to160$ 时 $2.15\\times10^{-5}$） | ✅ **4 位小数稳定** |
| **表面列**（修正口径后） | $N{=}80$ 与 $N{=}160$ 的相对差 $\\le9.8\\times10^{-5}$ | ✅ 4 位小数稳定 |

"""
t = t[:a] + NEW + "---\n\n" + t[b:]
io.open(p, "w", encoding="utf-8").write(t)
print("OK len=%d" % len(t))
