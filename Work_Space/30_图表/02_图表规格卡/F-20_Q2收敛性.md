# 图表规格卡：F-20 Q2 收敛性（空间／时间）

| 要素 | 内容 |
|---|---|
| **图号/图名** | 图 F-20：第二问空间与时间收敛性 |
| **目的** | 证明 Q2 的网格与步长选择**有据可依**（非经验取值）：空间二阶、时间一阶，且所选步长的相对变化**远小于输出精度要求** |
| **图类型与理由** | 类型：**双联折线**（与 F-06 同构，便于全题统一）；理由同 F-06——收敛论断须同时给出"值—步长"关系与判据参考线 |
| **数据源** | **`04_图表包/data/fig_q2_gci.csv`**——**本包内自足**（列：`group`（spatial／time_T／time_C）、`delta`、`value`、`rel_change`）；过程留痕见 W2 预试验日志（追溯用，非取数用） |
| **坐标轴** | 左子图 X：步长（$\Delta r$ / mm 或内部步长 / s），**对数轴**；Y：终态关键量（$T(R)$ ℃ 或 $C(R)$ kg/kg）；右子图 X 同；Y：**相对变化**（无量纲，对数轴）＋ $O(h)$／$O(h^2)$ 灰虚线参考 |
| **标签文本（逐字，全中文）** | 图题「F-20 Q2 收敛性（空间／时间）」；坐标轴与色标**逐字**采用本卡「坐标轴」行：左子图 X：步长（$\Delta r$ / mm 或内部步长 / s），**对数轴**；Y：终态关键量（$T(R)$ ℃ 或 $C(R)$ kg/kg）；右子图 X 同；Y：**相对变化**（无量纲，对数轴）＋ $O(h)$／$O(h^2)$ 灰虚线参考；**英文列值映射**：`group`：`spatial`→空间、`time`→时间；**图内一律中文**（仅符号、单位、公认缩写可保留）；**禁止**出现 CSV 英文列值／裸代号 |
| **字体与公式** | 中文 **思源黑体／微软雅黑**（macOS `Heiti SC`／`PingFang SC`；Linux 须装中文字体）；西文 **Arial**；**图内公式 `Cambria Math`／Times，10 pt，Italic，`Interpreter='latex'`（禁截图）**；字号层级 2:1.5:1.2:1:0.85 且**最小 ≥7 pt**；**导出 300 DPI PNG ＋ 矢量 PDF 并嵌入全部字体**（`exportgraphics(...,'ContentType','vector')`）。依据：`00_规则/图表设计语言.md` **§2.1 六条硬约束** |
| **配色与尺寸** | 与 F-06 一致：空间‑温度 #1F3A93 ○、空间‑含水率 #F39C12 □、时间‑温度 #27AE60 △、时间‑含水率 #C0392B ◇；比例 **16:9**；参考线 #9E9E9E 虚线 |
| **绘制注意点** | ① **横轴刻度须显式设定**（0.015625／0.03125／0.0625／0.125／0.25／0.5／1），否则对数轴出现 minor 刻度标签重叠；② **必须画出 $5\times10^{-5}$ 的判定阈值参考线**（L3-07），否则读者无法判断"收敛够了没有"；③ 须标注**选定值**（$\Delta r=0.25$ mm、内部步长 $1/32$ s）及其相对变化（实测 $\le6.8\times10^{-7}$） |
| **对应文字表述建议** | 图注："图 F-20 第二问收敛性检验。虚线为判定阈值 $5\times10^{-5}$。" 正文："如表/图 F-20，空间呈二阶收敛、时间呈一阶收敛；本文选用的 $\Delta r=0.25$ mm 与内部步长 $1/32$ s 对应相对变化 $\le6.8\times10^{-7}$，**比阈值严约 70 倍**，满足 4 位小数输出要求。" |
| **支撑论证点** | 检验充分性（要点19）；**数值设置的可辩护性**（呼应 Q1 的 F-06） |
