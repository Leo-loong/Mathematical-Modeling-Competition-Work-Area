# 图表规格卡：F-44 Q4 t_dry 离散误差带（GCI）

| 要素 | 内容 |
|---|---|
| 图号/图名 | 图 F-44：Q4 烘干时长的空间/时间离散误差带（GCI） |
| 目的 | 评委一眼确认答案 t_dry=50.6523±0.0249 h 的数值可信度：空间误差可忽略、时间项主导且已给保守上界 |
| 图类型与理由 | 类型：横向条形（各档 t_dry 相对主线偏差，对数尺度）＋标注框（±0.0249 h 合成口径）；理由：量级差异横跨两个数量级，对数条形最清晰 |
| 数据源 | `30_图表/03_图数据准备/fig_q4_INV1_gci.csv`；列：kind（space/time）、key（档位标签）、N、n_sub、t_dry_s（秒）。偏差=∣t_dry(档)−t_dry(N=160,n=32)∣/3600 h |
| 坐标轴 | X：`∣Δt_dry∣ / h`（对数轴 1e-5～1e-1，刻度 10⁻ᵏ）；Y：档位（**5 档，逐字文本见下行**） |
| **标签文本（逐字，全中文）** | Y 刻度（自上而下）：`空间 N=80`／`空间 N=160`／`空间 N=320`／`时间 子步数=16`／`时间 子步数=64`；X 轴标签：`∣Δt_dry∣ / h`；图内标注：`时间三档非单调 → 取保守上界 ±0.0249 h`；图例：`空间离散`／`时间离散`。**英文代号映射（CSV 值不改）**：`space→空间`、`time→时间`、`n_sub→子步数` |
| **字体与公式** | 中文 `Microsoft YaHei`（macOS `Heiti SC`）／西文 **Arial**；公式（$t_{\rm dry}$、$\Delta$）用 `Cambria Math`／MATLAB `latex` 解释器；字号按 `04_设计语言实例/` §二（标题 10／轴标签 9／刻度 8 pt） |
| 配色与尺寸 | 引用设计语言；空间条/时间条双色；主线参考线虚线标注 0 |
| 绘制注意点 | 对数轴下 0 值不可绘（主线自身作参考线）；时间档标注"非单调→保守上界"；合成口径=取大 |
| 对应文字表述建议 | "如图 F-44 所示，空间离散误差可忽略（GCI≈5×10⁻⁶），时间离散经三档验证取保守上界 ±0.0249 h（±0.049%）" |

---

## ★ 展示层文字 · 覆盖更新（提交方 · **覆盖式**）

> **效力**：本块**覆盖**本卡前述同名项在「**展示层**」上的表述；**线上成品图与本卡不一致者，以本卡与本块为准**。
> **不变**：**数值与 CSV 列名一律不改**（唯一真值）；映射只发生在图的**展示层**，且**已写入本卡**。


### 0) **本图工单号**

> 前版 **A#11**（`space`／`time`→空间／时间）
> **清单位置**：前版 `30_图表/05_交付清单/A_图表残留问题清单.md`（A／B／C 三组）；续编 `30_图表/05_交付清单/A_图表残留问题清单_续.md`（D 组）。

### 1) 通用条款（本图适用）

- **图内一律中文**；**禁止**出现 CSV 英文列值／裸代号；
- **符号必须用数学体**（斜体，`Interpreter='latex'`）；**严禁用正文黑体直接排拉丁串**——这是本类问题的**根因**；
- **图内不得出现图号/编号**（图题由论文排版方添加）；
- 字体族、字号层级、导出与字体嵌入要求**照本卡「字体与公式」行不变**。

### 2) 本图专属（**逐字替换**）

| 位置 | 禁止写法 | **必须写成** |
|---|---|---|
| 图层/系列名 | ``space`、`time`` | **空间**、**时间** |

### 3) 核对

- 图中数字与《`A_数值口径总表`》不一致时**停下回询**（依本卡「绘制注意点」）。
