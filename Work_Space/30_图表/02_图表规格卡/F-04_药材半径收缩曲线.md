# 图表规格卡：F-04 药材半径收缩曲线

| 要素 | 内容 |
|---|---|
| **图号/图名** | 图 F-04：药材半径收缩曲线（附件 2，145 节点） |
| **目的** | **量化尺寸收缩幅度**，支撑第 4 问采用移动边界模型的必要性 |
| **图类型与理由** | 类型：**折线图**；理由：$R(t)$ 是一维时序，折线可直观读出收缩速率与平台段 |
| **数据源** | `03_图数据准备/fig_q4_shrinkage.csv`（**图数据唯一真值**）；列说明：`t_h`＝时间（h）、`R_cm`＝药材半径（cm）；源自**附件 2**（0–259200 s，步长 1800 s，145 节点） |
| **坐标轴** | X：时间 $t$（h），0–72；Y：药材半径 $R$（cm），1.15–2.05（**纵轴零点不截断时须注明**，若截断须显式标注） |
| **标签文本（逐字，全中文）** | 图题「图 F-04 药材半径随时间的收缩曲线」；X 轴标签「时间 / h」；Y 轴标签「药材半径 / cm」；**图内一律中文**（仅符号、单位、公认缩写可保留）；**禁止**出现英文列名与裸代号（CSV 列名 `t_h`／`R_cm` **不得**直接上图） |
| **字体与公式** | 中文 **思源黑体／微软雅黑**（macOS `Heiti SC`／`PingFang SC`）；西文 **Arial**；**图内公式 `Cambria Math`／Times，10 pt，Italic，`Interpreter='latex'`（禁截图）**；字号层级 2:1.5:1.2:1:0.85 且**最小 ≥7 pt**；**导出 300 DPI PNG ＋ 矢量 PDF 并嵌入全部字体**。依据：`00_规则/图表设计语言.md` **§2.1** |
| **配色与尺寸** | 主线＝**Nature 皇家蓝**（语义冻结）；单图 4:3；线宽 1.5；参考线（如 $t=67$ h 之后取常值）用**灰虚线** |
| **绘制注意点** | ① **末端平台必须体现**：$t\ge241200$ s（≈67 h）后 $R$ 恒为 **1.1980 cm**（口径 H7）——不要在末端画出虚假的继续下降；② **标注总收缩幅度**：$R_0=2.0000$ cm → $1.1980$ cm（**约 40.1%**）；③ 单位用 **h 与 cm**，与论文表 6／`result4` 的距离轴保持一致；④ 如需叠加"收缩率–干基含水率"关系，另见 F-50 体系，**本卡只画 $R(t)$** |
| **对应文字表述建议** | 图注："图 F-04 药材半径随时间的收缩曲线。"；正文引用句："如图 F-04 所示，药材半径由 2.00 cm 收缩至 1.198 cm（约 40.1%），收缩幅度不可忽略，第 4 问须按移动边界处理。" |
| **支撑论证点** | 机理严谨性；假设 H7／H8 的量化依据 |

> **状态**：**卡已出（事后补）**；成品 `图04` 已由绘图方交付；图数据 CSV（2.26 KB）已入 `03_图数据准备/` 与交付侧 `data/`。
