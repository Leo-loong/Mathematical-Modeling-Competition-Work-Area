# F-26 灵敏度结构雷达图（Q1 vs Q2）

> **本卡效力**：遵循《04-2_项目图表设计语言》§一～§五（配色／字体／尺寸／图型／一致性）。
> 该文件为**全项目视觉规范的唯一出处**，本卡只做本图的决定。

| 项 | 内容 |
|---|---|
| **编号** | **F-26** |
| **图名** | 灵敏度结构雷达图（Q1 vs Q2） |
| **图型** | 雷达图（双叠层） |
| **用于位置** | 5.2.5 模型检验 |
| **优先级** | ★★★★☆ |
| **数据源** | Q1：**`fig_q1_gs.csv`（Morris $\mu^*$，列 `param,mu_star,…`）**（**已按本卡「⚠ 口径更正」改指**；原写 `fig_tornado_data.csv`（OAT）已作废）；Q2：**`fig_q2_sens_S.csv`**（列 `output,parameter,rel_minus,rel_plus,S_half`；`S_half=(rel_plus−rel_minus)/2`）——**本包内自足** |
| **坐标轴** | 5 个轴＝5 个参数（`h`／`km`／`D0`／`Tinf`／`Cinf`；**`C0` 属初值直传、不入选**）；径向＝$\vertS\vert$ |
| **标签文本（逐字，全中文）** | 图题「图 F-26 灵敏度结构雷达图（Q1 vs Q2）」；坐标轴与色标**逐字**采用本卡「坐标轴」行：5 个轴＝5 个参数（`h`／`km`／`D0`／`Tinf`／`Cinf`；**`C0` 属初值直传、不入选**）；径向＝$；**英文列值映射**：`output`→中文量名；`parameter`→符号（同 F-13 规则）；**图内一律中文**（仅符号、单位、公认缩写可保留）；**禁止**出现 CSV 英文列值／裸代号 |
| **字体与公式** | 中文 **思源黑体／微软雅黑**（macOS `Heiti SC`／`PingFang SC`；Linux 须装中文字体）；西文 **Arial**；**图内公式 `Cambria Math`／Times，10 pt，Italic，`Interpreter='latex'`（禁截图）**；字号层级 2:1.5:1.2:1:0.85 且**最小 ≥7 pt**；**导出 300 DPI PNG ＋ 矢量 PDF 并嵌入全部字体**（`exportgraphics(...,'ContentType','vector')`）。依据：`00_规则/图表设计语言.md` **§2.1 六条硬约束** |
| **图内要素** | ① 两个多边形（Q1 半透明蓝、Q2 半透明橙）；② 每个轴标注实际 $\vertS\vert$ 数值；③ 右下角注明各层对应的输出量（如 $C(R)$）；④ **每层先按自身 $\vertS\vert$ 最大值归一，再画**（见下"口径提示"） |
| **⚠ 口径提示** | 两层 $S$ 的**归一化口径不同**：Q1 的 `S_norm` 含**扰动幅度归一**（分母含 $\Delta_{\rm rel}$），Q2 的 `S_half` 为**相对变化的半差**（不含 $\Delta_{\rm rel}$）。**不得跨层比较绝对高度**；须各层自归一后比较**形状与排序**，并在图注声明"两层按各自最大 $\vertS\vert$ 归一" |
| **配色与尺寸** | Q1 皇家蓝 #1F3A93（alpha=0.35）、Q2 金黄 #F39C12（alpha=0.35） |
| **输出** | **300 DPI PNG ＋ PDF 矢量**（尺寸按《设计语言》§三：单图 8×6 cm／通栏 16×9 cm／热力图 1:1） |
| **⚠ 本图注意事项** | 每张雷达图**只对同一个输出量**（如 $C(R)$），**不得**把 4 个输出混在一张图上 |

> **⚠ 口径更正（本轮落地）**：Q1 侧数据源由原卡的 `fig_tornado_data.csv`（OAT，参数集 $T_0/h/D_0/k_m/C_0$）
> **改为 `fig_q1_gs.csv`（Morris $\mu^*$，参数集 $h/k_m/D_0/T_\infty/C_\infty$）**——
> 只有后者能给出本卡所定的 **5 个公共轴**（与 Q2 的 `fig_q2_sens_S.csv` 对齐；`C_0` 按卡不入选）。
> 两层度量口径仍不同（Morris 绝对效应 vs OAT 半差）⟹ **各层自归一，只比较形状与排序**。

---

## 一、这张图**为什么应该存在**（依据《设计语言》§六 配图判据）

- **触发条件**：「灵敏度分析无图」★★★★☆ —— 灵敏度章节**有表格无图**（雷达图为其推荐图型）
- **图文比检查**：该节现有图数若已达标则不补；本图经判定为**应当补充**。
- **不违反"禁止补图"**：本图数据**全部来自已完成的求解／检验**，**无任何编造数值**。

## 二、写作者使用指引

1. **数据从哪取**：见上表"数据源"栏；CSV 均在 `04_图表包/data/`（**本包内**）。
2. **必须保留**：上表"图内要素"与"⚠ 注意事项"两栏的全部要求。
3. **不得改写**：配色、线型语义、标记形状按《设计语言》§五 固定，**不得逐图自创**。
4. **图注**：须写"图意 ＋ 数据来源 ＋ 关键结论提示（≤3 句）"，**含句号结尾**。
5. **正文引用**：必须在该节**显式引用**（"如图 X 所示"），否则删除本图。

---

## 三、变更记录

| 版次 | 内容 |
|---|---|
| v1 | 依《设计语言》§六 配图判据新增（Q2 缺口补检 · 图表扩充） |

---

## ★ 展示层文字 · 覆盖更新（提交方 · **覆盖式**）

> **效力**：本块**覆盖**本卡前述同名项在「**展示层**」上的表述；**线上成品图与本卡不一致者，以本卡与本块为准**。
> **不变**：**数值与 CSV 列名一律不改**（唯一真值）；映射只发生在图的**展示层**，且**已写入本卡**。


### 0) **本图工单号**

> 前版 **A#4**（`CR`→$C_R$；`vs`→对比）
> **清单位置**：前版 `30_图表/05_交付清单/A_图表残留问题清单.md`（A／B／C 三组）；续编 `30_图表/05_交付清单/A_图表残留问题清单_续.md`（D 组）。

### 1) 通用条款（本图适用）

- **图内一律中文**；**禁止**出现 CSV 英文列值／裸代号；
- **符号必须用数学体**（斜体，`Interpreter='latex'`）；**严禁用正文黑体直接排拉丁串**——这是本类问题的**根因**；
- **图内不得出现图号/编号**（图题由论文排版方添加）；
- 字体族、字号层级、导出与字体嵌入要求**照本卡「字体与公式」行不变**。

### 2) 本图专属（**逐字替换**）

| 位置 | 禁止写法 | **必须写成** |
|---|---|---|
| 图例中变量 | ``CR` ×2` | **$C_R$**（数学斜体） |
| 图上对比说明 | ``vs`` | **对比** |

### 3) 核对

- 图中数字与《`A_数值口径总表`》不一致时**停下回询**（依本卡「绘制注意点」）。
