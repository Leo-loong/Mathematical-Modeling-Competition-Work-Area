# 图表规格卡：F-36 Q3 离散不确定度与误差带

| 要素 | 内容 |
|---|---|
| **图号/图名** | 图 F-36：第三问核心答案的离散不确定度（Richardson 外推与误差带） |
| **目的** | 把核心答案由单点值升级为**带误差带的结论**；分别给出**空间维**与**时间维**的离散不确定度量级 |
| **图类型与理由** | **双联图**。左：空间维（$t_{\rm end}$ vs $\Delta r$ 三档，含 Richardson 外推点与 GCI 误差棒）；右：时间维（$t_{\rm end}$ vs 内部步长三档，标注**非单调**与保守上界）。理由：两维误差**来源不同、量级差两个数量级**（$0.0006$ h vs $0.0802$ h），必须**分列**呈现，避免被误读为单一误差源 |
| **数据源** | **`04_图表包/data/fig_q3_gci.csv`**（列：`kind`、`dr_mm`／`nsub`、`t_end_h`）——**本包内自足**；过程留痕见日志 `q3_INV4_gci.log`（追溯用，非取数用） |
| **坐标轴** | 左：X $=\Delta r$/mm（对数轴，0.25／0.125／0.0625），Y $=t_{\rm end}$/h（**窄范围 57.526–57.533**）；右：X ＝内部步长/s（对数轴，$1/64$／$1/32$／$1/16$），Y $=t_{\rm end}$/h（57.49–57.58） |
| **标签文本（逐字，全中文）** | 图题「F-36 Q3 离散不确定度与误差带」；坐标轴与色标**逐字**采用本卡「坐标轴」行：左：X $=\Delta r$/mm（对数轴，0.25／0.125／0.0625），Y $=t_{\rm end}$/h（**窄范围 57.526–57.533**）；右：X ＝内部步长/s（对数轴，$1/64$／$1/32$／$1/16$），Y $=t_{\rm end}$/h（57.49–57.58）；**英文列值映射**：`kind`：`space`→空间、`time`→时间；**图内一律中文**（仅符号、单位、公认缩写可保留）；**禁止**出现 CSV 英文列值／裸代号 |
| **字体与公式** | 中文 **思源黑体／微软雅黑**（macOS `Heiti SC`／`PingFang SC`；Linux 须装中文字体）；西文 **Arial**；**图内公式 `Cambria Math`／Times，10 pt，Italic，`Interpreter='latex'`（禁截图）**；字号层级 2:1.5:1.2:1:0.85 且**最小 ≥7 pt**；**导出 300 DPI PNG ＋ 矢量 PDF 并嵌入全部字体**（`exportgraphics(...,'ContentType','vector')`）。依据：`00_规则/图表设计语言.md` **§2.1 六条硬约束** |
| **关键标注** | ① 左图标注**观测阶 $p=1.650$**、Richardson 外推值 $t_{\rm ext}=57.5275$ h、$\mathrm{GCI}_{\rm 细}=0.0011\%$；② 右图须显式标注"**三档非单调 ⟹ 不适用 Richardson，改报保守上界 $0.0802$ h**"；③ 两图共用一条水平参考线（正式答案 $57.53$ h） |
| **配色与尺寸** | 与 F-06／F-20／F-34 一致：空间 #1F3A93、时间 #27AE60、参考线 #9E9E9E；比例 **16:9** |
| **绘制注意点** | ① **纵轴范围必须收窄**，否则三档差异不可见；② 右图**不得**连成平滑趋势线——非单调本身就是要传达的信息；③ 误差带用**阴影带**表示，而非仅误差棒；④ 两个子图的纵轴**不可共用**（量级不同） |
| **对应文字表述建议** | 图注："图 F-36 第三问核心答案的离散不确定度。左为空间维（含 Richardson 外推），右为时间维（三档非单调，取保守上界）。" 正文："经三档网格外推，**空间离散不确定度约 $\pm0.001\%$**；时间维三档呈非单调，**取保守上界 $\pm0.08$ h**；二者来源不同，按较大者合成，得 **$t_{\rm end}=57.53\pm0.08$ h**。" |
| **支撑论证点** | 结论的**可信区间声明**；对应《A_Q3可靠性与方法学》**§2.6**；数值见《A_数值口径总表》**§10.8(1)** |

---

## 附：数据要点（绘制时核对）

| 维 | 档位 | $t_{\rm end}$/h |
|---|---|---|
| 空间（固定 $1/32$ s） | 0.25000／0.12500／0.06250 mm | **57.5314**／57.5281／57.5270 |
| 时间（固定 0.25 mm） | $1/16$／$1/32$／$1/64$ s | 57.4986／**57.5314**／57.5788 |

| 派生量 | 值 |
|---|---|
| 空间观测阶 $p$ | $1.650$ |
| 空间 Richardson 外推 $t_{\rm ext}$ | $57.5275$ h |
| 空间 $\mathrm{GCI}_{\rm 细}$ | $0.0011\%$（$\pm0.0006$ h） |
| 时间保守上界 | $0.0802$ h |
| **合成误差带（取较大者）** | $\pm0.0802$ h ⟹ **$57.53\pm0.08$ h** |

> **⚠ 图注纪律**：本图**仅**呈现**离散**不确定度；**模型外推不确定度**（低 $C$ 端 $D$，见 F-38）
> **不得**并入同一误差带或相加。

---

## ★ 展示层文字 · 覆盖更新（提交方 · **覆盖式**）

> **效力**：本块**覆盖**本卡前述同名项在「**展示层**」上的表述；**线上成品图与本卡不一致者，以本卡与本块为准**。
> **不变**：**数值与 CSV 列名一律不改**（唯一真值）；映射只发生在图的**展示层**，且**已写入本卡**。


### 0) **本图工单号**

> 续编 **D#4**（x 轴量纲须分列或分别注明 mm／s）
> **清单位置**：前版 `30_图表/05_交付清单/A_图表残留问题清单.md`（A／B／C 三组）；续编 `30_图表/05_交付清单/A_图表残留问题清单_续.md`（D 组）。

### 1) 通用条款（本图适用）

- **图内一律中文**；**禁止**出现 CSV 英文列值／裸代号；
- **符号必须用数学体**（斜体，`Interpreter='latex'`）；**严禁用正文黑体直接排拉丁串**——这是本类问题的**根因**；
- **图内不得出现图号/编号**（图题由论文排版方添加）；
- 字体族、字号层级、导出与字体嵌入要求**照本卡「字体与公式」行不变**。

### 2) 本图专属（**逐字替换**）

| 位置 | 禁止写法 | **必须写成** |
|---|---|---|
| x 轴量纲 | `「步长（**mm 或 s**）」——两量纲共用一轴` | **分列两轴**或**分别注明**「mm」（空间步长）／「s」（时间步长） |

### 3) 核对

- 图中数字与《`A_数值口径总表`》不一致时**停下回询**（依本卡「绘制注意点」）。
