# 图表规格卡：F-41 误差驱动自适应步长的步长轨迹

| 要素 | 内容 |
|---|---|
| **图号/图名** | 图 F-41：问题1 误差驱动自适应步长的步长轨迹（阶梯图） |
| **目的** | 证明"问题1 温度的**时间步长不驱动结果**"——即时间离散误差已被消除，而非靠经验取小步长硬压 |
| **图类型与理由** | 类型：**阶梯图**（`h_next` 对 `t`）；理由：步长是"分段常数、逐段跳变"的量，阶梯图是该语义的**唯一正确画法**（折线图会暗示步长连续变化，属误导） |
| **数据源** | `04_图表包/data/fig_q1_stepsize.csv`（**本包内**）；列：`t_s`（时间，s）、`h_next_s`（该时刻控制器选定的下一步长，s）。日志见 `code/innov/logs/q1_inv_T1_adaptive.log` |
| **坐标轴** | X：时间 $t$（s，0–1800，线性）；Y：步长 $h$（s，**对数轴**，因步长呈几何阶梯下降：1 → 0.4 → 0.16 → 0.064 → …，比率≈0.4）。刻度用 1／0.1／0.01／0.001 |
| **标签文本（逐字，全中文）** | 图题「F-41 误差驱动自适应步长的步长轨迹」；坐标轴与色标**逐字**采用本卡「坐标轴」行：X：时间 $t$（s，0–1800，线性）；Y：步长 $h$（s，**对数轴**，因步长呈几何阶梯下降：1 → 0.4 → 0.16 → 0.064 → …，比率≈0.4）。刻度用 1／0.1／0.01／0.001；**图内一律中文**（仅符号、单位、公认缩写可保留）；**禁止**出现 CSV 英文列值／裸代号 |
| **字体与公式** | 中文 **思源黑体／微软雅黑**（macOS `Heiti SC`／`PingFang SC`；Linux 须装中文字体）；西文 **Arial**；**图内公式 `Cambria Math`／Times，10 pt，Italic，`Interpreter='latex'`（禁截图）**；字号层级 2:1.5:1.2:1:0.85 且**最小 ≥7 pt**；**导出 300 DPI PNG ＋ 矢量 PDF 并嵌入全部字体**（`exportgraphics(...,'ContentType','vector')`）。依据：`00_规则/图表设计语言.md` **§2.1 六条硬约束** |
| **配色与尺寸** | 主线用皇家蓝 `#1F3A93`；参考线（若画）用灰虚线 `#9E9E9E`；比例 **4:3**；见《图表设计语言》§1／§2 |
| **绘制注意点** | ① **必须用 `step` 型绘制**（`plt.step(..., where='post')`），不得用默认折线；② Y **必须对数轴**（步长跨 3–4 个数量级）；③ 需在图上**标注"判定阈值"或"稳定段区间"**，否则读者看不出"何时已不再细化"；④ **数据已抽稀**（257403 → 4964 行，前 200 点全保留），阶梯形态不变——图注中**须如实说明**，避免与日志行数不一致被质疑；⑤ 图中数字与《A_数值口径总表》不一致时**停下回询** |
| **对应文字表述建议** | 图注："图 F-41 误差驱动自适应步长的步长轨迹（对数纵轴）。" 正文引用句："如图 F-41 所示，自适应控制器使步长在初始急剧段迅速细化、随后稳定；**在 $h=1$ s 与 $h=60$ s 两种设定下，关键量结果差仅 $3.1\times10^{-11}$ ℃**，反证时间离散误差已被消除。" |
| **支撑论证点** | 创新效果量化（要点9.7"每个创新点须说明效果提升"）；检验充分性（要点7.4） |

---

## ★ 展示层文字 · 覆盖更新（提交方 · **覆盖式**）

> **效力**：本块**覆盖**本卡前述同名项在「**展示层**」上的表述；**线上成品图与本卡不一致者，以本卡与本块为准**。
> **不变**：**数值与 CSV 列名一律不改**（唯一真值）；映射只发生在图的**展示层**，且**已写入本卡**。


### 0) **本图工单号**

> 续编 **D#5**（数据源须用 `fig_q1_stepsize.csv`；本图属**问题 1**）
> **清单位置**：前版 `30_图表/05_交付清单/A_图表残留问题清单.md`（A／B／C 三组）；续编 `30_图表/05_交付清单/A_图表残留问题清单_续.md`（D 组）。

### 1) 通用条款（本图适用）

- **图内一律中文**；**禁止**出现 CSV 英文列值／裸代号；
- **符号必须用数学体**（斜体，`Interpreter='latex'`）；**严禁用正文黑体直接排拉丁串**——这是本类问题的**根因**；
- **图内不得出现图号/编号**（图题由论文排版方添加）；
- 字体族、字号层级、导出与字体嵌入要求**照本卡「字体与公式」行不变**。

### 2) 本图专属（**逐字替换**）

| 位置 | 禁止写法 | **必须写成** |
|---|---|---|
| 数据源 | `误用同名 q3 版（`fig_q3_stepsize_adaptive.csv`）` | **必须用 `fig_q1_stepsize.csv`**（本图属**问题 1**，依本卡「数据源」行） |

### 3) 核对

- 图中数字与《`A_数值口径总表`》不一致时**停下回询**（依本卡「绘制注意点」）。
