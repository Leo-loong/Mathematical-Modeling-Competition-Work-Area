# 图表规格卡：F-19 Q1 与 Q2 重叠段（0–30 min）差异归因

| 要素 | 内容 |
|---|---|
| **图号/图名** | 图 F-19：第一问与第二问在重叠时段（0–1800 s）的差异及其归因 |
| **目的** | **正面呈现**两问在 0–30 min 重叠段的结果不同，并用物性体系差异**定量解释**该差异——把"看起来矛盾"变成"**可解释、可复现的必然结果**"，落实 P9（**禁止反向调参强求一致**） |
| **图类型与理由** | 类型：**双联折线**（上：两条曲线的对比；下：逐时刻差值）；理由：单看两条曲线重合度不足以说明差异量级，**必须独立画出差值曲线** |
| **数据源** | Q1 结果 `result1.xlsx` ＋ Q2 结果 `result2.xlsx`（取 0–1800 s 段）合并导出 `04_图表包/data/fig_q1q2_overlap.csv`（列：`t`, `T_center_q1`, `T_center_q2`, `C_surface_q1`, `C_surface_q2` 等） |
| **坐标轴** | 上子图 X：时间 $t$（s），0–1800；Y：温度（℃）／含水率（kg/kg）择一（建议**分两列**：左列温度、右列含水率）；下子图 X 同；Y：**差值**，**必含零参考线** |
| **标签文本（逐字，全中文）** | 图题「F-19 Q1 与 Q2 重叠段（0–30 min）差异归因」；坐标轴与色标**逐字**采用本卡「坐标轴」行：上子图 X：时间 $t$（s），0–1800；Y：温度（℃）／含水率（kg/kg）择一（建议**分两列**：左列温度、右列含水率）；下子图 X 同；Y：**差值**，**必含零参考线**；**图内一律中文**（仅符号、单位、公认缩写可保留）；**禁止**出现 CSV 英文列值／裸代号 |
| **字体与公式** | 中文 **思源黑体／微软雅黑**（macOS `Heiti SC`／`PingFang SC`；Linux 须装中文字体）；西文 **Arial**；**图内公式 `Cambria Math`／Times，10 pt，Italic，`Interpreter='latex'`（禁截图）**；字号层级 2:1.5:1.2:1:0.85 且**最小 ≥7 pt**；**导出 300 DPI PNG ＋ 矢量 PDF 并嵌入全部字体**（`exportgraphics(...,'ContentType','vector')`）。依据：`00_规则/图表设计语言.md` **§2.1 六条硬约束** |
| **配色与尺寸** | Q1 用**实线**、Q2 用**虚线**（同色不同线型，避免增色）；差值曲线用深红 #C0392B；多子图比例 **16:9** |
| **绘制注意点** | ① 差值的**量级要如实呈现**（实测：温度差约 1.4 ℃、表面含水率差约 0.14 kg/kg）；② **必须在图注中写明正确归因（★此处极易写错）**：**(a)** 两问物性体系不同（附录2 常物性 vs 附录3 变物性）；**(b)** **热扩散率更小**（$\alpha$ 由 $1.689$ 降至 $1.448\times10^{-7}$ m²/s，−14.2%）⟹ Q2 升温更慢、温度偏低；**(c)** **扩散系数更大**（$+14\%$）但**不可写成"传质更快"**——在 Robin 边界下边界通量由 $k_m$ 控制、**与 $D$ 无关**，$D$ 决定**剖面平坦度**：$D$ 越大 ⟹ 表面浓度越高、中心越低（实测单调性检验确认）；**(d)** 故 **Q2 的 $C(R)$ 更高是"剖面更平"而非"传质更慢"**；③ 禁止为"看起来一致"而调参 |
| **对应文字表述建议** | 图注："图 F-19 两问在重叠时段（0–30 min）的差异。差异源于物性体系不同（附录2 vs 附录3），属模型的必然结果。" 正文："需要说明的是，两问在同一时段采用**不同的物性体系**（题面分别指定附录2 与附录3），故结果并不相同——**这恰是两问设定的差异所在，而非计算不一致**。其中第二问因热扩散率更小、升温更慢，而扩散系数更大使内部剖面更平坦，故其表面含水率反而略高于第一问。" |
| **支撑论证点** | **模型讨论的诚实性**（主动交代而非回避）；方法学严谨（差异归因而非调参掩盖） |

---

## ★ 展示层文字 · 覆盖更新（提交方 · **覆盖式**）

> **效力**：本块**覆盖**本卡前述同名项在「**展示层**」上的表述；**线上成品图与本卡不一致者，以本卡与本块为准**。
> **不变**：**数值与 CSV 列名一律不改**（唯一真值）；映射只发生在图的**展示层**，且**已写入本卡**。


### 0) **本图工单号**

> 前版 **A#3**（`TR`→$T_R$）
> **清单位置**：前版 `30_图表/05_交付清单/A_图表残留问题清单.md`（A／B／C 三组）；续编 `30_图表/05_交付清单/A_图表残留问题清单_续.md`（D 组）。

### 1) 通用条款（本图适用）

- **图内一律中文**；**禁止**出现 CSV 英文列值／裸代号；
- **符号必须用数学体**（斜体，`Interpreter='latex'`）；**严禁用正文黑体直接排拉丁串**——这是本类问题的**根因**；
- **图内不得出现图号/编号**（图题由论文排版方添加）；
- 字体族、字号层级、导出与字体嵌入要求**照本卡「字体与公式」行不变**。

### 2) 本图专属（**逐字替换**）

| 位置 | 禁止写法 | **必须写成** |
|---|---|---|
| 图例/标注中变量 | ``TR`（黑体，与同图其余数学体不一致）` | **$T_R$**（数学斜体，与同图 `$C_R$` 统一） |

### 3) 核对

- 图中数字与《`A_数值口径总表`》不一致时**停下回询**（依本卡「绘制注意点」）。
