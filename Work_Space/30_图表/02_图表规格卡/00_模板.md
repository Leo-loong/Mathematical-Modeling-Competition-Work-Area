# 图表规格卡：<图号> <图名>（模板）

> 一图一卡。复制为 `<图号>_<图名>_<MMDD>.md`。**七要素 ＋ 2 新增必填**（**标签文本（逐字，全中文）**／**字体与公式**）缺一不可；终态标准：拿卡+CSV 即可出图（**含"图上写什么字"都不得再问**）。字体与公式规则见 `00_规则/图表设计语言.md` **§2.1**。
> **既有卡（2026-09-12 之前出卡）**：两新增字段**以** `30_图表/05_交付清单/A_绘图成果与要求差距清单.md` **§二-B 的逐图"英文代号→中文标签"映射**为准执行（该表覆盖全部含英文代号列的风险图；卡内『坐标轴／绘制注意点』栏已给中文标签者照卡执行）——**新出卡一律在卡内逐字给出**。

| 要素 | 内容 |
|---|---|
| 图号/图名 | 图X：____ |
| 目的 | 评委看这张图要得出什么结论（一句话） |
| 图类型与理由 | 类型：____；理由：____（为什么该类型最能表达） |
| 数据源 | `30_图表/03_图数据准备/figX_data.csv`；列说明：col1=____（单位），col2=____ |
| 坐标轴 | X：____（单位/范围）；Y：____（单位/范围）；刻度：____ |
| **标签文本（逐字，必填）** | 轴标签/刻度/图例/图内标注的**逐字文本**（**一律中文**）；**数据列值为英文代号时，此处必须给出逐字中文映射**（如 `space→空间`、`integral→积分平均`）——**CSV 数据列不改** |
| **字体与公式** | 中文 ____（`Microsoft YaHei`/`Heiti SC`）／西文 Arial；公式 ____（`Cambria Math`／MATLAB `latex` 解释器）；字号按 `04_设计语言实例/` §二 |
| 配色与尺寸 | 引用 `04_设计语言实例/`（色值/字号/线宽/长宽比）；标注差异：____ |
| 绘制注意点 | 易错处：____（如对数轴/双轴标注/图例顺序/负值处理） |
| 对应文字表述建议 | 图注要点 + 正文引用句（"如图X所示，____"） |
