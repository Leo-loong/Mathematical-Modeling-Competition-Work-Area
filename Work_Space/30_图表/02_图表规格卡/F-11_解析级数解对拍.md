# 图表规格卡：F-11 解析级数解对拍

| 要素 | 内容 |
|---|---|
| **图号/图名** | 图 F-11：数值解与圆柱级数解析解的对比（常边界 ＋ 分段常边界） |
| **目的** | 用**独立方法**证明求解器本体正确（要点19 双方法验证），并显示偏差的**来源与量级** |
| **图类型与理由** | 类型：**上下双联**（上：数值 vs 解析折线／散点；下：偏差曲线）；理由：只画重合曲线无法说服评委，**下方独立画出偏差**才能同时证明"一致"与"误差可控" |
| **数据源** | `04_图表包/data/fig_series_check.csv`（**本包内**）；列：`case`（E2a_drX／E2b_step）、`t`、`Tc_num`、`Tc_ana`、`Ts_num`、`Ts_ana`、`Cs_num`、`Cs_ana` |
| **坐标轴** | 上子图 X：时间 $t$（s），0–1800；Y：温度（℃）；下子图 X：同；Y：偏差 $\Delta T$（℃），**含零参考线** |
| **标签文本（逐字，全中文）** | 图题「F-11 解析级数解对拍」；坐标轴与色标**逐字**采用本卡「坐标轴」行：上子图 X：时间 $t$（s），0–1800；Y：温度（℃）；下子图 X：同；Y：偏差 $\Delta T$（℃），**含零参考线**；**图内一律中文**（仅符号、单位、公认缩写可保留）；**禁止**出现 CSV 英文列值／裸代号 |
| **字体与公式** | 中文 **思源黑体／微软雅黑**（macOS `Heiti SC`／`PingFang SC`；Linux 须装中文字体）；西文 **Arial**；**图内公式 `Cambria Math`／Times，10 pt，Italic，`Interpreter='latex'`（禁截图）**；字号层级 2:1.5:1.2:1:0.85 且**最小 ≥7 pt**；**导出 300 DPI PNG ＋ 矢量 PDF 并嵌入全部字体**（`exportgraphics(...,'ContentType','vector')`）。依据：`00_规则/图表设计语言.md` **§2.1 六条硬约束** |
| **配色与尺寸** | 数值解用实线＋标记，解析解用**虚线**（同色不同线型，避免增色）；偏差曲线用深红 #C0392B；多子图比例 16:9；见《图表设计语言》§1／§2 |
| **绘制注意点** | ① 偏差量级为 $10^{-3}$ ℃，**下子图纵轴必须自适应**，否则看不见结构；② 需标注偏差的主导来源（时间离散）**以及**"$T$ 偏差不随网格收敛、$C$ 偏差随网格二阶下降"这一**双场互证**要点，可加小字注释；③ $C$ 场在 $t\le600$ s 处解析级数未收敛（$Fo$ 过小），该区段**须明确标注"级数不适用"**或直接留白，不得混画成"误差很大"；④ 分段常边界（E2b）建议单独一条或单独面板，避免与常边界结果混淆 |
| **对应文字表述建议** | 图注："图 F-11 数值解与圆柱级数解的对比及偏差（下图为偏差）"。正文引用句："如图 F-11 所示，两者偏差在 $10^{-3}$ ℃ 量级且不随网格加密而减小，表明该偏差来自**时间离散**而非空间离散；细化时间步后偏差降至 $4\times10^{-5}$ ℃。" |
| **支撑论证点** | 检验充分性（要点19 双方法验证）；数值方法专业性（要点18） |

---

## ★ 展示层文字 · 覆盖更新（提交方 · **覆盖式**）

> **效力**：本块**覆盖**本卡前述同名项在「**展示层**」上的表述；**线上成品图与本卡不一致者，以本卡与本块为准**。
> **不变**：**数值与 CSV 列名一律不改**（唯一真值）；映射只发生在图的**展示层**，且**已写入本卡**。


### 0) **本图工单号**

> 前版 **A#1**（`vs`→对比）
> **清单位置**：前版 `30_图表/05_交付清单/A_图表残留问题清单.md`（A／B／C 三组）；续编 `30_图表/05_交付清单/A_图表残留问题清单_续.md`（D 组）。

### 1) 通用条款（本图适用）

- **图内一律中文**；**禁止**出现 CSV 英文列值／裸代号；
- **符号必须用数学体**（斜体，`Interpreter='latex'`）；**严禁用正文黑体直接排拉丁串**——这是本类问题的**根因**；
- **图内不得出现图号/编号**（图题由论文排版方添加）；
- 字体族、字号层级、导出与字体嵌入要求**照本卡「字体与公式」行不变**。

### 2) 本图专属（**逐字替换**）

| 位置 | 禁止写法 | **必须写成** |
|---|---|---|
| 图上对比说明 | ``vs`（黑体拉丁串）` | **对比** |

### 3) 核对

- 图中数字与《`A_数值口径总表`》不一致时**停下回询**（依本卡「绘制注意点」）。
