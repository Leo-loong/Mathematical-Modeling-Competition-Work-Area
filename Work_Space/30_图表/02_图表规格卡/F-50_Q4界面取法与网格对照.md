# 图表规格卡：F-50 Q4 界面系数取法 × 网格对照（方法学对照，非结果图）

| 要素 | 内容 |
|---|---|
| 图号/图名 | 图 F-50：界面系数取法（积分平均 vs 调和平均）× 网格（N=40/80）的 t_dry 对照 |
| 目的 | 在 Q4 复现 Q3 方法学结论：错误取法（调和平均）高估 t_dry 且随网格细化缓慢收敛 ⟹"系数取法正确性优先于网格加密" |
| 图类型与理由 | 类型：分组条形（2 取法 × 2 网格）＋积分平均 N=160 参考线（50.6523 h）；理由：2×2 对照矩阵最清晰的呈现 |
| 数据源 | `30_图表/03_图数据准备/fig_q4_INV4_iface_grid.csv`；列：iface（integral/harmonic）、N、t_dry_h |
| 坐标轴 | X：4 档（**2 取法 × 2 网格，逐字文本见下行**）；Y：`烘干时长 t_dry / h`（48–56）；参考线：`交付值 50.6523 h` |
| **标签文本（逐字，全中文）** | X 刻度：`积分平均 N=40`／`积分平均 N=80`／`调和平均 N=40`／`调和平均 N=80`；Y 轴标签：`烘干时长 t_dry / h`；参考线标注：`交付值 50.6523 h`；图注（必写）：`调和平均为对照失败臂，非本题结果（高估 +6.7% / +1.3%）`。**英文代号映射（CSV 值不改）**：`integral→积分平均`、`harmonic→调和平均` |
| **字体与公式** | 中文 `Microsoft YaHei`（macOS `Heiti SC`）／西文 **Arial**；公式（$t_{\rm dry}$）用 `Cambria Math`／MATLAB `latex` 解释器；字号按 `04_设计语言实例/` §二 |
| 配色与尺寸 | 引用设计语言；积分平均/调和平均双色；参考线虚线标注 |
| 绘制注意点 | ⚠ 图注必须声明：调和平均轨为**对照实验失败臂，不是本题结果**；高估幅度 +6.7%（N40）/+1.3%（N80）。**图内不得出现英文代号**（`integral`/`harmonic` 须按上行映射改写）；组内标题的 ASCII `x` 须改为 `×`；导出用矢量并嵌入字体 |
| 对应文字表述建议 | "如图 F-50 所示，若界面系数改用两端点调和平均，烘干时长被高估 1.3%–6.7%，且误差随网格加密仅缓慢消除——印证本文采用沿 C 积分平均口径的必要性" |

---

## ★ 展示层文字 · 覆盖更新（提交方 · **覆盖式**）

> **效力**：本块**覆盖**本卡前述同名项在「**展示层**」上的表述；**线上成品图与本卡不一致者，以本卡与本块为准**。
> **不变**：**数值与 CSV 列名一律不改**（唯一真值）；映射只发生在图的**展示层**，且**已写入本卡**。


### 0) **本图工单号**

> 前版 **A#12**（`harmonic`／`integral`→调和平均／积分平均）
> **清单位置**：前版 `30_图表/05_交付清单/A_图表残留问题清单.md`（A／B／C 三组）；续编 `30_图表/05_交付清单/A_图表残留问题清单_续.md`（D 组）。

### 1) 通用条款（本图适用）

- **图内一律中文**；**禁止**出现 CSV 英文列值／裸代号；
- **符号必须用数学体**（斜体，`Interpreter='latex'`）；**严禁用正文黑体直接排拉丁串**——这是本类问题的**根因**；
- **图内不得出现图号/编号**（图题由论文排版方添加）；
- 字体族、字号层级、导出与字体嵌入要求**照本卡「字体与公式」行不变**。

### 2) 本图专属（**逐字替换**）

| 位置 | 禁止写法 | **必须写成** |
|---|---|---|
| 曲线/系列名 | ``harmonic`、`integral`` | **调和平均**、**积分平均** |

### 3) 核对

- 图中数字与《`A_数值口径总表`》不一致时**停下回询**（依本卡「绘制注意点」）。
