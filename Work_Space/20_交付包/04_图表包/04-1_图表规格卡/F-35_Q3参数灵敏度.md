# 图表规格卡：F-35 Q3 烘干结束时间的参数灵敏度

> **⚠ 图名变更说明**：本图原名"灵敏度与**干壳阻滞**"。经复检，"干壳阻滞"结论**已被证伪**
> （系界面扩散系数口径错误所致），故**图名与内容一并更正**为"参数灵敏度"。
> （本卡为更名后的正式规格卡；旧文件名仅作历史留痕，不得再引用。）

| 要素 | 内容 |
|---|---|
| **图号/图名** | 图 F-35：第三问烘干结束时间的参数灵敏度 |
| **目的** | ① 给出**参数重要度排序**（$T_\infty$ ＞ $D_0$ ≫ $k_m$ ＞ $C_\infty$ ≈ $h$）；② 说明**表面侧参数（$h$、$k_m$、$C_\infty$）影响都很有限**，支持"**内部扩散阻力主导**"；③ 呈现 $t_{\rm end}$ 对判据的敏感度（可作子图或附注） |
| **图类型与理由** | 类型：**水平条形图（龙卷风式）**，以烘干结束时间的**变化量** $\Delta t_{\rm end}$（h）为长度。理由：与 F-13（Q1 龙卷风图）同构，便于跨问对照；且各参数影响量级差异大（$12.8$ h vs $0.03$ h），条形图最直观 |
| **数据源** | `q3_e5_log.txt`；**已归档** `04_图表包/data/fig_q3_sensitivity.csv`（列：`param`、`level`、`t_end_h`、`dt_end_h`、`S`） |
| **坐标轴** | X：$\Delta t_{\rm end}$（h），范围约 $-9$ ～ $+13$，**0 处画竖实线**；Y：参数及其扰动档（自上而下按 $\vertS\vert$ 降序） |
| **标签文本（逐字，全中文）** | 图题「F-35 Q3 烘干结束时间的参数灵敏度」；坐标轴与色标**逐字**采用本卡「坐标轴」行：X：$\Delta t_{\rm end}$（h），范围约 $-9$ ～ $+13$，**0 处画竖实线**；Y：参数及其扰动档（自上而下按 $\vertS\vert$ 降序）；**英文列值映射**：`param`：`C_inf`→$C_\infty$、`D0`→$D_0$、`T_inf`→$T_\infty$、`h`→$h$、`k_m`→$k_m$；**图内一律中文**（仅符号、单位、公认缩写可保留）；**禁止**出现 CSV 英文列值／裸代号 |
| **字体与公式** | 中文 **思源黑体／微软雅黑**（macOS `Heiti SC`／`PingFang SC`；Linux 须装中文字体）；西文 **Arial**；**图内公式 `Cambria Math`／Times，10 pt，Italic，`Interpreter='latex'`（禁截图）**；字号层级 2:1.5:1.2:1:0.85 且**最小 ≥7 pt**；**导出 300 DPI PNG ＋ 矢量 PDF 并嵌入全部字体**（`exportgraphics(...,'ContentType','vector')`）。依据：`00_规则/图表设计语言.md` **§2.1 六条硬约束** |
| **关键标注** | ① **$D_0$ 与 $T_\infty$ 的条形最长**（主导参数），须文字标注"内部扩散控制量"；② **$h$ 的条形几乎不可见**（$\pm0.03$ h），须用**箭头 ＋ 文字**注明"**风速几乎无影响**"；③ $k_m$ 的两条**方向为"参数增大→时间缩短"**（正常方向），须避免读者误读为"越强越慢" |
| **配色与尺寸** | 缩短（负）：#27AE60；延长（正）：#C0392B；比例 **16:9** |
| **绘制注意点** | ① **不得**画出"$C_\infty-20\%$ 未达标"的旧表示——修正口径下 $C_\infty\pm20\%$ **均能达标**（$57.37$／$57.78$ h）；② $h$ 的条形差异极小，须**显式放大标注**否则读者会误以为"没有这条"；③ 建议在图中**并列显示弹性系数 $S$**（每个条形右对齐数值标签） |
| **对应文字表述建议** | 图注："图 F-35 第三问烘干结束时间对五个参数的灵敏度（基准 57.53 h），横轴为时间变化量。" 正文："如图 F-35，$t_{\rm end}$ 对**环境温度**最敏感（$\vertS\vert\approx1.6$），**扩散系数**次之（$\vertS\vert\approx1.1$）；**表面传质系数、风速与环境湿度的影响均有限**（$\vertS\vert\lesssim0.15$），说明**干燥速率由内部扩散阻力主导**。" |
| **支撑论证点** | 参数敏感性的**完整交代**；支撑"**优先控温而非加大风速**"的工程建议；呼应《A_Q3结果与分析》§五；**全局方法复核见 F-37** |

---

## 附：数据要点（绘制时核对，取《A_数值口径总表》§10.4）

| 参数扰动 | $t_{\rm end}$/h | $\Delta t_{\rm end}$/h | $S$ |
|---|---|---|---|
| $T_\infty$ −1.0 ℃ | 59.417 | $+1.88$ | **−1.637** |
| $T_\infty$ +1.0 ℃ | 55.750 | $-1.78$ | **−1.550** |
| $C_\infty$ −20% | 57.367 | $-0.17$ | +0.014 |
| $C_\infty$ +20% | 57.783 | $+0.25$ | +0.022 |
| $h$ −20% | 57.567 | $+0.03$ | **−0.003** |
| $h$ +20% | 57.533 | $0.00$ | 0.000 |
| $k_m$ −20% | 59.233 | $+1.70$ | **−0.148** |
| $k_m$ +20% | 56.500 | $-1.03$ | **−0.090** |
| $D_0$ −20% | 70.333 | $+12.80$ | **−1.112** |
| $D_0$ +20% | 49.100 | $-8.43$ | −0.733 |

基准 $t_{\rm end}=57.5333$ h（主力二分定位值为 $57.5314$ h，差 $\le0.002$ h）。

> **附注（可作子图）**：判据敏感度 $C_{\rm crit}=0.18/0.15/0.13$ ⟹ $t_{\rm end}=38.33/57.53/85.09$ h
> （收紧 13% ⟹ $+48\%$；放宽 20% ⟹ $-33\%$），说明**必须严格执行题面判据**。

> **⚠ 表述纪律**：本图呈现**单因素（OAT）**灵敏度。全局方法（Morris／Sobol，见 **F-37**）给出**一致的重要度排序**，
> 且额外揭示 $D_0$ 的显著交互；两者**排序一致但口径不同**（扰动幅度不同），引用时须注明。
