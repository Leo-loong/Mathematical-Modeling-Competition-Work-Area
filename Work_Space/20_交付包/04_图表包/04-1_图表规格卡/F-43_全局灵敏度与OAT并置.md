# 图表规格卡：F-43 全局灵敏度（Morris／Sobol）与 OAT 并置

| 要素 | 内容 |
|---|---|
| **图号/图名** | 图 F-43：问题1 全局灵敏度（Morris $\mu^*$／$\sigma$ ＋ Sobol $S_1$／$S_T$）与 OAT 排序并置 |
| **目的** | 用**全局方法独立复核**单因素（OAT）的排序，并**揭示交互占比**——回答"OAT 是否低估了参数交互" |
| **图类型与理由** | 类型：**双联图**（左：$\mu^*$–$\sigma$ 散点；右：$S_1$ 与 $S_T$ 分组条形）；理由：散点图同时表达"重要性（$\mu^*$）"与"非线性／交互（$\sigma$）"，$S_T-S_1$ 的条形差直接量化交互 |
| **数据源** | `04_图表包/data/fig_q1_gs.csv`（**本包内**）；列：`output`（输出量）、`param`（参数：`h`／`km`／`D0`／`Tinf_off`／`Cinf_off`）、`mu_star`（Morris 均值绝对影响）、`sigma`（Morris 标准差）、`S1`（Sobol 一阶指数）、`ST`（Sobol 总效应指数）。日志见 `code/innov/logs/q1_inv_T2_gs.log` |
| **坐标轴** | 左联：X = $\mu^*$（无量纲或原量纲，须标）、Y = $\sigma$；右联：X = 参数，Y = 灵敏度指数（—，0–1，可略超 1 时须注明）。两组联图**共用参数顺序** |
| **标签文本（逐字，全中文）** | 图题「F-43 全局灵敏度（Morris／Sobol）与 OAT 并置」；坐标轴与色标**逐字**采用本卡「坐标轴」行：左联：X = $\mu^*$（无量纲或原量纲，须标）、Y = $\sigma$；右联：X = 参数，Y = 灵敏度指数（—，0–1，可略超 1 时须注明）。两组联图**共用参数顺序**；**英文列值映射**：`param`→符号化；`output`：`CR`→含水率比、`TR`→温度比；**图内一律中文**（仅符号、单位、公认缩写可保留）；**禁止**出现 CSV 英文列值／裸代号 |
| **字体与公式** | 中文 **思源黑体／微软雅黑**（macOS `Heiti SC`／`PingFang SC`；Linux 须装中文字体）；西文 **Arial**；**图内公式 `Cambria Math`／Times，10 pt，Italic，`Interpreter='latex'`（禁截图）**；字号层级 2:1.5:1.2:1:0.85 且**最小 ≥7 pt**；**导出 300 DPI PNG ＋ 矢量 PDF 并嵌入全部字体**（`exportgraphics(...,'ContentType','vector')`）。依据：`00_规则/图表设计语言.md` **§2.1 六条硬约束** |
| **配色与尺寸** | $S_1$ 用皇家蓝 `#1F3A93`、$S_T$ 用金黄 `#F39C12`（差值即交互，配色需与《图表设计语言》一致）；散点用深灰 `#424242`；比例左右各 **4:3** |
| **绘制注意点** | ① **两条并置图的参数顺序必须一致**（便于评委逐行对照）；② 散点**须标参数名**（不得只画点）；③ $S_1$ 为负（数值噪声）时**如实保留并注明**"接近 0，属采样噪声"，**不得静默截断为 0**；④ 须画 $S_1=S_T$ 的对角参考线或直接叠加 $S_T-S_1$ 的条形差；⑤ 与 OAT 排序**不一致时不得择优选好看的**——须在图注或正文如实说明差异 |
| **对应文字表述建议** | 图注："图 F-43 参数全局灵敏度（Morris 与 Sobol）与单因素分析对照。" 正文引用句："如图 F-43 所示，全局方法的**重要性排序与单因素分析一致**（均为 ____ 主导），故结论稳健；同时 $S_T-S_1$ 显示 ____ 存在**不可忽略的交互效应**，这是 OAT 无法给出的信息。" |
| **支撑论证点** | 检验充分性（要点7.1 灵敏度分析）；创新性（要点9.3"算法改进类创新"——由 OAT 升级为全局灵敏度） |

> **⚠ 口径更正（本轮落地）**：Q1 的 **Sobol $S_1$ 越界**（56 点二次代理）⟹ 依《A_数值口径总表》**§8.6
> 「T2-3 的 Sobol 数值不得引用」**，本图**不画 Sobol 数值层**。右联改为
> "**Morris $\mu^*$（归一） vs OAT $|S_{\rm norm}|$（归一）**"在**公共参数**（$h/k_m/D_0$）上的并置；
> 参数顺序两联一致，仅 Morris 侧含 $T_\infty/C_\infty$、仅 OAT 侧含 $T_0/C_0$（图内已注明）。

---

## ★ 展示层文字 · 覆盖更新（提交方 · **覆盖式**）

> **效力**：本块**覆盖**本卡前述同名项在「**展示层**」上的表述；**线上成品图与本卡不一致者，以本卡与本块为准**。
> **不变**：**数值与 CSV 列名一律不改**（唯一真值）；映射只发生在图的**展示层**，且**已写入本卡**。


### 0) **本图工单号**

> 前版 **A#10**（`CR`→$C_R$；并核对 `off`）
> **清单位置**：前版 `30_图表/05_交付清单/A_图表残留问题清单.md`（A／B／C 三组）；续编 `30_图表/05_交付清单/A_图表残留问题清单_续.md`（D 组）。

### 1) 通用条款（本图适用）

- **图内一律中文**；**禁止**出现 CSV 英文列值／裸代号；
- **符号必须用数学体**（斜体，`Interpreter='latex'`）；**严禁用正文黑体直接排拉丁串**——这是本类问题的**根因**；
- **图内不得出现图号/编号**（图题由论文排版方添加）；
- 字体族、字号层级、导出与字体嵌入要求**照本卡「字体与公式」行不变**。

### 2) 本图专属（**逐字替换**）

| 位置 | 禁止写法 | **必须写成** |
|---|---|---|
| 图例中变量 | ``CR`` | **$C_R$**（数学斜体） |
| 图上其他拉丁串 | ``off` 等` | **核对其是否须中文化**（如「关闭／未计入」），并逐字写入本卡 |

### 3) 核对

- 图中数字与《`A_数值口径总表`》不一致时**停下回询**（依本卡「绘制注意点」）。
