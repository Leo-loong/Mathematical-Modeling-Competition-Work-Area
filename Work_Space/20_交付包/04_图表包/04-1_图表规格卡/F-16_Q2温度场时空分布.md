# 图表规格卡：F-16 Q2 温度场时空分布（0–3 h）

| 要素 | 内容 |
|---|---|
| **图号/图名** | 图 F-16：第二问温度场 $T(r,t)$ 时空分布（0–3 h） |
| **目的** | 展示**时间跨度放大 6 倍**（30 min → 3 h）后温度场的完整形态；与 F-08（Q1，0–30 min）**并排对照**，说明"预热段只是全过程的开头一小段" |
| **图类型与理由** | 类型：**热力图**（viridis）；理由与 F-08 一致——径向 × 时间的二维连续场，一次呈现全部演化；与 F-08 同坐标系同尺寸便于并排 |
| **数据源** | `result2.xlsx`（工作表「温度」，1801…10800 行 × 22 列）导出为 `04_图表包/data/fig_q2_field_T.csv`（列：`time_s` ＋ 21 个距离列 0–2.0 cm） |
| **坐标轴** | X：到药材中心距离 $r$（cm），0–2.0；Y：时间 $t$（s），**0–10800**；色标：温度（℃），范围按实测**窄范围**设定（建议 28–50） |
| **标签文本（逐字，全中文）** | 图题「F-16 Q2 温度场时空分布（0–3 h）」；坐标轴与色标**逐字**采用本卡「坐标轴」行：X：到药材中心距离 $r$（cm），0–2.0；Y：时间 $t$（s），**0–10800**；色标：温度（℃），范围按实测**窄范围**设定（建议 28–50）；**图内一律中文**（仅符号、单位、公认缩写可保留）；**禁止**出现 CSV 英文列值／裸代号 |
| **字体与公式** | 中文 **思源黑体／微软雅黑**（macOS `Heiti SC`／`PingFang SC`；Linux 须装中文字体）；西文 **Arial**；**图内公式 `Cambria Math`／Times，10 pt，Italic，`Interpreter='latex'`（禁截图）**；字号层级 2:1.5:1.2:1:0.85 且**最小 ≥7 pt**；**导出 300 DPI PNG ＋ 矢量 PDF 并嵌入全部字体**（`exportgraphics(...,'ContentType','vector')`）。依据：`00_规则/图表设计语言.md` **§2.1 六条硬约束** |
| **配色与尺寸** | 色阶 **viridis**；热力图比例 **1:1**（与 F-08 一致，便于并排）；色标置右并标单位 ℃ |
| **绘制注意点** | ① **必须用窄范围色标**并在图注标注初值／终值参考；② 建议在图上叠加 $t=1800$ s 的**水平细线**，标出"Q1 的窗口在此结束"，使两图对照关系一目了然；③ 若与 F-08 并排，两图**色标范围须一致**（否则视觉误导）；④ 标注关键值（$T(0,10800)$、$T(R,10800)$） |
| **对应文字表述建议** | 图注："图 F-16 第二问温度场 $T(r,t)$（0–3 h）。" 正文："如图 F-16，与第一问 30 min 的窗口相比，3 h 内温度场已充分发展……" |
| **支撑论证点** | 结果分析（时空演化的完整呈现）；Q1→Q2 的时间尺度对照 |
