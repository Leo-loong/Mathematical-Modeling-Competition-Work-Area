# 图表规格卡：F-07 第一问求解原理示意图

| 要素 | 内容 |
|---|---|
| **图号/图名** | 图 F-07：第一问（圆柱药材）求解原理示意图 |
| **目的** | **三秒看懂**：解的是什么对象、边界条件在哪、空间/时间如何离散（对应"三大特色元素"之"原理示意图"） |
| **图类型与理由** | 类型：**矢量示意图**（TikZ／draw.io）；理由：本图不含数据，目的是传达"物理对象 → 控制体 → 差分网格"的建模链条，唯有示意才能同时表达几何、边界与离散结构 |
| **数据源** | 无（示意）；但几何与离散参数须与正文一致：$R_0=2$ cm、$\Delta r=0.25$ mm（$N=80$）、$h=25$、$k_m=8\times10^{-7}$ |
| **坐标轴** | 不适用。建议**三联布局**：(a) 圆柱截面＋边界条件；(b) 径向控制体（含 $r=0$ 的 1/2 格控制体、内部完整控制体、表面半格控制体）；(c) 时空离散网格（$\Delta r$ × $\Delta t$）与推进方向 |
| **标签文本（逐字，全中文）** | 图题「F-07 第一问求解原理示意图」；坐标轴与色标**逐字**采用本卡「坐标轴」行：不适用。建议**三联布局**：(a) 圆柱截面＋边界条件；(b) 径向控制体（含 $r=0$ 的 1/2 格控制体、内部完整控制体、表面半格控制体）；(c) 时空离散网格（$\Delta r$ × $\Delta t$）与推进方向；**图内一律中文**（仅符号、单位、公认缩写可保留）；**禁止**出现 CSV 英文列值／裸代号 |
| **字体与公式** | 中文 **思源黑体／微软雅黑**（macOS `Heiti SC`／`PingFang SC`；Linux 须装中文字体）；西文 **Arial**；**图内公式 `Cambria Math`／Times，10 pt，Italic，`Interpreter='latex'`（禁截图）**；字号层级 2:1.5:1.2:1:0.85 且**最小 ≥7 pt**；**导出 300 DPI PNG ＋ 矢量 PDF 并嵌入全部字体**（`exportgraphics(...,'ContentType','vector')`）。依据：`00_规则/图表设计语言.md` **§2.1 六条硬约束** |
| **配色与尺寸** | 按《图表设计语言》§4 流程/示意节点规范：边界条件用暖色标注、控制体用浅绿填充、网格用灰色；比例 **2:1**（三联）；矢量导出（PDF/SVG），禁截图 |
| **绘制注意点** | ① **必须标出两类边界**：$r=0$ 的对称条件 $\partial_rT=0$ 与 $r=R_0$ 的 Robin 条件（$-k\partial_rT=h(T_R-T_\infty)$、$-D\partial_rC=k_m(C_R-C_\infty)$）；② 控制体示意须显示界面位置 $r_{i\pm1/2}$，并**单独高亮中心控制体**（半径 $\Delta r/2$，体积 $\Delta r^2/4$）以说明奇点处理；③ 必须标注**热流与湿流方向均由外向内**；④ 需注明"**输出点＝计算点**"（0.1 cm = 4Δr，无需插值）；⑤ 时间轴须体现"1 s 输出步长、T 内部 1/32 s 子步、C 内部 0.1 s 子步"的三层步长结构 |
| **对应文字表述建议** | 图注："图 F-07 圆柱药材非稳态热湿耦合问题的求解原理（边界条件、径向控制体与时空离散）"。正文引用句："如图 F-07 所示，问题化为轴对称一维圆柱域上的两个抛物型方程，经元体平衡离散后化为三对角线性系统，沿时间以全隐式逐层推进。" |
| **支撑论证点** | 物理建模深度（要点18）；三大特色元素（原理示意图） |
