# F-24 温度–含水率相轨迹图

> **本卡效力**：遵循《04-2_项目图表设计语言》§一～§五（配色／字体／尺寸／图型／一致性）。
> 该文件为**全项目视觉规范的唯一出处**，本卡只做本图的决定。

| 项 | 内容 |
|---|---|
| **编号** | **F-24** |
| **图名** | 温度–含水率相轨迹图 |
| **图型** | 相图（参数轨迹） |
| **用于位置** | 5.2.4 结果分析 |
| **优先级** | ★★★★☆ |
| **数据源** | `fig_q2_curves.csv`（中心／表面）＋ 若干中间半径的轨迹 |
| **坐标轴** | $x$＝含水率 $C$/(kg/kg)；$y$＝温度 $T$/℃；不同半径用不同颜色、起始点标 ○、终点标 ▲ |
| **标签文本（逐字，全中文）** | 图题「图 F-24 温度–含水率相轨迹图」；坐标轴与色标**逐字**采用本卡「坐标轴」行：$x$＝含水率 $C$/(kg/kg)；$y$＝温度 $T$/℃；不同半径用不同颜色、起始点标 ○、终点标 ▲；**图内一律中文**（仅符号、单位、公认缩写可保留）；**禁止**出现 CSV 英文列值／裸代号 |
| **字体与公式** | 中文 **思源黑体／微软雅黑**（macOS `Heiti SC`／`PingFang SC`；Linux 须装中文字体）；西文 **Arial**；**图内公式 `Cambria Math`／Times，10 pt，Italic，`Interpreter='latex'`（禁截图）**；字号层级 2:1.5:1.2:1:0.85 且**最小 ≥7 pt**；**导出 300 DPI PNG ＋ 矢量 PDF 并嵌入全部字体**（`exportgraphics(...,'ContentType','vector')`）。依据：`00_规则/图表设计语言.md` **§2.1 六条硬约束** |
| **图内要素** | ① 5 条代表半径（$r=0,0.5,1.0,1.5,2.0$ cm）的轨迹；② 起点／终点标记；③ 一条"若完全解耦"的参考折线（浅灰虚线）作对照 |
| **配色与尺寸** | 半径递增用 viridis 连续色；参考折线 #9E9E9E 虚线 |
| **输出** | **300 DPI PNG ＋ PDF 矢量**（尺寸按《设计语言》§三：单图 8×6 cm／通栏 16×9 cm／热力图 1:1） |
| **⚠ 本图注意事项** | 须在图注说明"每条轨迹＝固定半径处随时间演化的 (C,T) 路径" |

---

## 一、这张图**为什么应该存在**（依据《设计语言》§六 配图判据）

- **触发条件**：「对比分析无图」★★★★☆ —— 有耦合强度数据但未以耦合路径形式呈现
- **图文比检查**：该节现有图数若已达标则不补；本图经判定为**应当补充**。
- **不违反"禁止补图"**：本图数据**全部来自已完成的求解／检验**，**无任何编造数值**。

## 二、写作者使用指引

1. **数据从哪取**：见上表"数据源"栏；CSV 均在 `04_图表包/data/`（**本包内**）。
2. **必须保留**：上表"图内要素"与"⚠ 注意事项"两栏的全部要求。
3. **不得改写**：配色、线型语义、标记形状按《设计语言》§五 固定，**不得逐图自创**。
4. **图注**：须写"图意 ＋ 数据来源 ＋ 关键结论提示（≤3 句）"，**含句号结尾**。
5. **正文引用**：必须在该节**显式引用**（"如图 X 所示"），否则删除本图。

---

## 三、变更记录

| 版次 | 内容 |
|---|---|
| v1 | 依《设计语言》§六 配图判据新增（Q2 缺口补检 · 图表扩充） |
