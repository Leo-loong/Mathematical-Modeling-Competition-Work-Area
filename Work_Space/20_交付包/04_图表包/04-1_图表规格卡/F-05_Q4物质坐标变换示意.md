# 图表规格卡：F-05 Q4 物质坐标变换示意（物理域 ↔ 计算域）

> **本卡效力**：遵循《04-2_项目图表设计语言》§一～§五（配色／字体／尺寸／图型／一致性）。
> **新增（2026-09-12）**：Q4 已定稿，本卡由"待 Q4 定稿后出卡"转为**可执行**；Q4 侧几何参数以《A_数值口径总表》§11 为准。

| 要素 | 内容 |
|---|---|
| 图号/图名 | 图 F-05：Q4 **物质坐标变换**示意——物理域 $r$ ↔ 计算域 $\xi=r/R(t)$ |
| 目的 | 让评委三秒看懂"**收缩域如何变成固定域求解**"：这是 Q4 能把移动边界问题用固定网格解掉的关键一步 |
| 图类型与理由 | 类型：**示意图（双栏并置）**——左：物理域 $r\in[0,R(t)]$ **随时间收缩**；右：计算域 $\xi=r/R(t)\in[0,1]$ **固定**；中间箭头＝映射；理由：变换前后对照最直观 |
| 数据源 | **无（示意）**；几何与离散参数**只允许**用卡内下列值：$R_0=2.00$ cm、$R(t_{\rm dry})=1.200$ cm、$N=160$（$\Delta\xi=1/160$，161 节点）（口径表 §11）；**不得引入卡外数值** |
| 坐标轴 | 不适用（双轴并置）：左轴 `r / cm`（0 → 2.0，随 $R(t)$ 收缩）；右轴 `ξ = r / R(t)`（0 → 1，固定） |
| **标签文本（逐字，全中文）** | 左栏标题：`物理域 r（随时间收缩）`；右栏标题：`计算域 ξ = r/R(t)（固定）`；中箭头标注：`坐标变换  ξ = r / R(t)`；左下注：`R(t)：2.000 → 1.200 cm`；右下注：`N = 160（Δξ = 1/160）`；底部图注：`网格随 R(t) 同步映射，节点数不变` |
| **字体与公式** | 中文 `Microsoft YaHei`（macOS `Heiti SC`）／西文 **Arial**；公式（$r$、$\xi$、$R(t)$）用 `Cambria Math`／MATLAB `latex` 解释器；字号按 `04_设计语言实例/` §二（**最小 ≥7 pt**） |
| 配色与尺寸 | 引用 `04_设计语言实例/`：物理域＝**皇家蓝 #1F3A93**、计算域＝**森林绿 #27AE60**、网格＝浅灰；尺寸 **16 × 8 cm（2:1）**；导出 300 DPI PNG ＋ 矢量 PDF（嵌入字体） |
| 绘制注意点 | ① 参数**必须与正文完全一致**（禁自行改数）；② 收缩方向画箭头并标注 $R(t)$ **随时间减小**；③ 必须看出**网格随映射同步变密**（画变换前后的网格疏密对照，而非只画两条轴）；④ **不得**出现脚本名／变量名等内部代号；⑤ 图内**一律中文**（符号 $r,\xi,R(t)$ 除外） |
| 对应文字表述建议 | "如图 F-05 所示，令 $\xi=r/R(t)$ 后，收缩物理域上的守恒方程化为**固定计算域**上的形式，网格随 $R(t)$ 同步映射——这正是 Q4 能以固定网格求解移动边界问题的原因。" |
