# 图表规格卡：F-51 Q3 求解流程图（事件驱动 ＋ 子步级二分）

> **本卡效力**：遵循《04-2_项目图表设计语言》§一～§五（配色／字体／尺寸／图型／一致性）与 **§八.3 流程图节点规范**。
> **新增（2026-09-12）**：编号顺延（F-41～F-43 已由 Q1 创新项占用、F-44～F-50 已由 Q4 登记）；承接"**每小问 ≥1 张建模流程图**"的验收项（Q3 原缺）。

| 要素 | 内容 |
|---|---|
| 图号/图名 | 图 F-51：Q3 求解流程——**事件终止 ＋ 子步级二分**定位烘干结束时间 $t_{\rm end}$ |
| 目的 | 让评委三秒看懂"**57.5314 h 是怎么被精确定位的**"：不是人工试凑，而是**事件检测 + 二分回溯** |
| 图类型与理由 | 类型：**流程图**（含判定与回溯循环）；理由：算法有分支与迭代，流程图是唯一自解释的表达 |
| 数据源 | **无（示意）**；节点内容取自 `11-建模/11-3_算法与管线/Q3/code/`（事件终止＋子步二分实现）与《A_Q3求解记录》§一 W3–W4；**不得引入卡外数值** |
| 坐标轴 | 不适用（流程图；**自上而下**主流程，循环回路走右侧） |
| **标签文本（逐字，全中文）** | 节点（自上而下）：`输入：初值 + 边界（附件1） + 尺寸（附件2）` → `空间离散（N，元体平衡二阶）` → `单步隐式推进（追赶法求解）` → `子步级事件检测：中心含水率 < 0.15 ?` →〔否〕`步进下一输出步（自适应/固定步）`（回到推进）／〔是〕`回退至该输出步起点` → `在子步内二分（精度 1e-3 s）` → `输出 t_end 与全场剖面` → `写 result3.xlsx + 日志留痕`；判定节点文字＝`中心含水率是否 < 0.15 ?`；旁注：`循环回路＝未达判据` |
| **字体与公式** | 中文 `Microsoft YaHei`（macOS `Heiti SC`）／西文 **Arial**；公式（$t_{\rm end}$、$C$）用 `Cambria Math`／MATLAB `latex` 解释器；字号按 `04_设计语言实例/` §二（节点文字 ≥8 pt，**最小 ≥7 pt**） |
| 配色与尺寸 | 引用 `04_设计语言实例/` **§八.3 节点规范**（输入＝平行四边形/蓝；过程＝圆角矩形/绿；决策＝菱形/橙；模块＝矩形/紫；输出＝圆角矩形加粗边框/红；反馈＝灰虚线箭头）；尺寸 **16 × 8 cm（2:1）**；导出 300 DPI PNG ＋ 矢量 PDF（嵌入字体） |
| 绘制注意点 | ① 每节点文字 **≤8 词、动词开头**；② 主流程方向全图统一；③ **判定节点必须是菱形**、两条出边标注"是／否"；④ 二分回溯画**虚线**并标注"子步级二分（1e-3 s）"；⑤ **不得**出现 `q3_solver.py`／变量名等内部代号；⑥ 图内**一律中文**（符号 $t_{\rm end}$ 除外） |
| 对应文字表述建议 | "如图 F-51 所示，本文以**事件终止**检测判据穿越，并在穿越子步内**二分回溯**至 1e-3 s 精度，从而把 $t_{\rm end}=57.5314$ h 定位为**可复算的事件**而非人工试凑值。" |
