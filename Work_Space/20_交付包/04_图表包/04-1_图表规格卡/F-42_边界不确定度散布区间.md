# 图表规格卡：F-42 边界不确定度 → 关键输出的**散布区间**（降级口径）

> **状态：数据就绪（◾）**。数据源 `04_图表包/data/fig_q1_uq.csv`（**已产出**，4 行）。
> **三方一致纪律**：本卡 ↔ 总规划 ↔ CSV 必须同名同口径；改口径须三处同改。

> **⚠ 本卡于 2026-09-11 依"实际方法能力"重写（原卡作废）**：
> 原卡为"**折线 ＋ P5–P95 分位带**（不确定性随 $t$ 演化）"，**该图画不出来**，理由有二——
> **(a)** UQ 流水线的**代理未通过 1% 留出检验**（`q1_inv_T2_uq.log`：抽查相对误差 $T(R)=1.02\times10^{-2}>1\%$）
> ⟹ 依纪律**降级为"只报严格样本散布带，不报分位"**；
> **(b)** 降级后产出的 CSV 只有**终值（$t=1800$ s）的 4 个量**，**没有时间序列** ⟹ 无法画"随 $t$ 演化"的带。
> **⟹ 本卡据此改为"区间标尺图"**，与数据实际能力严格对齐。**不得**再写 P5／P50／P95。

| 要素 | 内容 |
|---|---|
| **图号/图名** | 图 F-42：边界不确定度传播下**关键输出量的散布区间**（严格样本区间，$t=1800$ s） |
| **目的** | 把结论从"给出一个数"升级为"**一个数 ＋ 不确定性区间**"——直接回应"第 4 位小数可信吗"这类追问 |
| **图类型与理由** | 类型：**区间标尺图（水平区间 ＋ 基准点标记）**，**分子图**（温度／含水率各一）；理由：数据仅为 **4 个量的区间**，用区间标尺图可**一眼看出"正式交付值是否落在区间内"与"区间相对最小位的大小"**；折线／分位带因**无时间序列且无分位**而不适用 |
| **数据源** | `04_图表包/data/fig_q1_uq.csv`（**已就绪**）；列：`quantity`（`T0`／`TR`／`C0`／`CR`）、`base`（原始边界解，即**正式交付口径值**）、`lo`／`mid`／`hi`（区间下／中／上）、`halfband`（半幅）、`sigma`；文件首行注释标明 **`lo/hi` 口径 = `strict_sample_range`**（代理未通过 ⟹ 严格样本 min／max） |
| **数值（须与 CSV 一致，禁止手抄到别处）** | $T(0)$：$33.57534\in[33.53935,\ 33.60650]$；$T(R)$：$36.78554\in[36.71478,\ 36.85604]$；$C(0)$：$2.5499919\in[2.5499919,\ 2.5499919]$；$C(R)$：$1.5103812\in[1.5103442,\ 1.5104084]$（$n=40$ 严格样本） |
| **坐标轴** | **子图 1（温度）**：X 为区间（℃，**共用区间轴或各自归一化到自身区间**，二选一并在图注声明）；Y 为 2 个量（$T(0)$、$T(R)$）。**子图 2（含水率）**：同上（kg/kg）。**严禁**把 ℃ 与 kg/kg 画在同一数值轴上 |
| **标签文本（逐字，全中文）** | 图题「F-42 边界不确定度 → 关键输出的**散布区间**（降级口径）」；坐标轴与色标**逐字**采用本卡「坐标轴」行：**子图 1（温度）**：X 为区间（℃，**共用区间轴或各自归一化到自身区间**，二选一并在图注声明）；Y 为 2 个量（$T(0)$、$T(R)$）。**子图 2（含水率）**：同上（kg/kg）。**严禁**把 ℃ 与 kg/kg 画在同一数值轴上；**图内一律中文**（仅符号、单位、公认缩写可保留）；**禁止**出现 CSV 英文列值／裸代号 |
| **字体与公式** | 中文 **思源黑体／微软雅黑**（macOS `Heiti SC`／`PingFang SC`；Linux 须装中文字体）；西文 **Arial**；**图内公式 `Cambria Math`／Times，10 pt，Italic，`Interpreter='latex'`（禁截图）**；字号层级 2:1.5:1.2:1:0.85 且**最小 ≥7 pt**；**导出 300 DPI PNG ＋ 矢量 PDF 并嵌入全部字体**（`exportgraphics(...,'ContentType','vector')`）。依据：`00_规则/图表设计语言.md` **§2.1 六条硬约束** |
| **配色与尺寸** | 区间条用皇家蓝 `#1F3A93`（温度）／森林绿 `#1B5E20`（含水率），透明度 0.25；**基准点（正式交付值）**用醒目实心圆 ＋ 虚线竖引；比例 **4:3**（两子图上下排布） |
| **绘制注意点** | ① **必须标出"正式交付值"**（`base` 列），并让读者看到它**落在区间内**；② **严禁**把"边界不确定度"与"网格离散误差带"**相加**（纪律承 H10 与口径总表 §8.5）——两者须**分列呈现**；③ **图题不得写 P5／P95**（本卡为降级口径）；④ 图注**必须**声明降级原因与样本数：代理未通过 1% 留出检验 ⟹ 改报 $n=40$ 严格样本的散布区间；⑤ 去噪边界**只作对照轨**，图注须声明附件1 即边界输入、本图不含替换交付口径 |
| **对应文字表述建议** | 图注："图 F-42 边界噪声传播下关键输出的散布区间（**$n=40$ 严格解样本的极差区间**；因代理未通过 1% 留出检验而**不报分位**）。" 正文引用句："如图 F-42，在边界噪声不确定度传播下，$C(R,1800\,\mathrm s)$ 的严格样本区间为 $[1.510344,\ 1.510408]$，**正式交付值 $1.5103812$ 落在区间内**；该区间为**边界（模型输入）不确定度**，与**离散误差带分列、不相加**。" |
| **支撑论证点** | 不确定性量化（要点7.4"误差分析"＋要点7.7"检验结果须有分析"）；"模型评价"的"局限"一节 |
| **可用的正面表述（重要）** | 该图**同时是一份诚实性证据**：它如实展示了"**代理加速失败 ⟹ 主动降级**"的处置，而非硬报一组不可靠的分位数。论文可据此说明"**不确定度结论只在方法可担保的范围内给出**" |
