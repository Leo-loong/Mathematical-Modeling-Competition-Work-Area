# 图表规格卡：F-37 Q3 全局灵敏度（Morris ＋ Sobol）

| 要素 | 内容 |
|---|---|
| **图号/图名** | 图 F-37：第三问烘干结束时间的全局灵敏度分析（Morris 筛选与 Sobol 方差分解） |
| **目的** | ① 用**全局方法**复核单因素灵敏度结论，并**识别交互作用**；② 证明"干燥由内部扩散阻力主导"这一结论**不依赖于单因素方法的选择** |
| **图类型与理由** | **三联图**。左：Morris **$\mu^*$–$\sigma$ 散点图**（横＝平均绝对效应，纵＝离散度，气泡＝$S_{\rm norm}$）；中：Morris **$S_{\rm norm}$ 横向条形图**（与 F-35 的弹性排序并列）；右：Sobol **$S_T$ 与 $S_1$ 分组条形图**。理由：$\mu^*$–$\sigma$ 图是 Morris 的标准呈现方式，可**同时**读出重要度与非线性/交互强度；右图并列 $S_1$ 与 $S_T$ 可直接显示交互项 |
| **数据源** | **`04_图表包/data/fig_q3_gs_morris.csv`、`fig_q3_gs_sobol.csv`、`fig_q3_gs_samples.csv`**——**本包内自足**；过程留痕见日志 `q3_INV3_global.log`（追溯用，非取数用） |
| **坐标轴** | 左：X/Y 均为无量纲效应（净量纲为 h，须在图注注明）；中：X ＝相对灵敏度 $S_{\rm norm}$；右：X ＝方差贡献比例（$0$–$1$）；三图 Y 均为参数（按重要度降序排列） |
| **标签文本（逐字，全中文）** | 图题「F-37 Q3 全局灵敏度（Morris ＋ Sobol）」；坐标轴与色标**逐字**采用本卡「坐标轴」行：左：X/Y 均为无量纲效应（净量纲为 h，须在图注注明）；中：X ＝相对灵敏度 $S_{\rm norm}$；右：X ＝方差贡献比例（$0$–$1$）；三图 Y 均为参数（按重要度降序排列）；**英文列值映射**：`param`→符号化（同 F-35 规则）；**图内一律中文**（仅符号、单位、公认缩写可保留）；**禁止**出现 CSV 英文列值／裸代号 |
| **字体与公式** | 中文 **思源黑体／微软雅黑**（macOS `Heiti SC`／`PingFang SC`；Linux 须装中文字体）；西文 **Arial**；**图内公式 `Cambria Math`／Times，10 pt，Italic，`Interpreter='latex'`（禁截图）**；字号层级 2:1.5:1.2:1:0.85 且**最小 ≥7 pt**；**导出 300 DPI PNG ＋ 矢量 PDF 并嵌入全部字体**（`exportgraphics(...,'ContentType','vector')`）。依据：`00_规则/图表设计语言.md` **§2.1 六条硬约束** |
| **关键标注** | ① 左图标注"$\sigma/\mu^*$ 较大者（$D_0$、$k_m$）⟹ **非线性/交互强**"；② 中图标出**与 F-35 单因素排序完全一致**；③ 右图标注 $D_0$ 的 **$S_T=0.962$、交互 $S_T-S_1=0.209$**；④ **须标注**"$S_1$ 出现负值（$T_\infty$：$-0.076$）属代理/采样噪声，结论以 $\mu^*$ 与 $S_T$ 为准" |
| **配色与尺寸** | 与 F-31（Sobol 热力图）一致：主效应 #1F3A93、总效应 #E67E22、交互 #9E9E9E；比例 **16:9** |
| **绘制注意点** | ① **不得**把 $\mu^*$ 的**绝对**排序与 $S_{\rm norm}$／弹性排序混画在同一坐标轴上（二者口径不同：扰动幅度 $T_\infty$ 为 $2\%$、$D_0$ 为 $20\%$）；② 右图**不得**丢弃负 $S_1$——应如实画出并在图注说明；③ Morris 图的气泡面积须与 $S_{\rm norm}$ 成正比并在图例中说明 |
| **对应文字表述建议** | 图注："图 F-37 第三问全局灵敏度。左为 Morris 初等效应（$\mu^*$–$\sigma$），中为相对灵敏度，右为 Sobol 方差分解。" 正文："Morris 与 Sobol 两种独立方法给出**一致的参数重要度排序**（$T_\infty$ 与 $D_0$ 主导、表面侧参数影响有限），且 $D_0$ 的**交互作用显著**（$S_T-S_1=0.209$），说明扩散系数的影响并非简单线性叠加。" |
| **支撑论证点** | 灵敏度结论的**方法学稳健性**；对应《A_Q3可靠性与方法学》**§6.4**；数值见《A_数值口径总表》**§10.8(2)** |

---

## 附：数据要点（绘制时核对）

**Morris**（轨迹 $r=8$、4 水平 ⟹ 48 点）：

| 参数 | $\mu^*$ | $\sigma$ | $S_{\rm norm}$ |
|---|---|---|---|
| $T_\infty$ | 4.1339 | 0.4827 | **2.4441** |
| $D_0$ | 24.7840 | 3.7701 | **1.4654** |
| $k_m$ | 2.7669 | 0.5786 | 0.1636 |
| $C_\infty$ | 0.3274 | 0.1336 | 0.0194 |
| $h$ | 0.0393 | 0.0359 | 0.0023 |

**Sobol**（二次代理 $R^2=1.0000$，Saltelli $N=2048$）：

| 参数 | $S_1$ | $S_T$ | $S_T-S_1$ |
|---|---|---|---|
| $D_0$ | 0.7534 | **0.9622** | 0.2088 |
| $T_\infty$ | −0.0761 | 0.0321 | 0.1083 |
| $k_m$ | 0.0557 | 0.0147 | −0.0409 |
| $C_\infty$ | 0.0085 | 0.0003 | −0.0081 |
| $h$ | −0.0016 | 0.0001 | 0.0017 |

| 其他 | 值 |
|---|---|
| 代理可用性抽查（$1/32$ s 严格轨，2 点） | 相对差 $1.486\times10^{-4}$／$3.874\times10^{-4}$ ⟹ 可用 |

> **⚠ 与本图并列使用**：F-35 为**单因素**灵敏度；本图为**全局**灵敏度。二者**排序一致**，
> 论文中建议**先后呈现**（先单因素给直觉排序，再全局方法确认并补充交互信息）。

---

## ★ 展示层文字 · 覆盖更新（提交方 · **覆盖式**）

> **效力**：本块**覆盖**本卡前述同名项在「**展示层**」上的表述；**线上成品图与本卡不一致者，以本卡与本块为准**。
> **不变**：**数值与 CSV 列名一律不改**（唯一真值）；映射只发生在图的**展示层**，且**已写入本卡**。


### 0) **本图工单号**

> 前版 **A#9**（`vs`→对比）
> **清单位置**：前版 `30_图表/05_交付清单/A_图表残留问题清单.md`（A／B／C 三组）；续编 `30_图表/05_交付清单/A_图表残留问题清单_续.md`（D 组）。

### 1) 通用条款（本图适用）

- **图内一律中文**；**禁止**出现 CSV 英文列值／裸代号；
- **符号必须用数学体**（斜体，`Interpreter='latex'`）；**严禁用正文黑体直接排拉丁串**——这是本类问题的**根因**；
- **图内不得出现图号/编号**（图题由论文排版方添加）；
- 字体族、字号层级、导出与字体嵌入要求**照本卡「字体与公式」行不变**。

### 2) 本图专属（**逐字替换**）

| 位置 | 禁止写法 | **必须写成** |
|---|---|---|
| 图上对比说明 | ``vs`` | **对比** |

### 3) 核对

- 图中数字与《`A_数值口径总表`》不一致时**停下回询**（依本卡「绘制注意点」）。
