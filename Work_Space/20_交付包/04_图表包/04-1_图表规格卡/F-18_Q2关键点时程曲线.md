# 图表规格卡：F-18 Q2 关键点时程曲线（3 h）

| 要素 | 内容 |
|---|---|
| **图号/图名** | 图 F-18：第二问关键点温度与含水率时程曲线（0–3 h） |
| **目的** | 用最紧凑的方式给出中心与表面的**完整演化轨迹**，并标注关键值；与 F-10（Q1，30 min）对照，展示"后续 2.5 h 发生了什么" |
| **图类型与理由** | 类型：**双纵轴折线**（左：温度 ℃；右：含水率 kg/kg）；理由同 F-10——两个物理量量纲不同，共用一轴会相互压扁 |
| **数据源** | 由 `result2.xlsx` 抽取 $r=0$ 与 $r=R_0$ 两列，导出 `04_图表包/data/fig_q2_curves.csv`（列：`time_s`, `T_center`, `T_surface`, `C_center`, `C_surface`） |
| **坐标轴** | X：时间 $t$（s），0–10800（可加次要刻度标注小时）；左 Y：温度（℃）；右 Y：含水率（kg/kg）；**两轴均须明确标注单位与颜色对应** |
| **标签文本（逐字，全中文）** | 图题「F-18 Q2 关键点时程曲线（3 h）」；坐标轴与色标**逐字**采用本卡「坐标轴」行：X：时间 $t$（s），0–10800（可加次要刻度标注小时）；左 Y：温度（℃）；右 Y：含水率（kg/kg）；**两轴均须明确标注单位与颜色对应**；**图内一律中文**（仅符号、单位、公认缩写可保留）；**禁止**出现 CSV 英文列值／裸代号 |
| **字体与公式** | 中文 **思源黑体／微软雅黑**（macOS `Heiti SC`／`PingFang SC`；Linux 须装中文字体）；西文 **Arial**；**图内公式 `Cambria Math`／Times，10 pt，Italic，`Interpreter='latex'`（禁截图）**；字号层级 2:1.5:1.2:1:0.85 且**最小 ≥7 pt**；**导出 300 DPI PNG ＋ 矢量 PDF 并嵌入全部字体**（`exportgraphics(...,'ContentType','vector')`）。依据：`00_规则/图表设计语言.md` **§2.1 六条硬约束** |
| **配色与尺寸** | 严格沿用《图表设计语言》§五.1 的**固定语义配色**：温度中心 #1F3A93 ○／温度表面 #F39C12 △／含水率中心 #27AE60 □／含水率表面 #C0392B ◇；比例 **3:2**；线宽 1.5–2.0 pt |
| **绘制注意点** | ① **必须标出 $t=1800$ s 竖线**（Q1 与 Q2 的分界），使两问衔接关系可见；② 图内或图注给出四个关键值（$T(0),T(R),C(0),C(R)$ 在 3 h 末的数值）；③ **环境温度 $T_\infty(t)$ 建议以浅灰细线一并画出**（附件1 实测→14400 s 后为恒温外推），说明"边界如何变化"是结果的前提 |
| **对应文字表述建议** | 图注："图 F-18 第二问关键点时程曲线（0–3 h），竖线为第一问窗口末端。" 正文："如图 F-18，在前 30 min（竖线左侧）与第一问完全对应，其后……" |
| **支撑论证点** | 结果分析；Q1↔Q2 的时间衔接（**同一竖线两侧的连续性**是两问一致性的直观证据） |
