# 图表规格卡：F-39 Q3 界面系数取法 × 网格分辨率对照

| 要素 | 内容 |
|---|---|
| **图号/图名** | 图 F-39：界面变系数取法与网格分辨率的 $2\times2$ 对照（同一求解器） |
| **目的** | ① 证明**界面系数取法**与**网格分辨率**是两个**独立且量级相当**的误差源；② 给出本问最具方法学价值的结论——**"网格加密收敛"不能替代"系数取法正确"** |
| **图类型与理由** | **双联图**。左：$2\times2$ 柱状图（两组界面取法 × 两种网格，纵轴**对数**）；右：**误差-成本散点图**（横＝单次求解墙钟，纵＝$t_{\rm end}$，四点标注配置）。理由：左图直接呈现四格数值差 6.3 倍／89.7%，右图同时传达"精度不靠算力堆"的方法学含义 |
| **数据源** | **`04_图表包/data/fig_q3_iface_grid.csv`**——**本包内自足**；过程留痕见建模方工作区 Q3 复检记录（**追溯用，非取数用**） |
| **坐标轴** | 左：X ＝两组界面取法（调和平均／沿 $C$ 积分平均），每组两根柱（$\Delta r=1.0$ mm、$0.25$ mm），Y $=t_{\rm end}$/h（**对数轴**，$50$–$600$）；右：X ＝墙钟/s（对数），Y $=t_{\rm end}$/h（对数） |
| **标签文本（逐字，全中文）** | 图题「F-39 Q3 界面系数取法 × 网格分辨率对照」；坐标轴与色标**逐字**采用本卡「坐标轴」行：左：X ＝两组界面取法（调和平均／沿 $C$ 积分平均），每组两根柱（$\Delta r=1.0$ mm、$0.25$ mm），Y $=t_{\rm end}$/h（**对数轴**，$50$–$600$）；右：X ＝墙钟/s（对数），Y $=t_{\rm end}$/h（对数）；**英文列值映射**：`case` `A`–`F`→按本卡逐字中文（六案例＝取法×网格）；**图内一律中文**（仅符号、单位、公认缩写可保留）；**禁止**出现 CSV 英文列值／裸代号 |
| **字体与公式** | 中文 **思源黑体／微软雅黑**（macOS `Heiti SC`／`PingFang SC`；Linux 须装中文字体）；西文 **Arial**；**图内公式 `Cambria Math`／Times，10 pt，Italic，`Interpreter='latex'`（禁截图）**；字号层级 2:1.5:1.2:1:0.85 且**最小 ≥7 pt**；**导出 300 DPI PNG ＋ 矢量 PDF 并嵌入全部字体**（`exportgraphics(...,'ContentType','vector')`）。依据：`00_规则/图表设计语言.md` **§2.1 六条硬约束** |
| **关键标注** | ① 左图在正确取法两柱上标 **$57.44$／$57.5314$ h**，在错误取法两柱上标 **$559.31$／$88.43$ h**；② 须画水平参考线＝**正式答案 $57.53$ h**，并标注"**错误取法即使加密网格仍远离真值**"；③ 右图标注自检点"$\Delta r=0.25$ mm ＋ 积分平均 $=57.5314$ h，与正式答案**逐位一致**"；④ **数据含 6 个案例（A–F）**：左图仅取 **A／F／C／E** 四格（即 $2\times2$），**B／D 作单变量旁证** —— 须在图注或附注中点明：**A→B（仅把迭代提到收敛）几乎不变 ⟹ 迭代欠收敛非主因**；**A→F（仅加密网格 4 倍）改善 6.3 倍但仍远离真值 ⟹ 网格非主因**；**A→C（仅改界面取法）一步到位 ⟹ 界面取法才是主因** |
| **配色与尺寸** | 正确取法 #27AE60、错误取法 #E74C3C、参考线 #9E9E9E；比例 **16:9** |
| **绘制注意点** | ① 纵轴**必须用对数轴**，否则 $57$ 与 $559$ 在同一线性轴下不可辨；② 左图**不得**只画两种网格而省略两种取法（否则丢失结论）；③ 本图**属于方法学检验**，图注中不得出现任何非本文来源的表述 |
| **对应文字表述建议** | 图注："图 F-39 界面系数取法与网格分辨率的 $2\times2$ 对照。横轴为两种界面取法，每组对应两种网格；虚线为正式答案。" 正文："在同一求解器上仅更换界面系数取法，$t_{\rm end}$ 变化达 **89.7%**；而在**错误的取法**下，即使把网格加密 4 倍，结果仍**远离真值且不收敛**（$559.31\to88.43$ h）。这说明**离散格式的系数取法**与**网格分辨率**是两个独立的误差源，**前者的正确性优先级更高**。" |
| **支撑论证点** | 界面口径修正的**必要性**与**方法论警示**；对应《A_Q3可靠性与方法学》**§2.5**（隔离诊断步 4）；数值见《A_数值口径总表》**§10.8(3)** |

---

## 附：数据要点（绘制时核对）

| 界面取法 | $\Delta r=1.0$ mm（$N=20$） | $\Delta r=0.25$ mm（$N=80$） |
|---|---|---|
| 两端点**调和平均** | **559.31 h** | **88.43 h** |
| 沿 $C$ **积分平均**（采用） | **57.44 h** | **57.5314 h** |

| 派生结论 | 值 |
|---|---|
| 同一网格、仅换取法 ⟹ 时长变化 | **89.7%** |
| 同一取法、仅换网格 ⟹ 时长变化 | **6.3 倍**（错误取法下）／**0.16%**（正确取法下） |
| **自检** | $N=80$ ＋ 积分平均 $=57.5314$ h，与《口径总表》§10.2 正式答案**逐位一致** |

> **⚠ 图注纪律**：本图四个数值均由本文求解器在**同一离散框架**下产出（脚本与过程日志随建模方工作区 Q3 复检记录留痕），
> 图注与正文**须以"本文对照实验"表述**，不得出现任何外部来源或外部数值的说明。
