# A 题《药材的烘干问题》· 符号表

> **Agent 代号：Math_1** ｜ 版次：**v5** ｜ 关联：《A_假设清单》《A_数值口径总表》
> **v6 变更**：① 将合写行 `$\tau_T,\ \tau_C$` **拆为各自独立的符号行**（同 v5 处理 $G_0$／$G_1$、$\mathbf w_0$／$\mathbf w_1$ 的口径）—— 合写文本无法按**单符号**匹配，致正文使用 $\tau_T$／$\tau_C$ 时被判"表外符号"（成文轮临时登记表 R15-01）；② 该两符号为 5.1.4 与 6.1 正文明用的特征时间，原表仅以合写形式登记。
> **v5 变更**：§一-4b 的合写行（$G_0,G_1$ 与 $\mathbf w_0,\mathbf w_1$）**拆为各自独立的符号行** —— 合写文本在机检中无法按**单符号**匹配，致使正文使用 $G_0$／$G_1$／$\mathbf w_0$／$\mathbf w_1$ 时被判"表外符号"（成文轮临时登记表 R7-06）；② 并入成文轮第 8 章实符 **$\omega$**（欠松弛因子，取 0.7）—— 该符号在 5.2.3 正文明用，原表缺登。
> **v4 变更**：新增 **§一-4d**（元体平衡推导中的**控制体几何中间量** $V_0$、$A_{1/2}$、$V_N$）—— 该三量原仅在 §一-0 首现索引中以**合写**形式出现，单符号不可独立检索，而"模型建立"章正文将逐处使用 ⟹ 现各列一行；并声明**仅 §5.1.3 使用**、**不入论文侧表 0-1**（表 0-1 只列全局变量）。
> **v3 变更**：新增 **§一-4c**（§5.1 无量纲化论证专用符号 $\eta,\tau,\Theta,\Phi$）并声明"**不入论文侧表 0-1、首现处就地说明**"及两处**消歧**（$\eta\neq\xi$、$\tau\neq\tau_T/\tau_C$）；§一-0 首现索引同步。
> **v2 变更**：①新增 **§一-4b**（问题1 温度侧"时间精确推进"专用符号 $\mathbf A,E,G_0,G_1,\mathbf w_0,\mathbf w_1,\mathbf e_N$）并声明与 §一 其余符号的**场景区分**；②修正 $\Delta r$／$\Delta t$ 的**陈旧备注**（原"定稿前由网格无关性确定"→ 已定为 **0.25 mm／输出 1 s／含水率子步 0.1 s**）；③新增与**术语表分问口径**的一致性说明。
> 规则：建模前建立、边建模边补充；全文统一；公式中每个变量都能在本表查到。
> 按项目约定，本文件不引用/不记载资料中出现的日期。

---

## 一、符号总表（按论文分组）

### 0) 「首次出现」索引（★v2 新增；补足验收 B 类"五列齐全"）

> **为什么要这一张**：验收标准 B 要求符号表五列齐全——**符号 / 含义 / 单位 / 首次出现 / 备注**。
> 下面各表的列结构为"符号／含义／单位／**类别**／备注（取值·来源）"，其中"类别"承担了分组语义；
> 为**不破坏可读性**，把"**首次出现**"独立成本索引表（按论文正文首现顺序），
> 与各表的"备注"列互补使用。**五列要素由此齐备**。

| 符号 | 首次出现（论文位置） |
|---|---|
| $t,\ r,\ R_0,\ L$ | 5.1.1 物理背景／建模对象 |
| $T(r,t),\ C(r,t)$ | 5.1.1 待求场量 |
| $T_\infty(t),\ C_\infty(t)$ | 5.1.2 边界条件来源（附件1） |
| $T_0,\ C_0$ | 5.1.3 初始条件 式(3) |
| $\rho,\ c_p,\ k,\ h,\ k_m$ | 5.1.2 附录2 常物性参数表 |
| $D,\ \alpha$ | 5.1.3 控制方程 式(2)／派生量 |
| $\Delta r,\ N,\ r_i,\ r_{i\pm1/2}$ | 5.1.3 空间离散 式(6) |
| $V_0,\ A_{1/2},\ V_N$ | 5.1.3 中心／表面控制体 |
| $Bi_T,\ Bi_m,\ Fo$ | 5.1.2 无量纲诊断（模型选型依据） |
| $\eta,\ \tau,\ \Theta,\ \Phi$ | 5.1 无量纲化论证（**仅该处使用**，见 §一-4c） |
| $\mathbf A,\ h,\ E,\ G_0,\ G_1,\ \mathbf w_0,\ \mathbf w_1,\ \mathbf e_N$ | 5.1.3 温度侧时间精确推进 式(14)–(16)（**仅本问**） |
| $\mathbf d^{n},\ \omega,\ \Delta t$ | 5.1.3 后向欧拉与 Picard 式(11)–(13) |
| $C_{\rm crit},\ t_{\rm dry}$ | 5.1.4 求解结果／问题3–4 主结果 |
| $\tau_T,\ \tau_C,\ \varepsilon$ | 5.1.4 结果分析（时间尺度对比） |
| $R(t),\ \xi$ | 问题4 移动边界（**仅 Q4**） |
| $\rho_s$ | **不使用**（登记以免误用） |

### 1) 自变量与几何量

| 符号 | 含义 | 单位 | 类别 | 备注（取值/来源） |
|---|---|---|---|---|
| $t$ | 时间 | s | 自变量 | 输出与论文表分别用 s、h |
| $r$ | 到药材中心的距离（径向坐标） | m | 自变量 | 输出用 cm |
| $R_0$ | 药材初始半径 | m | 参数 | 0.02 m（＝2 cm） |
| $R(t)$ | 药材半径（时变） | m | 参数（时变） | 附件2 给定，2.000→1.198 cm |
| $L$ | 药材长度 | m | 参数 | 0.25 m（＝25 cm） |
| $\xi$ | 归一化径向坐标 $r/R(t)$ | — | 坐标变换 | 用于 Q4 移动边界 |

### 2) 场量与初值

| 符号 | 含义 | 单位 | 类别 | 备注 |
|---|---|---|---|---|
| $T(r,t)$ | 药材温度 | K | 因变量 | 输出用 ℃ |
| $C(r,t)$ | 干基含水率（kg 水/kg 干物质） | kg/kg | 因变量 | Q3/Q4 唯一输出 |
| $T_0$ | 初始温度 | K | 参数 | 28 ℃＝301.15 K |
| $C_0$ | 初始干基含水率 | kg/kg | 参数 | 2.55 |
| $T_\infty(t)$ | 烘房温度 | ℃ | 边界（时变→恒定） | 附件1；$t>14400$ s 取 50.00 ℃ |
| $C_\infty(t)$ | 烘房水分浓度 | kg/kg | 边界（时变→恒定） | 附件1；$t>14400$ s 取 0.04999 |
| $C_{\rm crit}$ | 烘干达标阈值 | kg/kg | 参数 | 0.15（严格小于） |
| $t_{\rm dry}$ | 烘干所需时间 | h | 输出 | Q3/Q4 主结果 |

### 3) 物性参数（随 $C$、$T$ 变化）

| 符号 | 含义 | 单位 | 类别 | 备注 |
|---|---|---|---|---|
| $\rho$ | 密度 | kg/m³ | 物性 | Q1 常值 820；Q2/Q3 $650+128C$；Q4 $760+90C$ |
| $c_p$ | 比热容 | J/(kg·K) | 物性 | 附录2/3/4 三套 |
| $k$ | 热传导系数 | W/(m·K) | 物性 | 附录2/3/4 三套 |
| $D$ | 水分扩散系数 | m²/s | 物性 | Q1 $D(C)$；Q2–Q4 $D(C,T)$（负指数连乘） |
| $h$ | 对流换热系数 | W/(m²·K) | 参数 | 25，全程沿用 |
| $k_m$ | 对流传质系数 | m/s | 参数 | $8\times10^{-7}$，全程沿用 |
| $\alpha$ | 热扩散率 $k/(\rho c_p)$ | m²/s | 派生 | 附录2 下 $1.689\times10^{-7}$ |

### 4) 无量纲数与数值参数

| 符号 | 含义 | 单位 | 类别 | 备注 |
|---|---|---|---|---|
| $Bi_T$ | 传热 Biot 数 $hR_0/k$ | — | 派生 | ≈1.39（O(1) → 需分布式求解） |
| $Bi_m$ | 传质 Biot 数 $k_mR_0/D$ | — | 派生 | **Q1（附录2 $D$）≈3.24；Q2、Q3（附录3 $D$）≈2.86**；均 $O(1)$。**引用时须注明物性套系** |
| $Fo$ | 傅里叶数 | — | 数值 | 显式格式稳定判据 $Fo\le1/2$ |
| $\Delta r$ | 空间步长 | m | 数值参数 | **定为 $0.25\times10^{-3}$ m**（$N=80$ 区间／81 节点），由 E1 网格无关性裁决 |
| $\Delta t$ | 时间步长（**输出版**） | s | 数值参数 | **定为 1 s**（输出分辨率）；问题1 含水率子步 $0.1$ s；**问题1 温度侧无时间步**（时间方向精确推进，见 4b） |
| $\varepsilon$ | 切换判据带宽阈值 | ℃ | 叙述用 | （不入假设） |
| $\tau_T$ | 热扩散特征时间 | s | 派生 | $2.37\times10^3$ |
| $\tau_C$ | 质扩散特征时间 | s | 派生 | $8.1\times10^4$ |

### 4c) 无量纲化论证专用符号（**仅在 §5.1 无量纲化推导时出现**）

> 说明：为把控制方程化为同一无量纲形式（说明两场由**同一几何因子**控制），引入下列无量纲量；
> 它们属**推导过程量**，**不入论文侧表 0-1**（表 0-1 维持 3 列 22 行），正文**首现处就地说明**即可。
> **★ 消歧**：$\eta$ **不得**写作 $\xi$（$\xi=r/R(t)$ 已占用于问题四物料坐标）；$\tau$ **不得**与特征时间 $\tau_T,\tau_C$ 混用。

| 符号 | 含义 | 单位 | 类别 | 备注（取值/来源） |
|---|---|---|---|---|
| $\eta$ | 无量纲径向坐标 $r/R_0$ | — | 无量纲 | **仅 §5.1 使用**；与问题四 $\xi=r/R(t)$ **严格区分** |
| $\tau$ | 无量纲时间 $\alpha_0 t/R_0^{2}$ | — | 无量纲 | 与特征时间 $\tau_T,\tau_C$ **不同**，正文分别声明 |
| $\Theta$ | 无量纲温度 $(T-T_0)/(T_\infty-T_0)$ | — | 无量纲 | 取 $\Theta$ 而非 $\theta$，避与下文符号形近 |
| $\Phi$ | 无量纲含水率 $(C-C_\infty)/(C_0-C_\infty)$ | — | 无量纲 | 取 $\Phi$ 而非 $\phi$，**避与体积含水率 $\phi_{\rm w}$ 形近** |

### 4d) 元体平衡推导中的控制体几何中间量（**仅 §5.1.3 使用**）

> 说明：柱坐标元体平衡中出现控制体的**体积**与**界面面积**。两套归一化口径只差同一常数因子 $\pi$，
> **比值不变**（中心式逐项一致；详见《A_公式展开方案》§7.1.3 的口径统一说明）。
> 三者属**推导中间量**，**不入论文侧表 0-1**（表 0-1 只列全局变量），正文**首现处就地说明**。

| 符号 | 含义 | 单位 | 类别 | 备注（取值/来源） |
|---|---|---|---|---|
| $V_0$ | 中心控制体的体积（单位长度） | m² | 派生 | 含 $\pi$ 口径为 $\pi\,\Delta r^{2}/4$；推导 `式 10` 的 $\pi$ 归一化写法为 $\Delta r^{2}/4$ |
| $A_{1/2}$ | 中心控制体的外界面面积（单位长度） | m | 派生 | 含 $\pi$ 口径为 $\pi\,\Delta r$；$\pi$ 归一化写法为 $\Delta r$ |
| $V_N$ | 表面控制体的体积（单位长度） | m² | 派生 | $\dfrac{\Delta r}{2}\left(R_0-\dfrac{\Delta r}{4}\right)$ |

### 4b) 问题1 温度侧"时间精确推进"专用符号（**仅在推导该格式时出现**）

> 说明：问题1 温度方程在半离散后成为**常系数线性 ODE 系统**，其时间方向可**解析积分**，
> 故不再用 $\Delta t$ 递推；下列符号仅在"模型建立"推导该格式时出现，正文须与 §一 其余符号**区分场景**。

| 符号 | 含义 | 单位 | 类别 | 备注（取值/来源） |
|---|---|---|---|---|
| $\mathbf A$ | 温度半离散系统的系数矩阵 | s$^{-1}$ | 矩阵 | **非奇异 M 矩阵**（非对角元 $\ge0$、严格对角占优），可逆 ⟹ $G_0,G_1$ 存在 |
| $h$ | 精确推进的分段步长 | s | 数值参数 | **1 s**；附件1 为等步长 60 s 数据 ⟹ 分段线性下积分**无近似** |
| $E$ | 矩阵指数 $E=e^{\mathbf A h}$ | — | 矩阵 | 离线预分解一次、**全程复用**（毫秒级） |
| $G_0$ | 分段线性 Duhamel 系数 $A^{-1}(E-I)$ | s | 矩阵 | 与 $E$ 同时离线算好 |\n| $G_1$ | 分段线性 Duhamel 系数 $A^{-2}(E-I-Ah)$ | s² | 矩阵 | 与 $E$ 同时离线算好 |
| $\mathbf w_0$ | 边界项 $T_\infty(t_n)\mathbf e_N$（把边界值映到节点 $N$） | K | 向量 | 与 $\mathbf w_1$ 成对使用 |\n| $\mathbf w_1$ | 边界斜率项 $\dfrac{T_\infty(t_{n+1})-T_\infty(t_n)}{h}\mathbf e_N$ | K/s | 向量 | 分段线性边界的斜率 |
| $\mathbf e_N$ | 边界选择向量 $(0,\dots,0,1)^{\rm T}$ | — | 向量 | 仅末节点为 1 |
| $\omega$ | 欠松弛因子（含水率场迭代） | — | 数值参数 | **取 0.7**；迭代容限取 10⁻¹⁰ |

> **与术语表的一致性**（见 `07_引用与术语/A_题录表.md` §五）：**问题1 温度＝时间方向精确推进**；
> **问题1 含水率、问题2–4 全部场量＝后向欧拉全隐式**。二者**不得混写**为"全隐式"一句话。

### 5) 下标与集合

| 符号 | 含义 | 类别 | 备注 |
|---|---|---|---|
| $i$ | 空间网格节点下标 | 下标 | $i=0$ 为药材中心 |
| $n$ | 时间层下标 | 下标 | |
| $q\in\{1,2,3,4\}$ | 小问编号 | 集合 | Q1–Q4 |
| $\rho_s$ | 干物质密度 | 参数 | **本文不使用**（本文不使用） |

---

## 二、LaTeX 三线表（可直接粘入论文）

```latex
\begin{table}[htbp]\centering\small
\caption{符号说明}\label{tab:symbols}
\begin{tabular}{llll}
\toprule
符号 & 含义 & 单位 & 备注 \\
\midrule
$t$ & 时间 & s & 输出与论文表分别用 s、h \\
$r$ & 到药材中心的距离 & m & 输出用 cm \\
$R(t)$ & 药材半径（时变） & m & 附件2；$2.000\to1.198$ cm \\
$T(r,t)$ & 药材温度 & K & 输出用 $^\circ$C \\
$C(r,t)$ & 干基含水率 & kg/kg & 问题3、4 的唯一输出量 \\
$T_0,\ C_0$ & 初始温度、初始含水率 & K, kg/kg & $28$，$2.55$ \\
$T_\infty,\ C_\infty$ & 烘房温度、水分浓度 & $^\circ$C, kg/kg & 附件1；$t>1.44\times10^4$ s 后恒定 \\
$\rho,\ c_p,\ k$ & 密度、比热容、热传导系数 & kg/m$^3$, J/(kg$\cdot$K), W/(m$\cdot$K) & 附录2/3/4 三套 \\
$D$ & 水分扩散系数 & m$^2$/s & 问题1 依赖 $C$；问题2--4 依赖 $C,T$ \\
$h,\ k_m$ & 对流换热、传质系数 & W/(m$^2$$\cdot$K), m/s & 全程沿用 \\
$\xi$ & 归一化坐标 $r/R(t)$ & --- & 问题4 移动边界 \\
$Bi_T,\ Bi_m$ & 传热、传质 Biot 数 & --- & $1.39$；$Bi_m$：Q1 为 $3.24$、Q2／Q3 为 $2.86$ \\
$t_{\rm dry}$ & 烘干所需时间 & h & 问题3、4 主结果 \\
\bottomrule
\end{tabular}
\end{table}
```

> 依赖 `\usepackage{booktabs}`；省略了次要符号，完整清单见 §一。

---

## 三、自查

- [x] 覆盖论文全部公式变量（能量方程、质量方程、两条边界、Q4 变换式）
- [x] 每个符号有单位（无量纲数、下标除外，已注明"——"）
- [x] 三线表格式
- [x] 与《01_模型叙述》公式一致（推导阶段同步核对）
- [x] 明确标注**不使用**的符号（$\rho_s$）
- [x] **（v2）覆盖问题1 温度侧的解析时间推进公式**：$\mathbf A,E,G_0,G_1,\mathbf w_0,\mathbf w_1,\mathbf e_N$ 已登记（§一-4b），**无表外符号**
- [x] **（v2）数值参数备注与现行求解设置一致**（$\Delta r=0.25$ mm、输出 1 s、含水率子步 0.1 s）
- [x] **（v2）五列要素齐全**：符号／含义／单位／**首次出现**（见 §一-0 索引）／备注（含类别与取值·来源）
- [x] **（v3）无量纲化论证专用符号已登记**（§一-4c：$\eta,\tau,\Theta,\Phi$）并**声明不入论文侧表 0-1**、两处消歧（$\eta\neq\xi$、$\tau\neq\tau_T/\tau_C$）
- [x] **（v4）控制体几何中间量可独立检索**（§一-4d：$V_0$、$A_{1/2}$、$V_N$ 各列一行）并**声明不入论文侧表 0-1**；两套归一化口径的**比值不变**已注明
