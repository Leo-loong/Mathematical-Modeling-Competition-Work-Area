# 12_AI工具使用详情 · 目录说明

| 文件 | 角色 |
|---|---|
| `A_AI工具使用登记.json` | ★ **唯一口径**（机读源）：官方四要素（①工具名称／版本 ②使用目的与环节 ③关键提示与交互 ④采纳、人工修改与核验）的滚动登记；**新增登记只改这里** |
| `00_模板.md` | 官方格式依据（字段参照），**不是数据** |

## md / PDF 在哪（为什么本目录里没有）

按用户判定（2026-09-12）：**登记的唯一口径是 JSON**；md／PDF **不是必要形式**，改为**按需导出**：

- 导出命令：`python tools\gen_ai_log.py build --pdf`
- 输出位置：`99_过程与归档/AI登记表_导出归档/`（`A_AI工具使用登记.md` ／ `AI工具使用详情.pdf`）
- **不外发**：该目录属我方 `99_过程与归档/`，不参与对外同步；**提交前**如需随材料附 PDF，跑一次上述命令即可（**不要求每次更新都导出**）
- 规则依据：`00_规则/工作约束.md` **§8.1**（v5 口径）；规范化与自检：`python tools\gen_ai_log.py check`

> md 是**生成物**，手改会被下次 `build` 覆盖；历史手写版与"格式修复前"版留证于同一归档目录（`*.bak_*`）。
