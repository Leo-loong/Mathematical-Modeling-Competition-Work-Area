# A_登记号与渲染号映射表（**定稿前置件**）

> **用途**：把**成文／图表登记里写的「登记号」**与**编译产物 `main.pdf` 中实际渲染的「渲染号」**一一对上，供**定稿环节一次机械排号**直接取用（不必再逐处考古）。
> **它不回答**：图件登记状态与呈现裁定（→ 各模块图表登记 **§一·补**）；F 号 ↔ 成品图文件（→ `A_图表编号对照.md`）；表内数值（→《A_数值口径总表》）。
> **渲染号权威来源**：`50_论文/04_交排版成图方/交付排版方论文/main.aux` 的全量 `\newlabel`（＝LaTeX 计数器实际输出）。
> **例检脚本**：见 `B_迭代工作流指导包/A_02_基础约束与验收（含陷阱）.md` **§四 指标 7**。
> **纪律**：本表**只登记映射，不改任何文字与数值**；正文行内一律写**渲染号**（`\ref` 自动跟随）。
> 按项目约定，本文件不引用、不记载资料中出现的日期。

---

## 一、图（正文页内呈现 **14** 张）

| # | 登记号（成文／图表登记） | **渲染号（PDF 实际）** | F 号 | 成品图文件 | 正文引用 |
|---:|---|---|---|---|---|
| 1 | 图 5-1 | **图 5-1** | `F-01` | `图01-总体建模流程图.pdf` | ✅ 一致（`\ref`） |
| 2 | 图 5-5 | **图 5-2** | `F-08` | `图06-Q1温度场时空分布.pdf` | ✅ 一致（`\ref`） |
| 3 | 图 5-6 | **图 5-3** | `F-09` | `图07-Q1水分浓度场时空分布.pdf` | ✅ 一致（`\ref`） |
| 4 | 图 5-8 | **图 5-4** | `F-13` | `图11-参数灵敏度龙卷风图.pdf` | ✅ 一致（`\ref`） |
| 5 | 图 5-9 | **图 5-5** | `F-15` | `图13-Q2变物性演化.pdf` | ✅ 一致（`\ref`） |
| 6 | 图 5-12 | **图 5-6** | `F-19` | `图17-Q1Q2重叠段差异归因.pdf` | ✅ 一致（`\ref`） |
| 7 | 图 5-16 | **图 5-7** | `F-33` | `图29-Q3长时程含水率演化.pdf` | ✅ 一致（`\ref`） |
| 8 | 图 5-17 | **图 5-8** | `F-05` | `图48-F05_物质坐标变换示意.pdf` | ✅ 一致（`\ref`） |
| 9 | 图 5-20 | **图 5-9** | `F-49` | `图45-Q4压缩温升机理与绝热包络.pdf` | ✅ 一致（`\ref`） |
| 10 | 图A-1 | **图 6-1** | `F-06` | `图05a-网格与时间收敛-a.pdf` ＋ `图05b-网格与时间收敛-b.pdf` | ✅ 一致（`\ref`）；**双联 a／b 均嵌入**（待裁项已闭合） |
| 11 | 图A-10 | **图 6-2** | `F-39` | `图35-Q3界面取法与网格对照.pdf` | ✅ 一致（`\ref`） |
| 12 | 图A-2 | **图 6-3** | `F-11` | `图09-解析级数解对拍.pdf` | ✅ 一致（`\ref`） |
| 13 | 图A-20 | **图 6-4** | `F-28` | `图24-Q2守恒性总账瀑布图.pdf` | ✅ 一致（`\ref`） |
| 14 | 图A-9 | **图 6-5** | `F-38` | `图34-Q3低C端外推不确定度.pdf` | ✅ 一致（`\ref`） |

> **★ 渲染号的排序口径（本轮实测更正）**：第六章渲染号**按 `sections/06_模型检验.tex` 中 `figure` 的出现顺序**排定，**不是**按登记号（`图A-n`）的数字顺序 —— 故 `A-10` 排在 `A-2` 之前（`图 6-2` 界面对照先出场）。**判据＝`main.aux` 的 `fig:*` 出现次序**（权威）。**更正前**本表曾按登记号顺序平移，属**登记错误**（已在本版修正）。

> **已降级（不在正文）**：`图A-3`（`F-12` 独立实现互验）——与 6.3「结果」段点值高度重复、图内含英文代号 ⟹ 由数值承载、不再制图；成品仍在 `05_成品图/`。**降级 ≠ 删件**，其呈现裁定见 `50_论文/02_章节稿/章节内容库/A_10_模型检验_图表登记.md §一·补`。

> **排版事实**：正文行内引用一律经 `\ref` 输出，故**读者所见引用号 ＝ 渲染号**；**成文里写的登记号与渲染号不同**（两类：第五章的 `5-5/5-6/…` 系列、第六章的 `A-n` 系列）——**属"留待定稿机械排号"的既定状态**（章节基线已声明），**不是错误**。
> **定稿动作**：按本表把成文的图注／占位清单一次性改写为**渲染号**（或保留 `\ref` 不动、仅同步占位清单），**不动任何图件与数据**。
> **待澄清（非排号问题）**：`图A-1`（`F-06`）在**成品库为双联 `图05a`／`图05b`** —— **实测 `sections/06_模型检验.tex` 中 `图05a` 与 `图05b` 均已嵌入、共用 1 个图注** ⟹ 早前"仅嵌入 `b`"的**呈现完整性待裁项已闭合**（第六章插图计数 6＝双联 2 ＋ 其余 4，图标签 5）。

> **★ 两张同名异问图的定论（长期最易混淆的一对；已由第四路 `\includegraphics` 实测锁定）**：
>
> | 问 | F 号 | 成品图文件 | 论文号（登记） | **渲染号** | 是否在正文 | 残留 |
> |---|---|---|---|---|---|---|
> | **Q3** | `F-39` | `图35-Q3界面取法与网格对照.pdf` | **图A-10** | **图 6-5** | **是**（`includegraphics` 命中 **1**） | **零英文残留**（拉丁 token 仅 `tdry`＝轴单位） |
> | **Q4** | `F-50` | `图46-Q4界面取法与网格对照.pdf` | **图A-16** | —（**未渲染**） | **否**（**已降级**；tex 中 `图46`／`fig:A-16` 命中 **0**） | 含 `harmonic`／`integral`，但**不在正文** ⟹ **挂账不改** |
>
> **判据来源（第四路）**：`sections/*.tex` 的 `\includegraphics` 全量（**以编译输入为准，不看登记表**）＋ 成品 PDF 的**全拉丁 token 列举**。
> **纪律**：正文行内**只写论文号**；**对建模组用 `F-xx`、对成品用"成品图序号"**，两条编号不得混用。

---

## 二、表（**27** 张：题面 6 ＋ 本文补充 7 ＋ 附录 14）

### 2.1 题面强制表（**6** 张；编号已并入全篇"章-序"）

| 登记号 | **渲染号** | 表题 | 状态 |
|---|---|---|---|
| 表 1 | **表 5-2** | 30 分钟内药材的温度（℃） | ✅ 已对齐（原独立计数器呈现为"表 1"，按用户口径**取消**） |
| 表 2 | **表 5-3** | 30 分钟内药材的水分浓度（kg/kg） | ✅ 已对齐（同上） |
| 表 3 | **表 5-5** | 3 小时内药材的温度（℃） | ✅ 已对齐（同上） |
| 表 4 | **表 5-6** | 3 小时内药材的水分浓度（kg/kg） | ✅ 已对齐（同上） |
| 表 5 | **表 5-7** | 药材的水分浓度随时间和位置的变化（kg/kg） | ✅ 已对齐（同上） |
| 表 6 | **表 5-8** | 计入收缩后药材的水分浓度随时间和位置的变化（kg/kg） | ✅ 已对齐（同上） |

> **编号口径（本轮变更）**：题面强制六表原以独立计数器 `reqtab` 呈现为"表 1–表 6"，与全篇"章-序"**两套编号并存**（同一节内同时出现"表 1"与"表 5-1"）⟹ 按用户口径**统一为"章-序"**（生成器内 `REQTAB_SPECIAL=False`；正文引用一律 `\ref`，自动跟随）。

### 2.2 本文补充表（**7** 张，按节编号）

| 登记号 | **渲染号** | 表题 | 落点 | 状态 |
|---|---|---|---|---|
| 表 1-1 | **表 1-1** | 建模对象与给定数据 | 1.1 | ✅ 一致 |
| 表 2-1 | **表 2-1** | 四问要素对照（同一模型 × 四组旋钮） | 2.3 | ✅ 一致 |
| 表 0-1（作者侧） | **表 4-1** | 主要符号一览 | 四 | ✅ 已对齐（作者侧写法为 `表 0-1`，渲染为 表 4-1） |
| 表 5-1 | **表 5-1** | 求解方案选型对比 | 5.0 | ✅ 一致 |
| 表 5-2 | **表 5-4** | 三套物性参数在初始状态处的取值对照 | 5.2.1 | ✅ 已对齐（题面六表并入"章-序"后顺延） |
| 表 5-3 | **表 5-9** | 四问核心结果对照 | 5.5 | ✅ 已对齐（同上） |
| 表 5-4 | **表 5-10** | 结果的可靠边界 | 5.5 | ✅ 已对齐（同上） |

### 2.3 附录表（**14** 张）

| 登记号 | **渲染号** | 表题 | 状态 |
|---|---|---|---|
| 表 A-1 | **表 A-1** | 支撑材料文件列表 类别 1.1（源程序） | ✅ 一致 |
| 表 A-1（续） | **表 A-2** | 类别 1.1b 问题一 改进代码 | ⚠ **续页语义**（见下注） |
| 表 A-1（续） | **表 A-3** | 类别 1.1c 问题一 检验日志 | ⚠ 同上 |
| 表 A-1（续） | **表 A-4** | 类别 1.2 自主查阅数据资料 | ⚠ 同上 |
| 表 A-1（续） | **表 A-5** | 类别 1.3 较大篇幅中间结果图表 | ⚠ 同上 |
| 表 A-1（续） | **表 A-6** | 类别 1.3b 第三问 源程序与结果 | ⚠ 同上 |
| 表 A-1（续） | **表 A-7** | 类别 1.3c 第二问 检验日志 | ⚠ 同上 |
| 表 A-1（续） | **表 A-8** | 类别 1.3d 第四问 源程序与结果 | ⚠ 同上 |
| 表 A-1（续） | **表 A-9** | 类别 1.3e 第二问 改进代码 | ⚠ 同上 |
| 表 A-1（续） | **表 A-10** | 类别 1.3f 第二问 改进链支持脚本 | ⚠ 同上 |
| 表 A-1（续） | **表 A-11** | 类别 1.3g 第三问 改进链脚本 | ⚠ 同上 |
| 表 A-1（续） | **表 A-12** | 类别 1.3h 复现与依赖（支撑材料根目录） | ⚠ 同上 |
| 表 A-1（续） | **表 A-13** | 类别 1.3i 未进论文正文的中间结果图件（40 张） | ⚠ 同上 |
| 表 A-1（续） | **表 A-14** | 类别 1.4 AI 工具使用详情 | ⚠ 同上 |
| 表 B-1 | **表 B-1** | 附录 B 源程序模块清单 | ✅ 已对齐（原写 `表 A-11`） |
| 表 C-1 | **表 C-1** | 附录 C 关键中间结果索引 | ✅ 已对齐（原写 `表 A-12`） |

> **注（续页语义）**：支撑材料清单在成文里是**一张表**、以"（续）"分块（登记号统一为 `表 A-1`）；而**每个分块各自带 `\caption`** ⟹ 计数器按**分块**计 ⟹ 渲染为 `表 A-1`…`表 A-11`。**登记与渲染各有其理**：前者表示"同一表的续页"，后者表示"各分块可被独立引用"。**定稿机械排号时以渲染号为准**（读者所见即渲染号）；本轮已把**附录 B／C 两表**的作者侧写法对齐为 `表 B-1`／`表 C-1`（与渲染逐字相等）。
> **机理**：附录各节 `\section` 处表计数器重置（`\numberwithin{table}{section}`）⟹ A 系列止于 `A-11`，其后为 `B-1`／`C-1`。**已登记 `A_02 §六 #12`**。

---

## 三、式（**16** 个）

| 章 | 登记号（成文 `\tag`） | **渲染号** | 连续性与缺号 |
|---|---|---|---|
| 五 | (5-1)–(5-13) | **(5-1)–(5-13)** | ✅ 连续、无缺号 |
| 六 | (6-1)、(6-2)、(6-7) | **(6-1)、(6-2)、(6-3)** | ✅ 渲染连续；**登记号的 (6-7) 为作者侧标签键**（渲染 6-3），且**正文无对它的引用** ⟹ 无读者可见影响 |

---

## 四、不符项

| 类 | 结论 |
|---|---|
| 表号三列（登记 ↔ 渲染 ↔ 引用） | **不符项 ＝ 0**（本轮闭合：`表 A-1…A-12` → `表 A-1…A-11 ＋ 表 B-1 ＋ 表 C-1`；成文 `表 A-11／A-12` → `表 B-1／C-1`） |
| 附录"（续）"分块号 | **属续页语义，非缺陷**（见 §2.3 注）；定稿以渲染号为准 |
| 式号 | **不符项 ＝ 0**（渲染连续；登记号 (6-7) 无引用） |
| 图号 | **不符项 ＝ 0**（引用经 `\ref` 自动跟随）；**原唯一待裁项（`图A-1` 双联）已闭合**（a／b 均嵌入、共用 1 图注）；**呈现集 14 张**，逐张定稿口径见 `A_图表定稿口径表.md` |

---

## 变更记录

| 版次 | 变更 |
|---|---|
| **v5（编排轮 · 表编号统一 ＋ 图并置）** | **① 表编号统一**：题面强制六表**取消独立计数器**，并入全篇"章-序" ⟹ 渲染号 `表 1→表 5-2`、`表 2→表 5-3`、`表 3→表 5-5`、`表 4→表 5-6`、`表 5→表 5-7`、`表 6→表 5-8`；本文补充三表顺延为 `表 5-2→表 5-4`、`表 5-3→表 5-9`、`表 5-4→表 5-10`（§2.1／§2.2 同步）。**② 附录表 13 → 14 张**：补 `表 A-11／A-12／A-13`（类别 1.3g／1.3h／1.3i），`表 A-14`＝类别 1.4（§2.3 同步）。**③ 图**：`图 5-1` **加宽到 1.000 `\textwidth`**；渲染 `图 5-2／图 5-3`（登记 `图 5-5／图 5-6`）**并置成对**（各自 0.485 `\textwidth`，同属一个 `figure`，**各自保留图号与 `\label`**）——图号本身不变（§一 渲染号未动）。**未改动任何数值、结论与图内内容** |
| **v4（成绩评估复核轮 · 排序更正）** | **★ 第六章渲染号排序更正（登记错误修复）**：渲染号按 `sections/06_模型检验.tex` 的 **`figure` 出现顺序**排定，**非**登记号数字顺序。据此更正为 **`A-1→图 6-1`、`A-10→图 6-2`、`A-2→图 6-3`、`A-20→图 6-4`、`A-9→图 6-5`**（v3 曾按登记号平移，把 `A-9` 记为 6-3、`A-10` 记为 6-4、`A-20` 记为 6-5 ⟹ 三行错位）。**判据＋证据**：`main.aux` 的 `fig:*` 次序（`fig:A-1→6-1`、`fig:A-10→6-2`、`fig:A-2→6-3`、`fig:A-20→6-4`、`fig:A-9→6-5`）与 PDF 实读（"图6-2 界面取法与网格对照／图6-3 解析级数解对拍／图6-4 守恒性总账瀑布图／图6-5 低含水率端外推不确定度"）**双向一致**。**未改动论文任何文字、数值与图件** |
| **v3（图表定稿会）** | **按实测收敛呈现集**：① §一 **呈现图 15 → 14**（`图A-3`／`F-12` 已降级，移入表下"已降级"注）；② **第六章渲染号平移**：`图A-1→图 6-1`、`A-2→图 6-2`、`A-9→图 6-3`、`A-10→图 6-4`、`A-20→图 6-5`（原 `图 6-3`–`图 6-6` 作废）；③ **§一 注更新**——`图A-1` 双联"仅嵌 b"的**待裁项已闭合**（a／b 均嵌入、共用 1 图注）；④ §四 图号行同步；⑤ 新增指向 `A_图表定稿口径表.md`（定稿唯一口径源）。**未改动任何文字、数值与图件** |
| **v2（独立复核轮）** | **① 新增「两张同名异问图的定论」块**（Q3＝`F-39`／`图35`／论文号 `图A-10`／渲染 **图 6-5**／**在正文**；Q4＝`F-50`／`图46`／论文号 `图A-16`／**未渲染**（已降级）），判据＝**第四路** `sections/*.tex` 的 `\includegraphics` 实测 ＋ 成品 PDF 全拉丁 token 列举；② 结论：`fig:A-10` 命中 **1**、`图46`／`fig:A-16` 命中 **0** ⟹ **P0 维持 4 项**（`F-50` 确不在正文）。**未改动任何文字、数值与图件** |
| **v1（自主复查轮 · 首版）** | **定稿前置件首次落盘**：① **图 15 张**（登记号 ↔ 渲染号 ↔ F 号 ↔ 成品图文件）逐行对照；② **表 26 张**分三组（题面 6／补充 7／附录 13）逐行对照；③ **式 16 个**（第五、六章）连续性核对；④ §四 给出不符项结论（**表／式＝0**，附录"（续）"分块属**续页语义**）。**性质＝新增索引件**：**未改动任何文字、数值、图件与 CSV**；渲染号取自 `main.aux` 实测 |
