# A_05_问题分析_基线

> **论文模块**：模块 5 · 问题分析（第二章，2.1–2.4 ＋ 要素对照）· 1–2 页
> **素材来源**：`20_交付包/01_模型叙述/01-0_问题分析与拆解.md`；`11_建模/11-1_题目分析/A_审题拆解.md`
> **真值核对**：本节数字已与《A_数值口径总表》**逐项对表**
> **术语核对**：已按 `20_交付包/07_引用与术语/A_题录表.md` §五 的统一定名比对
> **本稿状态**：初稿
> **留给排版成图方**：本节**不放流程图**（按体例流程图统一置于第五章开篇）；本节**不给结论**（只讲"怎么想"）
> 按组织约定，本文件不引用／不记载资料中出现的日期。

---

## 一、总体判断

四问共用**同一个物理过程**：圆柱形药材在热风环境中，内部以**导热**的方式升温、以**扩散**的方式失水，两者通过物性相互影响。因此全文只需建立**一个模型**，四问的区别仅在于**旋钮位置不同**（物性体系、耦合方式、时程与判据、几何是否收缩）。

本文采用的技术路线是：**从守恒律建立控制方程 → 元体平衡（单元中心有限体积）离散 → 全隐式时间推进配合追赶法求解 → 用解析解对拍、独立实现互验与守恒核算三类手段验证 → 以灵敏度分析界定结论的稳健范围**。

## 二、逐问分析

### 2.1 问题一的分析

问题的物理场景是预热平衡阶段：环境温度尚在上升，水分尚未大量脱除。**关键判断是"能否用集总参数法"**。计算两个无量纲数：传热毕奥数 $Bi_T=hR_0/k\approx1.39$、传质毕奥数 $Bi_m=k_mR_0/D\approx3.24$，二者均为 $O(1)$，说明**内部导热阻与外部对流阻同量级**，药材内部必然存在显著梯度，集总参数法不适用[1][8]，**必须做分布式求解**。

**另一个判断是"能否用半无限介质近似"**。由于扩散域径向厚度与半径之比不可忽略，忽略曲率的半无限近似会低估径向扩散路径。本文以对照计算验证这一点：同一条件下半无限近似把表面含水率高估约 6.7%（$1.6113$ 对 $1.5104$ kg/kg），故**必须保留圆柱坐标下的曲率项**。

**第三个判断是"两场能否解耦"**。问题一的物性按附录 2 取为常值，扩散系数只依赖含水率、不依赖温度，而含水率方程也不含温度，**两场之间没有反向依赖**，故可**严格解耦、依次求解**——这是本文在问题一能对温度场使用时间方向精确推进的前提。

### 2.2 问题二的分析

与问题一相比，变化集中在**两处**：物性从常值升为**含水率与温度的函数**（附录 3），两场由解耦转为**双向强耦合**（水分扩散系数含温度，热物性含含水率）[2][9]。因此必须回答"**为何必须联立迭代**"：若先算温度再算含水率而不回代，物性始终取旧状态，误差会沿时间累积。本文采用**交替推进 ＋ 物性逐内部子步更新**的方式化解：子步越小，物性冻结造成的滞后越小（子步取 $1/32$ s 时滞后影响不进入四位小数量级）。

### 2.3 问题三的分析

与问题二相比，仅动**两处**：时程由固定 3 h 改为**事件驱动**（达到判据即停），并新增**终止判据**（全域含水率严格小于 $0.15$ kg/kg）。分析要点有二：其一，**中心恒为最慢点**（因径向扩散路径最长、边界通量最先影响表面），故"全域达标"与"中心达标"等价，判据可只在中心监测；其二，几何半径在本题设定下**全程固定**（收缩是问题四的设定），故模型与离散可**完全复用问题二**。这决定了本文对问题三的处理是"**复用 ＋ 加终止机制**"，而不是另建模型。

### 2.4 问题四的分析

在问题三基础上叠加**尺寸收缩**（移动边界）与附录 4 物性。分析要点是"**为何必须做坐标变换**"：若在固定坐标系中求解，计算域随半径收缩而变小，网格点与输出位置会逐渐错位，表面附近的控制体不断被压缩，既引入插值误差又恶化表面列的收敛性。本文引入**物料坐标** $\xi=r/R(t)$，把收缩域映射为**固定计算域** $[0,1]$，使表面恒为节点、输出点恒为计算点，从源头消除上述两类困难。

## 三、四问要素对照（**同一模型 × 四组旋钮**）

| 要素 | 问题一| 问题二| 问题三| 问题四|
|---|---|---|---|---|
| **物性体系** | 附录 2（常物性） | 附录 3（变物性） | 附录 3（变物性） | 附录 4（变物性） |
| **耦合方式** | 严格解耦（依次求解） | 双向强耦合（交替推进） | 双向强耦合 | 双向强耦合 |
| **时程与终止** | 固定 $0$–$1800$ s | 固定 $0$–$3$ h | 事件驱动（达标即停） | 事件驱动（达标即停） |
| **几何** | 固定半径 $R_0$ | 固定半径 $R_0$ | 固定半径 $R_0$ | 收缩 $R(t)$，物料坐标 |
| **待求量** | 两场分布 | 两场分布 | 达标时间 | 烘干时长 |

**递进关系**：问题一建立解耦情形下的完整求解与验证框架；问题二在同一方程形式下引入变物性与双向耦合，并给出与问题一重叠段差异的归因；问题三复用问题二的求解核，加入反演机制得到达标时间；问题四再叠加移动边界，并用问题三的固定半径结果作为**对照臂**分解收缩效应。四问由此形成"**框架 → 耦合 → 反演 → 变域**"的闭环。

---

## 占位清单（交排版成图方）

| 类型 | 占位（**论文内索引号**，正文行内只用它） | 规格卡 F 号 | PDF 文件索引号 | 说明 |
|---|---|---|---|---|
| 图 | （本章不放图，按体例归第五章开篇） | — | — | 四问递进关系图与总体建模流程图均在第五章开篇引用 |
| 表 | 表 2-1 四问要素对照 | — | — | 四问要素对照为**本文补充表**，**按章编号为 表 2-1**（呈现号由生成器表计数器生成，口径见 `00_全文写作基线.md` §六） |

> **三套编号的分工与逐号对照**见 `50_论文/03_对照与索引/A_图表编号对照.md`；成品图存放于 `50_论文/05_成品图/`。

---

## 自查（依 `00_规则/角色分工与交付验收.md` §二.M）

- [x] 数字全部取自口径表，无手抄／自算
- [x] 素材来源已注明、可追溯
- [x] 无内部代号（检验编号／口径编号／台账编号）
- [x] 术语与 §五 统一定名一致（含"时间离散分问分场"）
- [x] 图表只占位、未内嵌图片
- [x] 公式编号与符号与《A_符号表》一致
- [x] 未改动任何建模结论

---

## 减法轮 v2 压缩记账（**本轮**）

| 项 | 压缩前 | 压缩后 | 处置 |
|---|---|---|---|
| 正文中文字 | 约 2 400 字 | **约 1 100 字** | 压缩同义复述与过渡铺陈 |
| 骨架 | 一、总体判断 ／ 二、逐问分析（2.1–2.4）／ 三、要素对照 | **同构保留** | 四问各段"关键判断"**一个不删** |
| 要素对照表 | 1 张（5 行） | **1 张（数值行列不动）** | 交排版按正文内嵌三线表呈现 |
| 未下调项 | — | — | 两个毕奥数量级（1.39／3.24）、半无限对照（6.7%／1.6113 对 1.5104）、子步 1/32 s、判据 0.15 kg/kg、"中心恒为最慢点"等价性、物料坐标 ξ=r/R(t) |

> **压缩口径**：本节是"怎么想"的承载处，**不给结论、不放流程图**（按体例流程图统一置于第五章开篇）。压缩只作用于**重复述说与解释性从句**，四问的**判断链条与量级数值一律保留**。

## 变更记录

| 版次 | 变更 |
|---|---|
| **v2（减法轮 · 本轮）** | 依《论文减法优化计划》§四／§五执行：正文约 **2 400 → 1 100 字**；骨架与四问判断链条、要素对照表数值行列**未动** |
| **v3（清源／回归轮）** | **占位清单表号归一**：由"（本章不放表）／不占表号"改为 **表 2-1 四问要素对照（本文补充表，按章编号）**——闭合内容库 §六 缺口 4；表内**数值与行列未动**（见 `00_全文写作基线.md` §六） |
| **v1** | 首版内容基线：一、总体判断／二、逐问分析（2.1–2.4）／三、四问要素对照 |
