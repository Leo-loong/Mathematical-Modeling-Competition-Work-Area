# A_11_模型评价与改进_基线

> **论文模块**：模块 11 · 模型评价与推广（第七章）· 0.5–1 页
> **素材来源**：`20_交付包/10_摘要与结论要点/模型评价与改进.md`；`20_交付包/01_模型叙述/A_Q4结果与分析.md`（优点素材）；`20_交付包/03_假设清单/A_假设清单.md`（局限对应假设）
> **真值核对**：本节全部数字已与《A_数值口径总表》**逐项对表**
> **术语核对**：已按 `20_交付包/07_引用与术语/A_题录表.md` §五 的统一定名比对
> **本稿状态**：初稿
> **留给排版成图方**：本节**允许条目化**（全篇仅两处例外之一）；优点与不足须成对出现，每条不足配改进方向
> 按项目约定，本文件不引用／不记载资料中出现的日期。

---

# 七、模型评价与推广

## 一、模型优点

**优点 1　离散格式严格守恒，且其数学性质可预先自检。** 空间上采用元体平衡（单元中心有限体积），保证**逐控制体严格守恒**；含水率方程的离散矩阵严格对角占优、非对角元非负，构成 M 矩阵，配合非负性与次随机性质，使**离散极值原理先有理论、后设断言**——本模型的物理自检不是事后凑数，而是格式性质的直接推论。

**优点 2　温度场时间方向精确推进，时间离散误差归零。** 针对常系数线性系统这一结构性质，采用矩阵指数与分段线性杜邦积分做解析推进，使时间维不再引入近似。效果可量化：步长由 $1$ s 放宽到 $60$ s，结果仅相差 **$3.1\times10^{-11}$ ℃**。这意味着该场的精度不再受时间步长支配，而只由空间离散决定。

**优点 3　中心奇点用两条独立推导互证，装配可靠。** 中心控制体为半格，其系数的两条推导路径——洛必达极限与半格通量平衡——相互独立，所得系数一致，解级比对差异为 **$2.8\times10^{-14}$ K**。这意味着最容易出错的一处几何奇异点由两条互不依赖的路径交叉确认。

**优点 4　误差带定量给出，验证体系可迁移。** 报告值均附**离散误差带**（问题三为 $\pm0.08$ h、问题四为 $\pm0.0249$ h），且两类不确定度分列报告、不得相加；所建立的"收敛性检验 ＋ 解析对拍 ＋ 独立实现互验 ＋ 守恒核算 ＋ 灵敏度分析"五类手段，在变物性与移动边界情形下**同样适用**，无需重构验证方案。


> **定位说明（本文的建模路线）**：本文为**单一机理模型**的四问递进求解，**未采用多模型融合**：本题属**完全自包含**型（数据、参数与物性公式均由题面给出），引入数据驱动模型**缺乏训练数据支撑**；因此本文的改进集中于**数值格式**与**问题特化策略**，而不在于模型族之间的融合。



## 二、模型不足与改进方向

**不足 1　物理简化带来系统性但已量化的偏差。** 本文忽略蒸发潜热与热质交叉扩散（对应假设 4），并把表面换热与传质系数全程沿用附录 2 的取值（对应假设 5）、传质驱动力取干基浓度差（对应假设 6）。经对照计算，计入潜热后温度系统性变化 **$0.07$–$0.10$ ℃**、问题四的烘干时长仅变化 **$+0.018\%$**，属已知且有界。
**改进方向**：若可获得汽化潜热与吸湿等温线，可把能量方程加回源项、把传质驱动力改为活度差，并用干燥实验标定表面换热与传质系数，从而把本节的三项简化一并解除。

**不足 2　降维与几何演化假设限制了适用面。** 一维轴对称（对应假设 1）忽略了端面效应，两端面面积约占总表面积 **7.4%**，使轴中部结果的偏差方向略偏保守；问题四采用仿射收缩（对应假设 8），未考虑表层先干先缩的非均匀分配。
**改进方向**：对端面效应可用二维轴对称模型做一次对照（本文已由长径比判据说明其量级）；对非均匀收缩可改用按局部含水率加权的收缩分配律，以灵敏度分析检验其影响。

**不足 3　低含水率端的外推构成主要不确定性。** 扩散系数经验式在低含水率端的外推对烘干时长的贡献，问题三达 **$43\%$–$66\%$**、问题四达 **$16.8\%$–$55.1\%$**，是全文最大的一项模型不确定性；其概率区间所依据的先验分布属**主观设定**，须在引用时声明，且**不得与离散误差带相加**。此外，表面含水率的网格分辨率存在限制，界面变系数的取法口径（沿含水率的积分平均）直接决定核心答案。
**改进方向**：可用低含水率段的独立实验数据标定扩散系数经验式，收窄外推区间；对表面附近做局部加密，进一步压低表面列的分辨率限制；把界面取法的选择作为**显式口径**随结论一并声明。

## 三、模型的推广

本文模型与求解框架可推广到**圆柱或球形物料在时变环境下的非稳态传热传质问题**，例如中药材与食品的热风干燥、农产品冷藏过程中的温湿耦合、土壤热湿迁移、生物质热解等。

迁移时需相应调整的内容为：物性经验式的形式与适用范围；边界条件的类型（第一类／第三类，是否含辐射）；是否需要计入几何收缩（若计入，则沿用本文的物料坐标与守恒形式）；是否需要计入蒸发潜热与热质交叉扩散。上述四项均可**在本文框架内替换或增补，而无需改变离散与求解结构**——这是因为本文的空间离散、时间推进与验证体系均建立在守恒律之上，与具体物性形式解耦。

---

## 占位清单（交排版成图方）

| 类型 | 占位（**论文内索引号**，正文行内只用它） | 规格卡 F 号 | PDF 文件索引号 | 说明 |
|---|---|---|---|---|
| — | （本章不含图） | — | — | 优点与不足按条目化排版 |

> **三套编号的分工与逐号对照**见 `50_论文/03_对照与索引/A_图表编号对照.md`；成品图存放于 `50_论文/05_成品图/`。

---

## 自查（依 `00_规则/角色分工与交付验收.md` §二.M）

- [x] 数字全部取自口径表，无手抄／自算
- [x] 素材来源已注明、可追溯
- [x] 无内部代号（检验编号／口径编号／台账编号）
- [x] 术语与 §五 统一定名一致
- [x] 图表只占位、未内嵌图片
- [x] 公式编号与符号与《A_符号表》一致（本章无公式）
- [x] 未改动任何建模结论
