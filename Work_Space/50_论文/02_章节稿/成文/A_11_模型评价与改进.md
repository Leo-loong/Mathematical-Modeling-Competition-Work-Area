# A_11_模型评价与改进（成文）

> **【以下为章节稿元信息，定稿交排版前整块删除】**

> **论文模块**：模块 11 · 模型评价与推广（第七章）· 0.5–1 页
> **素材来源**：`20_交付包/10_摘要与结论要点/模型评价与改进.md`；`20_交付包/01_模型叙述/`（优点素材）；`20_交付包/03_假设清单/A_假设清单.md`（局限与假设对应）；`20_交付包/05_数值口径总表/A_数值口径总表.md`；基线 `A_11_模型评价与改进_基线.md`；内容库 `A_11_模型评价与改进_内容条目.md`（10 条）与 `A_11_模型评价与改进_图表登记.md`
> **真值核对**：全部数字已与《A_数值口径总表》逐项对表
> **术语核对**：按题录表 §五 统一定名比对；正文统一用"改进"，不使用"创新"一类自我评价词
> **本稿状态**：**成文（v1）**
> **图表口径**：本章**无图无表**；优点与不足按**条目化**排版（本章为全篇允许条目化的两处之一）；所有图表证据均在第五、六章呈现，本章不复述
> **条数口径**：**优点 4 条 ＋ 定位说明 1 条／不足 3 条**（每条不足**紧跟**改进方向）；优点条数多于不足条数
> **留给排版成图方**：条目化排版，优点与不足成对出现；本章不含图
> 按项目约定，本文件不引用／不记载资料中出现的日期。

## 占位清单（交排版成图方）

| 类型 | 占位（**论文内索引号**，正文行内只用它） | 规格卡 F 号 | PDF 文件索引号 | 说明 |
|---|---|---|---|---|
| — | （本章不含图） | — | — | 优点与不足按条目化排版；图表证据见第五、六章 |

> **三套编号的分工与逐号对照**见 `50_论文/03_对照与索引/A_图表编号对照.md`。

## 自查

- [x] 素材来源已注明、可追溯
- [x] 数字全部取自口径表登记值（引用第五、六章登记值），无手抄、无自算
- [x] 无内部代号；无 F 号进正文（占位清单内除外）
- [x] 优点 4 条 ＞ 不足 3 条；每条不足**成对**给出改进方向
- [x] 不足 2 与假设 8 **同口径**（未做该对照、列为改进方向，无虚假声明）
- [x] 用"改进"不用"创新"
- [x] 数值以普通文本书写；行内数学片段均可逐符号回表
- [x] 未改动任何建模结论与数值

---

<!-- LATEX-EXPORT:BEGIN -->
# 七、模型评价与推广

## 一、模型优点

**优点 1　离散格式严格守恒，且其数学性质可预先自检。** 空间上采用元体平衡（单元中心有限体积），保证逐控制体严格守恒；含水率方程的离散矩阵严格对角占优、非对角元非负，构成 M 矩阵，配合非负性与次随机性质，使离散极值原理先有理论、后设断言——本模型的物理自检不是事后凑数，而是格式性质的直接推论。其边界为固定计算域（问题一至问题三），问题四的移动边界另见 5.4.5。

**优点 2　温度场时间方向精确推进，时间离散误差归零。** 针对常系数线性系统这一结构性质，采用矩阵指数与分段线性杜哈梅尔积分做解析推进，使时间维不再引入近似。效果可以量化：步长由 1 s 放宽到 60 s，结果仅相差 3.1×10⁻¹¹ ℃。这意味着该场的精度不再受时间步长支配，而只由空间离散决定，从而把调参的自由度集中到真正决定精度的那一维上。

**优点 3　中心奇点用两条独立推导互证，装配可靠。** 中心控制体为半格，其系数的两条推导路径，即洛必达极限与半格通量平衡，相互独立，所得系数一致，解级比对差异为 2.8×10⁻¹⁴ K。这意味着最容易出错的一处几何奇异点由两条互不依赖的路径交叉确认，而不是依赖单一路径的自洽。

**优点 4　误差带定量给出，验证体系可迁移。** 报告值均附离散误差带（问题三为 ±0.08 h、问题四为 ±0.0249 h），且两类不确定度分列报告、不得相加；所建立的"收敛性检验、解析对拍、独立实现互验、守恒核算、灵敏度分析"五类手段，在变物性与移动边界情形下同样适用，无需重构验证方案。这一点使本文的验证工作不止服务于本题，而具有可复用的方法学价值。

> **定位说明**：本文为单一机理模型的四问递进求解，未采用多模型融合——本题属完全自包含型（数据、参数与物性公式均由题面给出），引入数据驱动模型缺乏训练数据支撑，故改进集中于数值格式与问题特化策略。需要说明的是，本文的**方法多样性**并不依赖多模型融合，而由求解与检验链上的多种方法共同满足：元体平衡（单元中心有限体积）离散、矩阵指数与分段线性杜哈梅尔积分的精确推进、三对角追赶法、子步级二分的事件定位、交替推进、广义理查森外推与网格收敛指数、全局灵敏度与数据同化，合计**八类以上**，且每一类均在正文标注其适用边界；因此本文在"单一机理模型"这一自我限定下，仍具备方法层面的多样性与互证关系。

## 二、模型不足与改进方向

**不足 1　物理简化带来系统性但已量化的偏差。** 本文忽略蒸发潜热与热质交叉扩散（对应假设 4），并把表面换热与传质系数全程沿用附录 2 的取值（对应假设 5）、传质驱动力取干基浓度差（对应假设 6）。经对照计算，计入潜热后温度系统性变化 0.07–0.10 ℃、问题四的烘干时长仅变化 0.018%，属已知且有界。

改进方向：若可获得汽化潜热与吸湿等温线，可把能量方程加回源项、把传质驱动力改为活度差，并用干燥实验标定表面换热与传质系数，从而把本节的三项简化一并解除。

**不足 2　降维与几何演化假设限制了适用面。** 一维轴对称（对应假设 1）忽略了端面效应，两端面面积约占总表面积 7.4%，使轴中部结果的偏差方向略偏保守；问题四采用仿射收缩（对应假设 8），未考虑表层先干先缩的非均匀分配。

改进方向：对端面效应可用二维轴对称模型做一次对照，本文已由长径比判据说明其量级；对非均匀收缩可改用按局部含水率加权的收缩分配律，并以灵敏度分析检验其影响。需要说明的是，本文未做非均匀收缩的对照计算，此处只给出改进路径而不宣称其结果。

**不足 3　低含水率端的外推构成主要不确定性。** 扩散系数经验式在低含水率端的外推对烘干时长的贡献，问题三达 43%–66%、问题四达 16.8%–55.1%，是全文最大的一项模型不确定性；其概率区间所依据的先验分布属主观设定，须在引用时声明，且不得与离散误差带相加。此外，表面含水率的网格分辨率存在限制，界面变系数的取法口径（沿含水率的积分平均）直接决定核心答案。

改进方向：可用低含水率段的独立实验数据标定扩散系数经验式，收窄外推区间；对表面附近做局部加密，进一步压低表面列的分辨率限制；把界面取法的选择作为显式口径随结论一并声明，使读者在引用核心答案时同时看到其口径前提。

## 三、模型的推广

本文模型与求解框架可推广到圆柱或球形物料在时变环境下的非稳态传热传质问题，如中药材与食品热风干燥、农产品冷藏温湿耦合、土壤热湿迁移等；其同构之处在于均以守恒律描述内部迁移、以第三类边界描述表面交换、且环境随时间变化。

迁移时需相应调整的内容为四项：物性经验式的形式与适用范围；边界条件的类型；是否计入几何收缩（若计入则沿用本文的物料坐标与守恒形式）；是否计入蒸发潜热与热质交叉扩散。四项均可在本文框架内替换或增补而无需改变离散与求解结构——因为空间离散、时间推进与验证体系均建立在守恒律之上、与具体物性形式解耦。
<!-- LATEX-EXPORT:END -->

---

## 元信息（定稿交排版前整块删除）

### 成文修正（对基线／内容库的实质订正）

| # | 位置 | 基线／原文 | 成文处理 | 依据 |
|---:|---|---|---|---|
| 1 | 优点 2 | 未点明"调参自由度集中" | 增补一句"从而把调参的自由度集中到真正决定精度的那一维上" | 使优点落回方法论价值，非重复 6.1 数值 |
| 2 | 优点 4 | 未点明方法学价值 | 增补"使本文的验证工作不止服务于本题"一句 | 与 6.1–6.11 的检验体系呼应，不新增数值 |
| 3 | 不足 2 | 改进方向与假设 8 的边界未在章内点明 | **显式写明"本文未做非均匀收缩的对照计算"** | 与假设 8 同口径，杜绝虚假声明 |
| 4 | 不足 3 | 改进方向为三条并列 | 收束为"使读者在引用核心答案时同时看到其口径前提" | 与前两条形成同一叙述节奏 |

### 独立核验（⑦）

| 核验项 | 结果 |
|---|---|
| 章节机检 `paper_lint --file` | **❌ 0** |
| 图表登记一致性 | 本章**无图无表**，与 `A_11_模型评价与改进_图表登记.md` 一致 |
| 条数口径 | 优点 4 条（另 1 条定位说明）／不足 3 条，**优点数多于不足数** |
| 数值口径 | 全部为引用第五、六章登记值；无派生裸数 |
| 一致性约束 | 不足 2 与假设 8 同口径；优点 1 与 6.4 同义；优点 4 与摘要检验段同义；不足 3 与 5.5 可靠边界表同源 |
| 用语 | 全章使用"改进"，无"创新"类自我评价词 |

### 变更记录

| 版次 | 变更 |
|---|---|
| **v1** | 首版成文：第七章三节齐（优点 4 ＋ 定位说明／不足 3 各配改进方向／推广含四项迁移调整）。**无图无表**；引用数值 9 项均取登记值；"改进"用语统一 |
| **本轮（复核轮 · 篇幅压缩）** | **内容压缩（只删表述、不动数值与结论）**：依 `A_03 §一之二` 的**四个内容手段**（删过程句／并重复口径／删"可由表复算"类转述／压缩问内复述型检验要点）压缩本节；**护栏未动**：题面表 1–6 数值与行列、全部结论与误差带、量级对照（34 倍／52%／14.2%／43%–66%／0.337）、两条口径声明、每图每表的引用句与解释句 |
