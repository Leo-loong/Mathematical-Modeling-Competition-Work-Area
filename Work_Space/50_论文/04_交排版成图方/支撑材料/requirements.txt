# A 题 · 依赖清单
# ============================================================
# 安装：pip install -r requirements.txt
# 版本为本项目实际运行环境（Python 3.14.7）；以下依赖均为跨平台包
# （Windows / macOS / Linux 同源，均有官方预编译 wheel，无平台限制）
# ------------------------------------------------------------
# 一、求解与结果导出（附录程序运行所需，必装）
#     numpy      —— 数组与线性代数（核心求解、三对角追赶法）
#     scipy      —— 特殊函数与求根（解析对拍用的贝塞尔函数／特征根）
#     openpyxl   —— result1.xlsx / result2.xlsx 的读写
numpy>=2.5.2
scipy>=1.18.1
openpyxl>=3.1.5

# 二、可选加速（第二问起；**不装也能跑**，装了更快）
#     numba      —— 对求解核做 JIT 编译（物性／界面／装配／追赶法）
#     实测：第二问主力求解 273 s → 约 47 s（约 5.8 倍，见《A_Q2可靠性与方法学》§7.6）
#     缺失时：`q2_core.py`／`q2_core_c.py` 自动**降级**为纯 NumPy 实现，**数值结果不变**。
#     如仅需复现"优化前"行为，请用 `q2_core_orig.py`（不依赖 numba）。
# numba>=0.67.0

# 三、检验扩展（q4_INV6_sobol.py 方差分解；不装不影响求解与主检验）
#     SALib      —— Sobol 全局灵敏度（Saltelli 采样与分析）
SALib>=1.4

# 四、绘图（非本包所需）
#     本交付包的成图由写作者按《04_图表包/04-1_图表规格卡》＋ data/*.csv 自行绘制，
#     故此处不列入绘图库；如需复现建模方的参考图，请另行安装：
#         pip install matplotlib>=3.11.1
