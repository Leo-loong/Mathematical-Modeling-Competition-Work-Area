# A_移交排版方事项与定稿动作清单（**唯一一份 · 交接与定稿批执行**）

> **定位**：把**属排版方职责**与**须在定稿环节才执行**的事项集中一处，避免散落在各轮报告里。**我方不擅动版式**，故下列 §一 只给出**实测位置＋建议手段**，由排版方执行。
> **数据来源**：全部为编译产物实测（`main.log`／`main.pdf`／`main.aux`／`sections/*.tex`）。
> 按项目约定，本文件不引用、不记载资料中出现的日期。

---

## 一、移交排版方（**版式层**，我方不改）

| # | 事项 | 实测 | 建议手段 | 影响 |
|---:|---|---|---|---|
| 1 | **`Overfull \hbox`（原 130 处）** | **我方已修复：现 28 处**（附录 B 接线 ＋ 表格列宽优化后重编译实测）。原 129 处"等宽文件名不可断行"已由生成器在 `/`、`\_` 后插入 `\allowbreak`（无连字符断行点）消除；残余 28 处量级 **2–26 pt**，其中多数为**图占位框**（换真图后自然消失） | 原建议"附录内允许断行"已落地（生成器侧）；残余正文 1 处（4.03 pt）可微调措辞或断行 | 影响观感与"硬指标 3"；**未改动任何文字内容与数值** |
| 2 | **正文页内图表 27 件 > kb 建议区间 15–25** | 呈现 **14 图 ＋ 13 表**；根因＝**第六章 5 张附录图实测排在正文**（既定"附录图不占正文页数"口径未落地） | **建议把第六章 5 张图移回附录**（属"呈现位置"，非内容问题） | 移回后正文页内降至 **22 件**，落回区间 |
| 3 | **正文页数（原 30 页，顶红线）** | **现正文 29 页**（口径＝附录起始页 31 − 2；全文 **158 页**；摘要 1 页）——**已在红线内、余量 1 页**；降页来自**表格列宽优化**（各列按内容加权）与**路径断行** | 若需再收：① 行距 `1.06→1.02`；② 图宽 `0.66→0.60`；③ 表字号 `\small`；④ 图表混排（各约 **−1 页**，**均不消耗内容预算**） | 已达标（≤30）；余量仍紧，`13_论文可删减部分` 继续保留作缓冲 |
| 4 | **表 1–6 双行分组表头**（可选） | 题面模板为两行（上行跨列"**到药材中心的距离/cm**"）；当前渲染为单行 `时间/s | 0 | 0.5 | …`，距离单位由**表题**等价交代 | 若要求逐字复刻，需渲染管线支持**跨列分组表头**（`gen_latex.py` 侧改动） | 当前**语义完整、无信息缺失**；属"是否逐字复刻"的口味选择 |

---

## 二、定稿动作（**我方执行，须在定稿批一并排**）

| # | 动作 | 说明 | 影响 |
|---:|---|---|---|
| 1 | **附录 B 代码实体嵌入**（**✅ 已落实**） | 按裁定口径：**关键程序（B.1–B.7：公共模块 ＋ 四问求解的交付核与主流程）逐块嵌入正文**；**B.8–B.13 只列文件名清单**（不全量收录——全量 125 个脚本 ≈ 2.09 万行）；`code/` 由我方**递归镜像**同步（含 `innov/` 改进链脚本） | 附录增至 **158 页**（附录页数官方不限）；代码注释缺字与路径溢出已同步治理 |
| 2 | **元信息块／占位清单／脱敏说明整块删除** | 各章节稿头部与自查区（定稿交排版前删除） | 正文观感与页数 |
| 3 | **图注与占位按渲染号核对** | 正文行内引用经 `\ref` 自动跟随；按 `A_登记号与渲染号映射表.md` 核对即可 | — |
| 4 | **结果文件与附录清单一致性** | `result1–4.xlsx` 已在附录 A 逐件登记（含跨度、列契约、留空列口径说明） | — |

---

## 三、交付与打包口径（**已定，打包时执行**）

| # | 项 | 口径 |
|---:|---|---|
| 1 | **支撑材料 ≤20 MB** | **只收**：源程序（`code/`）＋ 自主数据（`data/`／`figdata/`／`results/`）＋ **未进论文的中间大图**；**已进论文的 51 张成品图不计入** —— 计入则 ≈**22.6 MB**（`09_代码与复现` 9.97 ＋ `04_图表包` 12.62）**超限**。已写入 `20_交付包/00_交付标准.md` |
| 2 | 四问结果文件 | `result1–4.xlsx` 齐备；工作表名与**附件 3 模板逐名一致**（`温度`／`水分浓度`／`Sheet1`） |
| 3 | 附录 A 清单 ↔ 实际 | 逐件一致（含第三问图数据 13 件、四问脚本与日志） |

---

## 四、图件（**由用户判定，我方不主动检查、不降级**）

| # | 项 | 约定 |
|---:|---|---|
| 1 | 制图侧交付 | 按 **`30_图表/05_交付清单/A_图件修复清单（交付绘图者）.md`**（7 张：P0 4 ＋ P1 3）执行 |
| 2 | 通过判定 | **由用户判断**；我方**不再主动检查、不降级** |
| 3 | 判定通过后 | 我方执行**换图**（覆盖 `50_论文/05_成品图/` 同名件）＋ **四处登记同步** ＋ 复测"**插图 1 次/图**" |

---

## 五、变更记录

| 版次 | 变更 |
|---|---|
| **v1** | 首版：按用户"其余内容一并按建议贯通处理"落盘。① §一 移交排版方 **4 项**（`Overfull \hbox` 130 处断行／第六章 5 图移回附录／定稿页数收口四手段／双行分组表头可选）；② §二 定稿动作 **4 项**（附录 B 代码实体嵌入、元信息整块删除、渲染号核对、结果文件一致性）；③ §三 打包口径（**排除已进论文的成品图**，防 ≈22.6 MB 超限）并同步写入 `20_交付包/00_交付标准.md`；④ §四 图件按用户约定（**判定权在用户，我方不检查不降级**，通过后换图）。**未改动任何文字、数值、图件与 CSV** |
| **v3（编排轮 · 图宽与并置 ＋ 表编号统一）** | ① **图宽逐张配置**：`图 5-1` **加宽到 1.000 `\textwidth`**；`图 5-2／图 5-3`（登记 5-5／5-6）**并置成对**（各自 **0.485 `\textwidth`**，同一 `figure` 内两个 `minipage`，**各自保留图号与 `\label`**）——§一 版式建议不再需要"缩小图宽"这一手段；② **表编号统一**：题面强制六表取消独立计数器、并入"章-序"（`表 1–6` → `表 5-2／5-3／5-5／5-6／5-7／5-8`），**正文同一节内不再出现两套表号**；③ **题注重复**（题注行与 `\caption` 各印一遍共 **24 处**）已修；④ 重编译实测：全文 **157 页**、正文 **29 页**、`Overfull \hbox` **28**、`Missing character` 44（余 `⟹`43／`✅`1 无字形）、`Float too large`／`Overfull \vbox`／未定义引用 **0**。**未改动任何数值、结论与图件** |
| **v2（附录 B 接线 ＋ 表格列宽轮）** | ① **附录 B 关键程序接线落地**（B.1–B.7 嵌入正文、B.8–B.13 列清单；`code/` 递归镜像含 `innov/`）⟹ §二 第 1 项由"待执行"改为 **✅ 已落实**；② **表格列宽按各列内容加权**（原各列均分）＋ 路径 `\allowbreak` 断行 ⟹ §一 第 1 项 `Overfull \hbox` **130 → 28 处**、§一 第 3 项正文 **30 → 29 页**（全文 158 页）；③ 代码注释缺字治理（`\lstmonofam` 等宽字体 ＋ ℃／①-④ 的 CJK 归类）：**396 → 44 处**（余 ⟹／✅ 无可用字形）。**未改动任何文字内容、数值、结论与图件** |
