# 源程序（附录 B 用）

> 本目录由 `50_论文/02_章节稿/gen_latex.py` 从 `20_交付包/09_代码与复现/code/` **递归镜像**而来（保留 `innov/` 子目录）。

- 源程序 **126** 个（`.py`／`.ps1`，其中 `innov/` **45** 个，含改进链脚本）；检验日志与结果数据属支撑材料，**不在本目录**。
- 附录 B **只嵌入关键程序正文**（B.1–B.7：公共模块 ＋ 四问求解）；其余（B.8–B.12 检验脚本、B.13 其余脚本）**只列文件名清单**，全文随支撑材料提交。块序见 `04_交排版成图方/附录B_代码块对应源文件.md`。
- **替换/新增源程序**：请改 `20_交付包/09_代码与复现/code/` 后重跑生成器（本目录**镜像同步**：源目录没有的 `.py`／`.ps1` 会被删除）。
