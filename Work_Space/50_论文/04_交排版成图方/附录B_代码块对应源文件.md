# 附录 B · 代码块 B.1–B.13 ↔ 源文件对应表（我方 → 排版成图方）

> **用途**：论文附录 B 的**编排顺序与收录口径**。官方《论文格式规范》第五条要求附录收录"建模所用到的**全部完整、可运行**的源程序"；本工程按**定稿裁定**采用"**关键程序全文 ＋ 全部程序清单**"的收录口径（全量 125 个 `.py` ≈ 20942 行，全嵌会把附录推到数百页）：
> ① **B.1–B.7（关键程序）**＝公共模块与四问求解的交付核、主流程 —— **逐块嵌入正文**（9 个文件 ≈ 2850 行）；
> ② **B.8–B.12（检验脚本）** —— **只列文件名清单**，正文不印；
> ③ **B.13（其余源程序）**＝`code/` 下其余全部脚本（**递归含 `code/innov/`**＝改进链脚本）—— **只列文件名清单**。
> ②③ 的**全文随支撑材料提交**，并在论文**表 A-1**（附录 A 类别 1.1／1.1b／1.3b／1.3c／1.3d／1.3e／**1.3f**／**1.3g**／1.3h）**逐项登记** ⟹ "完整、可运行"义务由**支撑材料 ＋ 附录清单**共同闭合。
> **纪律**：① 关键程序**不做删节**（仅按本表顺序重组）；② 清单部分**文件名要列全、不得按功能裁剪**；③ **不写运行环境与硬件信息**；④ 代码用**等宽字体、保留缩进**；⑤ 同一文件在多个块中复用属正常（按块切分展示，不重复计文件）。
> **上位**：`A_14_附录.md`（附录 B 正文与代码一致性声明）；`20_交付包/11_支撑材料包/A_支撑材料文件列表.md` §一（**逐项清单的权威源**）；`20_交付包/09_代码与复现/A_代码复现.md`。
> 按项目约定，本文件不引用／不记载资料中出现的日期。

## 一、十二块 ↔ 源文件

| 块 | 程序（模块） | 应嵌入的源文件（按此顺序串联） | 对应论文位置 | 收录 |
|---|---|---|---|---|
| **B.1** | 公共模块：网格与物性 | `code/q1_core.py`（网格生成与物性装配段），`code/q4_core.py`（附录 4 物性段） | 5.0.1／假设 3 | **正文嵌入** |
| **B.2** | 公共模块：元体平衡装配 | `code/q1_core.py`（三对角装配与中心半格段），`code/q2_core_c.py`（界面系数与装配段） | 5.1.3／式 (5-9)、(5-18)–(5-24) | **正文嵌入** |
| **B.3** | 公共模块：时间推进 | `code/q1_exact.py`（矩阵指数与分段线性推进），`code/q1_core.py`（含水率后向欧拉与迭代段） | 5.1.3／式 (5-25)–(5-30) | **正文嵌入** |
| **B.4** | 问题一求解 | `code/q1_solver.py`（主流程），`code/q1_core.py`（求解核） | 5.1 | **正文嵌入** |
| **B.5** | 问题二求解 | `code/q2_solver.py`（主流程），`code/q2_core_c.py`（交付核） | 5.2 | **正文嵌入** |
| **B.6** | 问题三求解 | `code/q3_solver.py`（事件驱动与二分主流程），`code/q3_core.py`（求解核） | 5.3 | **正文嵌入** |
| **B.7** | 问题四求解 | `code/q4_solver.py`（主流程），`code/q4_core.py`（物料坐标守恒核） | 5.4 | **正文嵌入** |
| **B.8** | 检验脚本：收敛性与误差带 | `code/q1_e1_gci.py`，`code/q2_w2_foreground.py`，`code/q3_w2_convergence.py`，`code/q3_grid_experiment.py`，`code/q4_w6_e15.py` | 6.1 | 清单 |
| **B.9** | 检验脚本：解析对拍 | `code/q1_e2_series.py`，`code/q2_e2_frozen.py`，`code/q4_w6_e2.py` | 6.2 | 清单 |
| **B.10** | 检验脚本：独立实现互验 | `code/q1_e3_explicit.py`，`code/q2_checks.py`（互验段），`code/q3_verify_nu.py`／`q3_verify_grid.py`／`q3_verify_opt.py`，`code/q4_core_fixed.py` | 6.3 | 清单 |
| **B.11** | 检验脚本：守恒核算 | `code/q1_conservation.py`，`code/q2_checks.py`（守恒段），`code/q3_conservation.py`，`code/q4_w6_e78b.py` | 6.5 | 清单 |
| **B.12** | 检验脚本：灵敏度分析 | `code/q1_e5_sensitivity.py`，`code/q2_e5_sensitivity.py`，`code/q2_e5b_global.py`，`code/q3_e5_sensitivity.py`，`code/q4_w6_e5.py`，`code/innov/q4_INV3_global.py`，`code/innov/q4_INV6_sobol.py` | 6.6／6.7 | 清单 |

> **路径口径（v2 修正）**：B.12 的第四问全局灵敏度两件**实体在 `code/innov/`**（`q4_INV3_global.py`／`q4_INV6_sobol.py`），原表误记作 `code/…`；本表已按**交付工程 `code/` 的实况**写全路径。生成器取本表第三列的反引号内容、去掉 `code/` 前缀后交给 `\srcfile`／清单（清单渲染时保留 `innov/` 层级）。

## 二、其余源程序（B.13：只列清单，正文不印）

除上表十二块外，附录 A 表 A-1 已逐项登记的下列各组**同属源程序**（官方第五条的"完整收录"义务范围）。本工程按上述口径，在附录 B 的 **B.13** 中列出其**文件名**（不嵌入正文），全文随**支撑材料**提交：

| 组（见附录 A 对应类别） | 内容 | 数量 |
|---|---|---|
| 类别 1.1 | 四问求解核、检验脚本、依赖清单 | 31 项 |
| 类别 1.1b | 问题一 改进代码（含精确推进核与各改进实验脚本、日志） | 24 项 |
| 类别 1.1c／1.2c | 问题一与问题二 检验日志 | 27 项 |
| 类别 1.3b／1.3c | 问题三与问题四 源程序与结果 | 41 项 |
| 类别 1.3d／1.3e | 问题二 改进代码与改进链支持脚本 | 20 项 |
| 类别 1.3g／1.3h | 第三问改进链脚本、复现与依赖 | 23 项 |

> **清单生成口径**：B.13 的逐条清单由生成器**按交付工程 `code/` 的目录实况自动列举**（`code/` 顶层 ＋ `code/innov/` 子目录，**递归**；`code/` 与 `code/innov/` 共 **125 个 `.py` ＋ `rerun_chain.ps1`**），并**减去 B.1–B.12 已列者**（去重）；逐项用途与大小以 `A_支撑材料文件列表.md` §一 为准。**检验日志与结果数据不属于源程序**，不进 `code/`（属支撑材料，不再重复登记于清单）。

## 三、排版建议（供贵方参考，不构成硬约束）

1. **关键程序（B.1–B.7）**：按本表顺序用 `\lstinputlisting`（或 `listings`／`minted` 环境）**逐块嵌入正文**，块首用一行**普通文本**标注 `B.1 公共模块：网格与物性`；代码块**不编号为表**；
2. **清单块（B.8–B.12）与 B.13**：以**紧凑的文件名清单**呈现（等宽字体），**不嵌正文**；
3. 附录 B 的**代码一致性声明**紧随附录 B 正文（原文见 `A_14_附录.md` 附录 B）；
4. 建议**等宽字体、字号不小于 7 pt**、保留原始缩进与空行；
5. **关键程序不得按功能裁剪代码本体**；清单部分**文件名不得删减**（要能逐条对上表 A-1）。

---

## 变更记录

| 版次 | 变更 |
|---|---|
| **v1** | 首版：十二块 ↔ 源文件对应表 ＋ 另须收录的四组清单 ＋ 排版建议。用于闭合附录 A 类别 1.3b 与附录 B 的**代码实体落地**待办 |
| **v2** | **收录口径裁定 ＋ 路径修正**：① 收录口径改为"**关键程序全文（B.1–B.7）＋ 全部程序清单（B.8–B.13）**"（不全量嵌入，避免附录页数爆炸），据此重写引言与纪律；② **B.12 的 `q4_INV3_global.py`／`q4_INV6_sobol.py` 路径由 `code/…` 修正为 `code/innov/…`**（与交付包实体、附录 A 表 A-1 v3 一致）；③ 新增 **B.13 清单生成口径**（生成器递归列举 `code/` ＋ `code/innov/`，去重后输出文件名）；④ 表增"**收录**"列，逐块标明嵌入或清单；⑤ §二 改为"只列清单"并按支撑材料类别补全组别 |
