# A 题 · 阶段一收口确认（文档地图 · 归档索引 · 状态声明）· v1

> **依据**：《A_阶段一收口总纲》（判定标准／轮次协议）。
> **本文件地位**：阶段一收口后的**现行文档地图与状态声明**——**新 Agent 只读本文件，即可知道"文档在哪、该读什么、什么还没做"**。
> **执行状态**：轮次 1（Math_1 写入）✅ ／ 轮次 2（Math_2 槽位补充）✅ ／ **轮次 3（改名＋归档＋引用同步）✅ 本文件即其确认**。
> 按项目约定，本文件不引用/不记载资料中出现的日期。

---

## 一、收口后文档地图（保留区 · **11 份**）

| # | 文件 | 读者用途（一句话） |
|---|---|---|
| 1 | `11_建模/11-0_选题与题型识别/A_选题与锁题决策.md` | 为何锁 A 题（含五维评估，完整矩阵见归档 F 组 v1） |
| 2 | `11_建模/11-1_题目分析/A_审题拆解.md` | 五要素／**12 条隐含条件**／四问子问题递进链 |
| 3 | `11_建模/11-1_题目分析/A_语义基线.md` | **L1 契约（最高权威）**：MB-01…MB-21、含义、变更规则 |
| 4 | `11_建模/11-1_题目分析/A_口径台账与裁定记录.md` | 口径 19 项状态 ／ 5 项用户锁定索引 ／ 证据索引 ／ 复核闭环 |
| 5 | `11_建模/11-2_模型推导/A_建模路线与选型.md` | 四问路线／数值方案／方法坑 P1–P10／检验基建／风险 |
| 6 | `20_交付包/02_符号表/A_符号表.md`（＋`.tex`） | 符号、单位、LaTeX |
| 7 | `20_交付包/03_假设清单/A_假设清单.md`（＋`.tex`） | **L2 唯一载体**：H1–H10 ＋ 论文呈现 ＋ LaTeX |
| 8 | `20_交付包/05_数值口径总表/A_数值口径总表.md` | **数值唯一真值源**：F／N／M ＋ 易错点 10 条 ＋ 5 项锁定 |
| 9 | `20_交付包/12_AI工具使用详情/A_AI工具使用登记.md` | 官方四要素（可直转"AI 工具使用详情.pdf"底稿） |
| 10 | `30_图表/01_图表总规划/A_图表总规划.md` | 图表 F-01–F-06（**待与阶段二实际出图重排号**） |
| 11 | `40_复核/02_差异台账/A_复核差异台账.md` | 复核差异唯一台账（**D-01～D-17 全闭环**） |

**命名规范（阶段一起）**：核心文档**无角色后缀、无日期**；版本以文内"**版次**"字段管理（如 **MB-v1.3**）。
**未动**：`00_规则/**`（方法论宪法）、全部 `00_*.md` 模板。

---

## 二、归档索引（`99_过程与归档/阶段一/`，共 **23 个文件**）

| 分组 | 数量 | 内容 |
|---|---|---|
| `A-口径过程/` | 2 | 口径歧义与矛盾研究（Math_1）／题目口径歧义研究与裁定（Math_2） |
| `B-裁定证据/` | 2 | 待裁定 4 项决策支持 ／ **MB-21 末行语义决策支持**（证据链 S1–S9 ＋ 通俗版） |
| `C-选型过程/` | 2 | 方向嗅探与初步分析规划（Math_1）／方向嗅探与选型（Math_2） |
| `D-架构论证/` | 1 | 语义基线与模型假设_分层定义（要点已并入 `A_语义基线` **§0.4**） |
| `E-复核往来/` | 15 | 全部复核单／指令／回执／通报／申请／总账／收口总纲与指令／收口回执 |
| `F-旧版次/` | 1 | 选题决策记录 v1（已并入 `A_选题与锁题决策.md`） |

**删除项：0**——本轮全部为**归档**（不删除），符合《工作约束》"隔离优先于物理删除"；临时脚本与中间产物已于早前清理。

---

## 三、引用同步结果

- **改名 11 处**：`A_语义基线`、`A_审题拆解`、`A_选题与锁题决策`、`A_符号表`（＋`.tex`）、`A_假设清单`（＋`.tex`）、`A_数值口径总表`、`A_AI工具使用登记`、`A_图表总规划`、`A_复核差异台账`；
- **保留区引用已同步**：`A_假设清单`（头部关联）、`A_口径台账`（§5）、`A_语义基线`（§5 引用表 → 归档标记 ＋ 补入两份合并版）；
- **历史行保护**：`A_AI工具使用登记` 与 `A_复核差异台账` 中的文件名属"**当时状态**"记录，已各加**命名与归档说明**指向本文件，避免被误读为现行路径；
- **顺带修正**：`A_语义基线` §6"复核修正（Math_2 直改）"计数笔误 **5 处 → 6 处**（Math_2 提请，实列 ①–⑥）。

---

## 四、阶段一状态声明（**防误判**）

| 项 | 状态 |
|---|---|
| 选题（锁 A） | ✅ 完成 |
| 审题拆解 | ✅ 完成 |
| 口径（19 项 ＋ 5 项锁定） | ✅ 完成 |
| 语义基线 MB-01…MB-21 | ✅ **全项闭合、零阻断**（MB-v1.3 ＋ L0 更正） |
| 模型假设 H1–H10 | ✅ 完成 |
| 数值口径 F／N／M ＋ 易错点 | ✅ 完成 |
| 复核差异 D-01–D-17 | ✅ **全部闭环（未闭环 0）** |
| **建模推导与求解（阶段③④）** | ⛔ **未开工**（`11-3_算法与管线`、`11-5_实验与决策链` 仍为**空模板**） |
| **图表实例化** | ⛔ 未开工（`20_交付包/04_图表包` 仅骨架） |
| 交付包 06／07／08／09／11 节 | ⛔ 仅模板 |
| 论文／摘要／支撑材料／"AI 工具使用详情.pdf" | ⛔ 未产出 |

---

## 五、遗留待办（阶段二入口）

1. **开工顺序**：**Q1**（隐式差分 ＋ 圆柱解析对拍，含"**预热期 $\partial_rT|_R>0$（表面先热）**"方向断言）→ **Q2**；**Q3／Q4** 归 Math_2（Q4 按 **MB-11** ＋ **L3-09**）。
2. `A_图表总规划` 的 F 编号须与阶段二实际出图**重排对齐**。
3. **阶段二起仍需执行本套收口流程**（合并 ＋ 清理）；用户已说明后续阶段会重新拆分角色文档。

---

## 六、留痕

- 本文件＝阶段一收口**最终确认**；AI 登记：交互 **#27**。
- 收口执行依据：《A_阶段一收口总纲》（已归档至 `阶段一/E-复核往来/`）。

---

## 七、三项确认（实测复核，非声明）

### ① 文档已清理干净 —— ✅ 是

- 保留区（含模板与工作区说明）共 **44 个 `.md`**，**无过程文件残留**：`40_复核/01_复核记录/` **仅余 `00_模板.md`**；目录收敛实测：`11-1` **9 → 4** 份、`11-2` **4 → 2** 份、`11-0` **3 → 2** 份；
- **角色后缀文件名：保留区 0 个** —— 全库 22 个带 `_Math_*` 的文件**全部位于** `99_过程与归档/阶段一/`；
- 归档区 **6 组 / 23 个文件**；**删除项 0**（隔离优先于物理删除）；
- 上一轮已同步引用 **27 处**，本轮复扫**无失效路径**。

### ② 按需保留 —— ✅ 是

| 保留文件 | 阶段二消费者／用途 |
|---|---|
| `A_选题与锁题决策` | 论文"问题重述/选题说明"的锁定依据 |
| `A_审题拆解` | 建模前复核题意（12 条隐含条件） |
| `A_语义基线` | **契约输入**：实现 Q1–Q4 前必读 |
| `A_口径台账与裁定记录` | 口径追溯与论文"数据说明/口径声明" |
| `A_建模路线与选型` | 数值方案与检验计划的执行依据 |
| `A_假设清单`（＋`.tex`） | 论文"模型假设"节直接取用 |
| `A_符号表`（＋`.tex`） | 论文符号表直接取用 |
| `A_数值口径总表` | **论文每个数字的唯一取数来源** |
| `A_AI工具使用登记` | 官方"AI 工具使用详情.pdf"底稿 |
| `A_图表总规划` | 阶段二出图排号 |
| `A_复核差异台账` | 复核闭环证据（交付审查用） |

→ **无"留着但没人用"的文件**；`11-3`／`11-5`／交付包 06–11／图表包为**阶段二空位**，非冗余。

### ③ 不再分角色割裂 —— ✅ 结构层已达成

**六个主题各只有一份权威**，不存在"两份并行"：口径 → `A_口径台账`；选型 → `A_建模路线与选型`；语义 → `A_语义基线`；数值 → `A_数值口径总表`；假设 → `A_假设清单`；复核 → `A_复核差异台账`。

### ④ 如实说明（文本层，不篡改历史）

保留文档中仍存在 `Math_1／Math_2` 的**署名式出现**（合计约 **113 行**；集中于 AI 登记 51 行、语义基线 18 行、差异台账 18 行）——性质为**协作来源署名与"双证/双复核"留痕**，**不是文档割裂**，故**不改写**。处置：

1. AI 登记与差异台账**各已加"命名与归档说明"**；
2. **导出阶段**（生成"AI 工具使用详情.pdf"时）建议把括号内代号**合并为中性表述**（如"独立复核"）；
3. 如需**现在**全库中性化，须做**保护式替换**（存档路径含旧文件名，不可误改）——**待用户指示**。

### ⑤ 另须知悉

`20_交付包/10_摘要与结论要点/` 下两个**非模板**小文件（`摘要要点卡.md`、`模型评价与改进.md`）**非本批产出**（工作区既有骨架），**保留**待阶段二填写。
