# A_偏移性检查报告（**范围收敛版：仅论文 LaTeX 与其原 md**）

> **检查范围（本版严格限定）**
> **IN**：`50_论文/04_交排版成图方/交付排版方论文/`（`main.tex`、`preamble.tex`、`sections/*.tex`、`frontmatter/*.tex`）＋ 其**原 md**：`50_论文/02_章节稿/成文/A_01–A_14.md`（并以 `02_章节稿/基线/*.md` 作"原稿"对照）。`main.pdf` 仅作为 tex 的**渲染视图**使用（用于读者可见性核实）。
> **OUT**：数值口径总表、`results/` 结果文件、`code/` 代码、支撑材料、交付过程文档、`40_复核/03_变更报告` 快照 —— **一律不读、不作为判据**。
> **检查性质**：只读。**本次未修改任何 tex／md**；产物只在 `06_偏移性检查/temp/`。
> 按项目约定，本文件不引用／不记载资料中出现的日期。

---

## 〇、与上一版报告的关系（诚实交代）

上一版报告的部分结论**依赖范围外材料**，本版**移出、不再作为结论**（详见 §四）。本版全部判据**只来自 tex 与 md**。

---

## 一、范围内结论

**论文本身没有发现"改变模型原意或结论"的偏移。** 六项范围内检查中，四项**全清**：

| # | 检查 | 结果 |
|---|---|---|
| 1 | **md ↔ tex 同版性**（本次新增，最关键） | **同版**：8 章二元组 Jaccard **0.964–0.995**；数值集合差异经定向核验**全部为清洗伪影**（含被我误当数值的 `\includegraphics[width=1.000\linewidth]` 参数） |
| 2 | **结构与引用** | 悬空引用 **0**；缺文献 **0**；未引用文献 **0**；**图 16 个 label → 16 个全被引用（100%）** |
| 3 | **数值内部自洽** | 摘要 **19 个**数据型数值**全部在正文/附录再现（0 孤儿）** |
| 4 | **机理与用语内部自洽** | 正向机理学名齐备；**反向表述 0**；**内部代号 0**（附录 A 的文件名除外）、**日期记载 0**、**"创新"自评 0**、**无身份信息**（命中项均为文献出版者与期刊名） |
| 5 | 图表与题注 | `caption` **48 = 图 16 ＋ 表 32**（严丝合缝）；表 32 个中 14 个被正文引用，其余 18 个为附录清单表与**表 A-1 的 13 个"（续）"分段**（无需引用） |
| 6 | **原味**（md 对基线 md） | AI 痕迹词 **全 0**；模板腔 **仅 1 处**；**第一人称判断句留存并增加**；句长普遍变短；四字格密度上升 30–50% |

---

## 二、逐项证据

### 2.1 md ↔ tex 同版性（`temp/_md与tex同版性.csv`）

| 章 | Jaccard | 判定 |
|---|---|---|
| 01 问题重述 0.977｜02 问题分析 0.995｜03 模型假设 0.993｜04 符号说明 0.975 | ≥0.975 | **同版** |
| 05 模型建立与求解 0.983｜06 模型检验 0.986｜07 模型评价与推广 0.989｜10 附录 0.964 | ≥0.96 | **同版** |
| 09 参考文献 0.890 | — | **差异为伪影**：文献页码（`21(1): 27-31`）无小数点，故"数值集合"两侧都匹配不到；`\bibitem{refN} 作者. 题名[J]. 刊名, 年, 卷(期): 页.` 格式规范 |
| 08 AI 声明 0.645 | — | **样本过小所致**（正文仅 1 句 53 汉字），非内容差 |

> **判据说明**：二元组 Jaccard 取**两侧对称清洗**（同样去掉标记与命令名），故对 LaTeX 标记不敏感；数值差集只在"两侧都保留表格内容"的前提下判定，并已对每个"仅 md／仅 tex"值**回原文定向复核**（`0.52`／`0.1653`／`3.245` 等两侧命中 0 ⟹ 确认为分隔符删除造成的拼接伪影）。

### 2.2 结构、引用与编号（范围内）

- `\ref` 目标 37、`\cite` 21、`\bibitem` 21：**悬空 0／缺文献 0／未引用文献 0** ✓
- **图**：label 16 → 被引用 16（**100%**）；图引用总数 **48**
- **表**：label 32 → 被引用 14；未被引用的 18 个 = `2-1`、`5-3-a`、`5-4-a`、`A-1-a…A-1-m`（13）、`B-1`、`C-1` —— **全部是附录／清单类表**，无需正文引用 ✓
- **式**：label 31 → 被引用 **7** ⟹ **24 个编号式在正文中零引用**（见 §三 R1-①）
- 附录 A 呈现 **表 A-1 … 表 A-14**：实为同一张表 A-1 的 **14 个"（续）"分段**（每段题注均带"（续）"），读者可辨 ✓

### 2.3 摘要（范围内 md 实测）

摘要正文 **不计空格字符 911 ／ 汉字 605 ／ 小数 28 处**；关键词 38／33；**合计 949** —— ≤1000 ✓；但其 19 个数据型数值在正文／附录 **100% 再现** ✓（无孤儿值）。

### 2.4 红扫（md＋tex 全文）

| 项 | 命中 | 说明 |
|---|---|---|
| 内部代号 `F\d{2}` | **0** | ✓ |
| 内部代号 `INV\d` | 11 | **全部是附录 A 表 A-1 中的真实文件名**（`code/innov/q4_INV0_regression.py` 等）—— 列表须与实体相符，属**必要** ✓ |
| 日期记载 | **0** | ✓ 符合"论文不记载日期"的约定 |
| 自评词"创新" | **0** | ✓ 全文用"改进" |
| 身份／机构 | 12 | **全部为文献的出版者与期刊名**（"湖北工业大学工程技术学院《化工原理》""华南理工大学学报""沈阳理工大学学报"）⟹ **非身份信息** ✓ |
| 口语／感叹 | **0** | ✓ |

### 2.5 机理与结论强度（范围内，逐条读原文）

- 正向学名齐备（元体平衡／有限体积／单元中心／严格守恒／M 矩阵／对角占优／极值原理／矩阵指数／杜哈梅尔／追赶法／三对角／子步级二分／交替推进／仿射收缩／物料坐标／第三类边界／积分平均／洛必达／半格／次随机／广义理查森／网格收敛指数／数据同化）。
- **反向 0**；命中的 4 个疑似词经读原文判定为**判据式／否定式**用法（"集总参数法**不适用**"、"**未做**多模型融合"、"引入数据驱动模型缺乏训练数据支撑"）。
- 结论强度**无通胀**：`证明` 4（含"**只证明**…**不等同于**…"）、`必然` 12（全部自带限定："属理论必然，**而非经验观察**"）、`唯一` 2、`首次` 4；**`最优／彻底／毋庸置疑／毫无疑问` 全 0**。

### 2.6 原味（md 对基线 md，`temp/_风格指纹对照.csv`、`_原味候选段.txt`）

| 指标 | 实测 | 基线 | 判定 |
|---|---|---|---|
| AI 痕迹词（赋能/助力/显著提升/完美/一体化/彰显/打造/抓手/痛点…） | **0**（"闭环"4） | "闭环"6 | ✅ 不高于基线 |
| 模板腔（综上所述/不难看出/众所周知/由此可见/总而言之/需要指出） | **0** | 0 | ✅ |
| 模板腔（**值得注意的是**） | **1** | **0** | ⚠️ **唯一新增**（元话语） |
| 第一人称判断句 | `本文认为` 6 ＋ `本文取` 4 | `本文取` 3 ＋`本文不采用` 1 ＋`本文选择` 1 | ✅ 留存并增加 |
| 平均句长（A_05/A_08/A_09/A_11） | 60.2／73.1／59.5／47.2 | 91.3／80.2／69.8／53.8 | ✅ 普遍变短 |
| 四字格（每百句） | A_05 300／A_07 220／A_08 209／A_11 240 | 197／71／141／191 | ⚠️ 上升 30–50%（R3） |

---

## 三、范围内偏移清单

### R0 阻断：**0 项** ✅

### R1 需修订（3 项）

| # | 项 | 证据 | 建议 |
|---|---|---|---|
| **R1-①** | **编号式 31 个，仅 7 个被正文引用**（24 个零引用：`5-2、5-3、5-4、5-6、5-8、5-10、5-13…5-25、6-1、6-2、6-7、6-8、6-10`）；正文无"式 (5-x)"文字引用（= 0） | `temp/p1b_refs.py`；A_08 自述"**只编号被正文引用的式**" ⟹ **自述与实际冲突** | **需你裁定**：给关键式补引用句（推荐）或撤回部分编号 |
| **R1-②** | md 元信息"独立核验"表声称 **`paper_lint` ❌ 0**，实测 **❌ 3**（合计 65 条发现）：A_08×2（含 **元信息附表少一个 `\|`**）＋ A_13×1 | `paper_lint --dir 成文 --mode draft` | 修正元信息附表的列数并**复跑机检**后据实改写各章声称（根因涉及范围外索引表，仅登记） |
| **R1-③** | **符号表外符号 7 个**：`f_1,f_2,f_3`、`F_s`、`Y`、`X_i`、`±10%`、`±20%` | `paper_lint` R-18 | 判定是"题面符号"（补入符号表）还是"新增未登记符号"（改用表内符号） |

### R2 需同步（2 项）

| # | 项 | 证据 | 建议 |
|---|---|---|---|
| **R2-④** | **摘要篇幅口径**：现测正文 **911 非空格字符／605 汉字**（合计 949） | 范围内 md 实测 | 与内部目标"≥800 字"的**口径关系需你一句话确认**（若指汉字则偏短；若指字符则达标） |
| **R2-⑤** | **图表解释句缺 3 处**（图 A-3／图 A-4／图 A-26 之后无"这意味着／表明／说明"式解释句） | `paper_lint` R-15 | 补解释句 |

### R3 观感（4 项）

| # | 项 | 说明 |
|---|---|---|
| **R3-⑥** | 附录 A 呈现 **表 A-1…A-14** | 实为同表 14 个"（续）"分段 ✓；但 md 元信息自述"**表 A-1 为唯一正式表**"措辞宜对齐 |
| **R3-⑦** | 选用依据表的括注"元体平衡（**差分／有限元**）" | 原意是"相对差分／有限元而言"，**当前写法易读窄**，建议加"相对…而言" |
| **R3-⑧** | 四字格密度上升 30–50% | 与句长变短**同时发生**，判为"删过程句、留凝练句"的压缩效应；**待你抽读** `temp/_原味候选段.txt` 的 12 段（我逐段看过：均为技术密度段，无空话排比） |
| **R3-⑨** | `main.pdf` 与 tex **差 2 个字形**（`≫/≪`，来自代码注释）；`preamble.tex` 已补映射 | **重编译一次即归零**，不涉任何数值与结论 |

---

## 四、**已移出范围**的结论（上一版有、本版不再声称）

| 上一版结论 | 为何移出 |
|---|---|
| "正文＋摘要 329 个数值未登记 = 0" | 判据来自**范围外**的数值口径总表 |
| "结果文件 4/4 命中、小时制换算自洽" | 判据来自**范围外**的 `results/` |
| P7 版本轴（67 快照、12 变化点、6 处消失值） | 快照位于**范围外**的 `40_复核/03` |
| 页数／体积／摘要字数的"多口径" | 属**过程文档**一致性问题（范围外） |
| 附录 B 收录口径（B.1–B.7→B.1–B.12）、冗余件删除 | 属**支撑材料／过程**层（范围外） |
| 文献 [21] 真实性检索 | 属**外部核验**，非 tex／md 内部判据 |

---

## 五、范围内未闭合项

| # | 未闭合 | 原因 |
|---|---|---|
| 1 | **md 元信息"独立核验"表的其他声称**（条数口径、数值已对表、无代号…）尚未逐条复跑验证 | 本轮只复跑了 `paper_lint` |
| 2 | **图表内数值与正文复述值**的逐格对照 | 需读图（图 PDF 在范围外，md/tex 内只有题注与引用句） |
| 3 | **原味人读盲评** | 需你或第三方读 20 段（材料已备） |
| 4 | **AI 声明措辞是否逐字符合官方模板** | 官方原文不属本范围；tex 内注释自述"逐字采用第（2）式" |

---

## 六、诚实边界

1. 本版**只读** tex 与 md（外加 PDF 作渲染视图），未修改任何文件；临时产物只在 `temp/`。
2. **同版性结论的可信度**取决于清洗对称性 —— 已对每个数值差**回原文复核**并标注伪影成因；剩余不确定项已列入 §五。
3. **"原味"无客观真值**，指纹仅作提示，最终判定需人读。

---

## 附：范围内取证件（`40_复核/06_偏移性检查/temp/`）

脚本：`p4_mdtex.py`／`p4b_mdtex.py`（同版性）／`p1_struct.py`／`p1b_refs.py`（结构引用）／`p3_terms.py`／`p3b_context.py`（机理与强度）／`p6b_segments.py`（原味候选）
产出：`_md与tex同版性.csv`／`_结构引用检查.csv`／`_词表扫描.csv`／`_风格指纹对照.csv`／`_上下文判读.txt`／`_原味候选段.txt`
