# 第二环节「章节内容库」全量重型独立复核 · 执行计划（v1 · 仅计划，未执行）

> **复核对象**：第二环节产出位 `50_论文/02_章节稿/章节内容库/` 的全部产出——**30 件**（`00_条目字段字典`＋`00_内容库总纲`＋`A_01…A_14_内容条目` ×14＋`A_01…A_14_图表登记` ×14）＋ 实施方案（流程对照）。
> **上游依据（判据源）**：基线（`02_章节稿/基线/`）、`A_章节计划与素材映射`、`A_论文写作骨架_本题落地`、`A_数值口径总表`、四问检验与创新采纳裁定、题录表、交付清单 §七、官方 21 条、我方第一轮复核报告（其 S5 即九项落点）。
> **定位**：这是第一轮"章节稿基线全面复核"的**下一层级**复核——对象从"基线稿"变为"**条目合同**"。条目是第三环节成文的唯一取材源，**条目错＝成文错**，故复核重心从"表述正确"转为"**依据真实、数值保真、覆盖闭环**"。
> **本文件状态**：计划 v1（2026-09-13 拟），**未执行**；执行以"第二环节落盘完成"为触发条件（见 §二）。
> 按项目约定，本文件不记载资料中出现的日期（文件名 MMDD 除外）。

---

## 一、时点条件（何时启动执行）

| 条件 | 判据 |
|---|---|
| C1 建库完成 | `00_内容库总纲` §一 文件索引 30 件全部 ✅（执行前先核对盘面实况，防总纲滞后） |
| C2 主 Agent 自检通过 | `check_contentlib`（C-1…C-7）＋`paper_lint --mode doc`＋`--mode draft`＋`build_indexes --check` 由主 Agent 复跑 ❌ 0（其结论留痕于实施方案 P4/P5） |
| C3 冻结状态 | 覆盖矩阵无 `⏸`；模块冻结登记完成（若分批冻结，可按"先冻结先核验"分波执行，见 §四-H0） |
| C4 并发约定 | 执行窗口内主 Agent 暂停改动内容库（或明确告知版本点）；**每次读取前做 mtime 快照**（沿用第一轮现势性复验纪律） |

> 若 C3 未满足而用户要求提前介入，则降级为"**在建抽检**"（只抽已完成模块），不做覆盖矩阵闭环判定。

## 二、核验维度（十维，按"主 Agent 机检覆盖不到"加权）

> 设计原则：**主 Agent 的 C-1…C-6 只做结构与称形**；本复核的重心放在它**做不了/没做**的六类深核（V3 依据真实性、V4 数值保真、V5 覆盖闭环独立重推、V6 三向一致深核、V8 状态真实性、V9 篇幅相容）。

| # | 维度 | 核什么 | 方法 | 相对主 Agent 机检的增量 |
|---|---|---|---|---|
| **V1** | 结构齐备 | 30 件在盘、命名、模块对应、索引表与盘面一致 | 只读清单比对 | 核"总纲≠盘面"漂移（清单≠实际也算漂移纪律） |
| **V2** | 条目 ID／字段／节号 | 13 字段齐、"不适用＋理由"、ID 形态与唯一、**所属节号 vs 基线 §三 骨架逐节比对** | 解析器＋骨架对照 | C-3 只查字段存在；本维**查节号合法性**（防自造节号） |
| **V3** | ★ 依据真实性 | 每条"依据（文件＋节/行）"**真实存在且内容相符**：数值类指口径表对应节且该节确含该值；叙述类指的素材文件定位可达 | **抽样全量验证**：数值类 100%，叙述类按模块抽 30%＋全部"关键依据" | **主 Agent 机检完全不查此点**（最高价值维度） |
| **V4** | ★ 数值保真 | 条目中的登记值 ↔ 口径表逐值；位数"登记几位写几位"；误差带＋合成口径表述；**同一数值跨条目写法一致**（口径漂移防护） | 数值提取器 → 口径表映射比对 | C 系列与 R-2 都不做"条目↔口径表"的值级比对 |
| **V5** | ★ 覆盖闭环独立重推 | 从 S1–S6 **独立枚举**覆盖项清单 → 与总纲矩阵逐项 diff：无"未回勾且无理由"项、无矩阵自称已承接但条目缺失的**虚勾** | 独立建表（S3 必写要点/S5 九落点/S6 21 条＋检查点为主干） | 主 Agent 是"自建自勾"；本维是**异体重推**（第二核心价值） |
| **V6** | 图表三向一致＋引用句 | 图表登记 ↔ `A_图表编号对照.md`（派生）↔ 交付清单 §七 ↔ 规格卡/CSV **存在性**；总账 51=20＋30＋1；每图"正文引用句"逐字质量抽读；表号口径（表 0-1/1-1/5-1/A-1 入册完整性） | 四源交叉表 | C-5 只对两源；本维补交付清单与 CSV 实存 |
| **V7** | 红线 17 条扫描 | 术语同形（13 项）／"改进"不用"创新"／问次汉字／内部代号与 F 号越位（内容条目属"正文承载件"，纪律同正文）／匿名脱敏／暂定口径标注（C-01…）／失败臂与事故不出现／形态禁令（摘要不分点等） | 扫描器（扩展第一轮 scan_paper 至内容库） | C-6 只查三条；本维全量 17 条 |
| **V8** | 状态机与冻结纪律 | 状态字段真实（自称"已取证"的条目 V3/V4 须真的通过；"已冻结"模块的冻结四条件逐项核）；变更痕规范；变更登记表与实际一致 | 状态×核验结果交叉 | 全新维度 |
| **V9** | 篇幅相容 | 每模块条目"目标篇幅"之和 vs 基线 §三 区间；偏离有无理由 | 汇总计算 | 全新（软核，出建议不出 ❌） |
| **V10** | 工具门禁独立复跑＋自有扫描 | 四命令独立重跑；我方扫描器（第一轮 scan/check 系列扩展） | 命令复跑 | 独立留证，不采信转述 |

## 三、读取源清单（执行前预备，含第一轮未全读件）

| 层 | 文件 | 本轮用途 | 预读状态 |
|---|---|---|---|
| L1 | `10_赛题/A题/A题.md`（表模板部分） | V6 表行列核验 | 已读（方案转述）→ 执行时回原件 |
| L2 | **`A_数值口径总表`（全量重读）** | V4 真值 | ★ 已有 §8–§11 精读底子；**执行前须重读**——口径表在本轮已新增 §十二 勘误记录等变更（00:47 版之后又有主 Agent 勘误） |
| L2 | 符号表／假设清单／题录表 | V4/V7 | 第一轮已读，执行时按需回查 |
| L2 | `30_图表/05_交付清单/A_图表交付清单.md` §七、规格卡目录、`04_图表包/data/` 实存清单 | V6 | ★ 新读 |
| L2 | `A_支撑材料文件列表.md`、`A_代码复现.md` | V3（模块 14 条目依据） | ★ 新读 |
| L3 | 模型叙述×4／结果与分析×4／可靠性×4／摘要要点卡／评价与改进 | V3（叙述类依据定位） | 抽查（第一轮读过交付包部分文件） |
| L4 | **`A_论文写作骨架_本题落地.md`（全读）** | V2 节号权威、V6 图表去向 | ★ 第一轮跳过，本轮必读 |
| L4 | `A_判据源冲突记录表`（C-01…C-18 现行判定） | V7 暂定口径标注核对 | 抽读（关注被裁定更新过的条目） |
| L4 | 我方第一轮复核报告＋台账 §十八/§二十 | V5 的 S5 源 | 已是我方产出 |
| 对象 | 内容库 30 件＋实施方案 | 复核对象 | 总纲/字段字典/A_08 已预读；其余执行时全读 |

## 四、执行步骤（H0–H8，产出随批落 temp）

> **专用目录**：脚本与中间记录一律落 `40_复核/01_复核记录/temp/h2/`（本轮新建子目录，避免与第一轮文件混放；不散落工作区、不落 root_temp 免与主 Agent 互相干扰）。正式报告执行后落 `40_复核/01_复核记录/`，差异入台账新节。

| 阶段 | 动作 | 产出 |
|---|---|---|
| **H0 时点与快照** | 核 C1–C4；全量 mtime 快照存档；确定单波/分波 | 快照清单 |
| **H1 机检复跑** | 四命令独立执行（V10） | 命令输出留痕 |
| **H2 解析建底表** | 写 `parse_entries.py`：从 30 件解析全部条目（ID/节/类型/依据/数值口径/图表/文献/篇幅/状态/验收判据）→ 单一 JSON 底表 | `entries.json`（预期量级：仅 A_08 即 72 条，全库估 250–350 条） |
| **H3 结构与字段核验** | V1＋V2：ID 形态/唯一/节号 vs 骨架/字段齐/类型合法 | 结构差异清单 |
| **H4 数值保真** | V4：数值提取 → 口径表映射逐值比对（脚本 `trace_numbers.py`，口径表真值行先结构化）＋跨条目一致性聚类 | 数值差异清单 |
| **H5 依据真实性** | V3：依据定位解析 → 文件实读验证（数值类全量、叙述类抽样 ≥30%＋关键项全查） | 依据失效/错位清单 |
| **H6 覆盖闭环重推** | V5：独立枚举 S3（14 模块要点＋检验 10 节）/S5（9 落点）/S6（21 条＋成绩评定检查点主项）→ 与总纲矩阵 diff → 抽验"已承接"指向的条目真实存在且内容相符 | 覆盖缺口/虚勾清单 |
| **H7 图表与红线** | V6＋V7：四源交叉表＋红线扫描器（扩展第一轮 scan） | 图表差异＋红线命中清单 |
| **H8 状态/篇幅/汇总** | V8＋V9；十维汇总裁定（❌/⚠/◻/💡 四级）；报告落正式位、差异入台账、AI 登记单行 | 复核报告 v2 体系新件 |

## 五、预置脚本规格（执行时实现，均只读）

| 脚本 | 输入 | 输出 | 要点 |
|---|---|---|---|
| `parse_entries.py` | 内容库 30 件 | `entries.json` | 兼容"条目总览表＋逐条详表"两种形态（A_08 已见总览式）；容错字段别名 |
| `trace_numbers.py` | entries.json＋口径表 | 数值差异清单 | 口径表真值行结构化（§8–§11＋§12 勘误）；位数逐字符比对（不归一化） |
| `verify_locations.py` | entries.json | 依据失效清单 | 解析"文件＋§节/行"→ 实读验证存在性＋关键词命中 |
| `coverage_diff.py` | 独立枚举清单＋总纲矩阵 | 覆盖 diff | 独立枚举表内置于脚本（S3/S5/S6 主干，可人工补正） |
| `scan_h2.py` | 内容库 30 件 | 红线命中 | 扩展第一轮 scan_paper 的代号/称形/图引用逻辑＋暂定口径标注检查 |

## 六、风险与对策

| 风险 | 对策 |
|---|---|
| 条目量大（估 250–350 条）导致 H4/H5 耗时 | 数值类全量（脚本化）；叙述类抽样 30%＋"关键依据"全查；抽样不达标的模块升级为全查 |
| 总纲矩阵与盘面不同步（建库在途） | H0 以盘面为准重建索引底表，总纲仅作对照 |
| 口径表持续被勘误（第一轮已见 §8.1/§8.4/§8.2 变更） | H4 执行前**重新快照口径表 mtime** 并在其留痕版上比对；报告注明真值版本点 |
| 主 Agent 并发改动 | C4 约定＋mtime 快照＋报告记录版本点（沿用第一轮现势性复验纪律） |
| "已冻结"判定与我的核验结果冲突 | 冲突即停并回询：先出报告，不自行改状态字段 |

## 七、授权与登记

- 执行前需用户确认：① 启动时点（C1–C3 满足后）；② V3 叙述类抽样比例（默认 30%）；③ 报告/台账仍按既定授权直写。
- AI 登记：按简化口径，执行轮单行登记（处置登记表续号）。

## 八、变更记录

| 版次 | 变更 |
|---|---|
| v1 | 首版计划：对象＝第二环节产出位 30 件；十维核验（重心为主 Agent 机检不覆盖的依据真实性/数值保真/覆盖闭环重推/三向一致/状态真实）；H0–H8 步骤；5 个预置脚本规格；时点条件与并发纪律 |
