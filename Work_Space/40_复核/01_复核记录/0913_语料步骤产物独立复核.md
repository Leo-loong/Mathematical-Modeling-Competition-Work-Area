# 0913 · 「国奖优秀论文语料」步骤产物独立复核（独立核验 Agent）

> **对象**：`50_论文/02_章节稿/国奖优秀论文语料/_笔记/` 8 件产物＋3 项参照件（流程 v1.6／台账 §二十五 行 1–11／公式展开方案）。
> **边界**：语料本体（11 md＋PDF/JPG）不做内容审读，仅按复算需要**脚本化定点访问**；内容库模块条目本轮未改动、不审。
> **版本点**：`_笔记/` 03:19–03:44 落盘版。
> **纪律**：本复核全程只读；复算脚本落 `40_复核/01_复核记录/temp/h2/corpus_recalc.py`／`gates_corpus.py`。
> 按项目约定，本文件不记载资料中出现的日期（文件名 MMDD 除外）。

---

## 〇、结论速览

| 项 | 结果 |
|---|---|
| **复算② 两处修正** | **证据充分、完全证实**：`A165` 独立"六、模型检验"章实测 **L1388–L1556＝169 行**（卡片"≈169 行"精确命中）；假设实测**恰好 8 条**、合计 **193 汉字**（卡片精确命中），`A092`/`A175` 各 **4 条** ✓ |
| **复算① 三项指标** | 引用÷图注**可复现**（图注行 A092＝33／A127＝50 与卡片精确一致；比值复算 0.09–1.04 vs 卡 0.09–1.16）；复合子图**去伪下降有据**（58 伪信号→复算 14 vs 卡 21，同量级，差异属括号匹配口径）；**小标题密度部分不可复现**（Y-01） |
| **六门禁** | ① 数值外泄：**无数值结论外泄**（仅结构比值，见 Y-04 措辞问题）；② 抄句：12 卡 8-gram **0 命中**；③ 卡可执行：40＋8 卡全部有"可迁移动作＋落点"；④ 落点全部存在；⑤ 题录表/A_13 **零**语料篇名；⑥ 同步 glob 覆盖 `_笔记/` ✓ |
| **总账核对** | 8 件产物与实况一致（文件名/版本）；待办 1 落点（公式展开方案）存在；kb 卡出处节号抽查真实（11.1.2／11.5.1／11.5.2 与教材原文吻合） |
| **发现** | **⚠ 3／💡 2**（Y-01…Y-05），**无 ❌ 级**；两处自修正确留痕充分 |

---

## 一、发现清单（Y 系列）

| # | 位置 | 发现 | 级别 | 处置建议 |
|---|---|---|---|---|
| **Y-01** | `A_语料亮点卡_第三批` GY-3-04 | **小标题密度两值不可复现**：卡片记 `A127`≈2、`A165`≈0.7 次/千字；按声明口径（顶级标题切段＋截附录＋剔代码＋剔纯数值）复算得 **A127＝0、A165＝0**（三级编号形态）；且 A127 之值与其自身"结构不可切、仅计数可信"定级**自相矛盾**。结论方向（"极不一致、属个人风格"）不受影响——复算值 0 vs `A175` 1.53 同样支持 | ⚠ | 更正/删除两值并注明匹配口径；或给出可复现的定义 |
| **Y-02** | `A_优秀论文高价值可借鉴内容` | "40 卡 ↔ 41 项**双向**映射"不完全成立：**8 张卡未被任何 GX 引用**（语料卡 GY-00-06／GY-00-12／GY-2-05／GY-2-07／GY-2-09／GY-3-04＋kb 卡 GY-KB-02／GY-KB-04；其中部分内容已被邻近项覆盖，如 GY-2-05≈GX-09）；且 GX-41 所引 **[GY-2-12] 系 [GY-00-12] 笔误**（GY-2-12 不存在） | ⚠ | 补 8 卡归并（或注明"已由 X 项覆盖/属登记类"）；改笔误 |
| **Y-03** | `A_语料结构提取_S2` | 对 `A175` 的定级"仅计数可信"与实况不符：实测 `A175` 有 **67 个中文序号顶级标题**（"一、问题分析"…"四、问题一的模型建立与求解"），且第四批已实际使用其结构信息（假设 4 条／无重述章／按问分章）——定级过严、与四批实际用法不自洽 | ⚠（低） | `A175` 升级为"部分可切"或加注；A127 维持"仅计数"（实测顶级标题 0，定级正确） |
| **Y-04** | 流程 §八 门禁 1 | 门禁措辞"**不得出现 ≥2 位小数数值**"与卡片自身形态冲突——卡片含结构比值 0.09–1.16／0.11–0.39／0.22–1.53 等（均为自算统计量，**无数值结论外泄**，意图已达成） | 💡 | 门禁 1 修订为"不得出现**语料数值结论**（结构量级统计除外）"，使规则可机检 |
| **Y-05** | S2／交接清单（轻） | ① S2 四篇字符数与实测差 0.3–1.8%（36084 vs 35827 等），计数口径未注明；② 交接清单参照件注"流程 **v1.5**"而实况已 **v1.6** | 💡 | 注明字符计数口径；参照件版本号随手更正 |

---

## 二、复算明细（供追溯）

| 项 | 卡片值 | 独立复算 | 判定 |
|---|---|---|---|
| GY-00-07 `A165` 检验章 | 约 169 行 | **L1388–L1556＝169 行**（章序：一重述/二分析/三假设/四符号/五建模/六检验/七评估） | ✅ 精确 |
| GY-00-08 `A165` 假设 | 8 条、≈193 汉字 | **8 条**（编号 1–8 逐条可读）、汉字 **193** | ✅ 精确 |
| GY-4-02 A092/A175 假设 | 各 4 条 | 各 **4 条** | ✅ |
| GY-3-02 `A165` 复合子图 | ≈21 次（剔代码） | **14 处**（截附录＋剔代码＋剔纯数值；样例"图1(b)""(a) 太阳时角…"）——同量级，差异属括号匹配口径 | ✅ 下降有据 |
| GY-3-04 小标题密度 | A175≈3／A127≈2／A092＝0／A165≈0.7 | 三级编号口径：**0／0／0／1.53**——A175 同向、**A127/A165 不可复现** | ⚠ Y-01 |
| GY-4-04 引用÷图注 | 0.09–1.16；A092 33／A127 50 图注行 | 图注行 **33／50 精确命中**；比值 **0.79／1.04／0.50／0.09**（卡 0.09–1.16，A175=0.09 精确） | ✅ 可复现 |
| S2 字符总量 | 35827/143651/37148/39577 | 36084/143965/37829/40664（差 0.3–1.8%，量级一致） | 💡 Y-05 |
| S2 定级 | A127/A175 仅计数 | A127 顶级标题 **0** ✓；A175 **67 个** ✗（Y-03） | ⚠ |

**门禁与映射**：抄句 12 卡 0 命中；题录表/A_13 零语料篇名；同步排除 glob 覆盖 `_笔记/`；41 项 GX 全部有来源卡号（45 处引用），8 张孤儿卡与 1 处笔误见 Y-02；"我方更强 5 项"中 3 项有 kb 独立支持（11.5.1 假设可验证／1.3 检验独立成章／教材局限讨论）✓。

## 三、对两处"自修正"的方法学判定

两处修正均为**对自身前批结论的推翻**，其新证据均已验证充分：`GY-00-07` 的"标题行直读"证据精确到行号区间且可复核；`GY-00-08` 的"顶级标题切段＋去伪口径"证据逐条可读。修正过程留痕完整（首批卡 v2/v3 变更记录＋第四批 GY-4-02＋总账 §3.4 三处互证），**修正方向正确、留痕合规**。唯一残留是第三批 GY-3-04 的两个密度值未随去伪口径刷新到位（Y-01）。

## 四、变更记录

| 版次 | 变更 |
|---|---|
| v1 | 首轮复核：8 件精读＋两项独立复算＋六门禁机检＋总账映射核对；**⚠ 3／💡 2／❌ 0**；两处自修正确认成立 |
