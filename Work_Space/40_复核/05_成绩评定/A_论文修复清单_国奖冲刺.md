# A 题 · **论文侧**修复清单（每项一段话 · 可直接执行）

> **本件定位**：把 `A_国奖差距与突破点_论文侧.md` 的差异点**逐项落成"一段话的修改方案"**，并按**可执行状态**三分：
> **① 本轮已改**（**9 项**：F-1／F-2／F-3／F-6b／F-10 ＋ **F-4／F-5／F-8／F-9**（v18 执行）＋ **C3甲**；逐项见 §一 与文末台账）｜
> **② 已裁定／待执行**（★ **已全部执行完毕**；剩余**仅 3 项**交付合规侧动作，见 §二 状态总表）｜
> **③ 受阻**（缺外部条件：**查重率**须外部工具，本区无此能力）。
> **前置核查已完成**（`code/grade_crosscheck4.py`，留证 `temp/_crosscheck4.txt`）：D-6 灵敏度参数实为 **5 个**（扩散／换热／传质各 ±20%，初始含水率 ±5%，初始温度 ±1 ℃）→ **达标**；D-7 误差指标 **9 类** → **达标**；D-17 ±20% 扰动句**已有**（仅缺判定句）；D-10 正文流程/示意类图**仅 2 张** → **差 3 张**；D-20 无独立启示段；D-15 排除式论证半具备；**15.1 抄题实测：论文↔题面最长公共片段 <20 字 ⟹ 无复制原题**。
> 按项目约定，本文件不引用／不记载资料中出现的日期。

---

## 一、① 本轮已改（**5 项**：F-1／F-2／F-3 ＋ **F-6b 图占位修复**（见 §三）＋ §二 的 F-10）

**F-1 · 选型"排除式"论证（对应 D-15／P-B 表 1.3 加分项 #1）**
在 `A_08` §5.0.2 段末、表 5-1 之前补一段"为什么不选另外三种做法"的排除口径：不用二阶中心差分，因中心点须特殊处理、守恒性只在代数上近似成立，而元体平衡是逐控制体严格守恒；不用显式格式，因扩散算子迫使显式步长受稳定性限制，含水率侧子步将被压到 10⁻³ s 量级；不用"显式＋大子步"逼近温度场，因其时间离散误差只随步长下降，而问题一温度场半离散后是常系数线性系统、可闭式积分直接消除该误差。**这三句全部是本文已记录的选用理由的"反向陈述"，不新增任何结论或数值**；量化对照仍沿用表 5-1 已有的"步长 1 s 与 60 s 相差 3.1×10⁻¹¹ ℃"。

**F-2 · 工程／工艺启示段（对应 D-20／P-A 国奖冲刺项"结果分析有启示"，并部分处置 D-14 人味浓度）**
在 `A_09` §5.5 正文末尾补一段（3–5 句）由**已登记结果**推出的工艺含义：① 由于存在压缩温升，物料最高温度（54.63 ℃）可高于环境温度上限，故工艺控温设定不能按环境温度直接取上限，须留出该内秉温升的余量；② 收缩使烘干时长较固定半径情形缩短约 61%，忽略收缩会系统性高估时长，故工艺排程不宜用常半径假设估算；③ 低含水率端经验式外推贡献了 43%–66% 的时长，末端时段的不确定性最大，故停机应以含水率阈值判据为准而非定时。段落中用"本文认为""值得注意的是"这类**判断性表述**承接，以降低模板化观感。**全部数字化用已在正文出现过的登记值，不新增测量。**

**F-3 · 灵敏度扰动"档位判定句"（对应 D-17）**
在 `A_10` §6.6 的"结论"句后补一句：按 ±10% 与 ±20% 两档幅度复核，前述参数影响排序与"无临界敏感参数"的判断**不变**，即结论不因参数估计误差的档位选择而改变。**只补"判定"的显性表述，不引入任何新的灵敏度数值**（避免与 §6.6 已给的归一化灵敏度 0.15／0.41–0.47 冲突）。

> **✅ 本轮实施核验（实测，非声明）**
> | 项 | 实测 |
> |---|---|
> | 落盘位置 | `A_08_模型建立与求解.md`（§5.0.2 末，表 5-1 之前）／`A_09_结果分析.md`（§5.5 正文末）／`A_10_模型检验.md`（§6.6 结论句后） |
> | 入稿确认 | `sections/*.tex` 命中"排除口径"1 处、"工艺含义"1 处、"两档幅度复核"1 处 |
> | 重生成 | `python gen_latex.py --apply`（版式件 **保留未覆盖**） |
> | 编译 | `xelatex ×2` ⟹ **0 错误**，`main.pdf` **131 页 / 1.39 MB** |
> | **排版告警** | **Overfull \\hbox = 0**、vbox = 0、Float too large = 0 |
> | **正文页数** | **仍 29 页（29/30，红线内，本轮零页数净增）** ✅ |
> | 摘要 | **未改动**（依 §F-10 的在案指令约束） |
> | 数值／结论 | 新增文字**只做已有理由的反向陈述与工艺含义引申**，未新增任何测量值或结论 |

---

## 二、② 已裁定／待执行（★ **本节各项已全部执行完毕**，逐项留档于下）

> **★ 状态总表（以文末台账 v22 为准；本节原"待执行"标题已过时，故在此同步）**
>
> | 项 | 内容 | 状态（实测） |
> |---|---|---|
> | **F-4** | 编号公式 → ≥30 | ✅ **已完成：31 式**（21 → 31；新增 (5-18)…(5-25) 与 (6-9)(6-10)） |
> | **F-5** | 正文补 4 张中间结果表 | ✅ **已完成**：表 5-3（Q1 网格/对拍）／表 5-4（Q2 耦合推进设置）／表 5-5（Q3 长时程收敛与定位）／表 5-6（Q4 网格·时间·取法） |
> | **F-6** | 四问流程图入正文 | ✅ 已完成（v4；图 5-3／5-10／5-11／5-15） |
> | **F-7** | 补近 5 年文献 | ✅ 按用户裁定**关闭**（补 3 篇 ⟹ 21 条；近 5 年 6 条 ＝ 28.6%） |
> | **F-8** | 求解复杂度与效率 | ✅ **已完成**：$O(N\cdot N_t)$ ＋ 实测墙钟（45／9.9（4.80×）／248.2／487 s，均取口径表登记值） |
> | **F-9** | 腾页 | ✅ **已完成**：删 表 5-2／图 5-8／图 5-9 ＋ 表述压缩 ⟹ 正文 31 → **30 页** |
> | **F-10** | 摘要压缩 | ✅ 已完成（严格口径 **940 字符**，落 [800, 1000]） |
> | **C3甲** | 图引用 ≥47 | ✅ **已完成：48**（表引用 49） |
> | **T-4** | 缺字 44 处 | ✅ 已闭合：**0** |
>
> **★ 仍未落盘（仅 3 项，均属交付合规侧）**：① **R1** 删除冗余留证件 `q4_w6_e5_stderr.txt`（**4.42 MB**，单一删除项已确认安全）；② **R2** 附录 B 源程序**扩至 B.12**（官方第五条"**全部**完整、可运行源程序"；官方第四条"附录页数不限" ⟹ 不触 30 页红线）；③ **R7** 把"多样性由 ≥8 种方法满足、不依赖多模型融合"一句写入**第七章模型评价**。

> **状态更新**：**C1 已定 ⟹ 取 ≥30** ⟹ **F-4 已执行完毕（编号式 16 → 31 ✅）**；**C2 已定 ⟹ 取交集** ⟹ **F-10 已执行完毕**（严格口径 **940 字符**，落 [800,1000] ✅）。**F-5／F-8／F-9 亦已执行完毕**（见 §二 状态总表）；**F-7 依用户裁定关闭**（补 3 篇后维持现状）。

**F-4 · 编号公式 16 → ≥30（对应 D-1／C1）**
方案：把**已有结论逐条形式化**为编号式，不新增结论——在 5.0.1 补"守恒律散度形式与变物性附加通量项"，在 5.1.3／5.2.3 补"元体平衡离散系数构造（界面调和／积分平均的代数式）"，在 6.1 补"误差带定义式与网格收敛指数式 $p=\ln(\phi_1/\phi_2)/\ln 2$、Richardson 外推式"，在 6.4 补"守恒覆盖判据式"，在 5.4.3 补"物料坐标下的守恒型能量方程与其离散式"。**★ 口径已定（用户裁定 C1「取交集、最严格」）**：按 **≥30** 执行（P-A 模块七 🟡"最高省二"侧），不取 ≥15。**执行要点（把页数代价压到最小）**：优先把**已存在的行内式升格为编号显示式**（全文行内式 ≈119 处 ⟹ 升格后每式仅多占约 1 行），再补 4 条**定义式**（离散误差带、观测阶 $p=\log_2(\phi_1/\phi_2)$、GCI／Richardson 外推、守恒覆盖判据）；**不新写无据公式**。**本项为本清单的下一执行动作。**

> **✅ 第一批已执行（本轮）**：编号式 **16 → 21**，新增五式 —— **(5-14) 毕奥数定义**（$Bi_T=hR_0/k\approx1.39$、$Bi_m=k_mR_0/D\approx3.24$）、**(5-15) 欠松弛更新式**（$\omega=0.7$）、**(5-16) 两场特征时间**（$2.37\times10^3$／$8.1\times10^4$ s）、**(5-17) 扩散长度**（$\ell_D=\sqrt{Dt}$）、**(6-8) 归一化灵敏度定义**（$S_i=\frac{\Delta Y/Y}{\Delta X_i/X_i}$）。**五式全部由正文已有数值与已述方法形式化而来，不新增结论**；稿内"式号口径"注（A_08）与成文修正表（A_10 #2）已同步更新。编译复核：**131 页／正文 29 页／Overfull 0／错误 0**（零页数净增）。
> **余 9 式的计划（下一批，需先读 5.2／5.3／5.4 与附录 4 定位）**：① 元体平衡通用平衡式；② 中心半格通量平衡式；③ 界面变系数积分平均式；④ 物料坐标变换式 $\xi=r/R(t)$；⑤ 物料坐标下的守恒型能量方程；⑥ 收缩律 $R(t)$；⑦ 达标判据式 $\max_r C=C_{\rm crit}$；⑧ 自适应子步规则；⑨ 判据敏感度定义式。

**F-5 · 正文补 4 张"中间结果表"（对应 D-9／P-A 5.4）**
方案：Q1 补"步长／网格对照表"，Q2 补"子步长×松弛因子对照表"，Q3 补"二分定位迭代表（区间收缩过程）"，Q4 补"网格×界面取法对照表"——**数据全部已存在于附录 C 与 `results/result*.xlsx`**，本项属"搬运＋表注"，不产生新结论。**推荐**：做，因为这是"工作量可见度"最直接的一击；**代价 +1 页**，同样受 30 页约束。

**F-6 · 每小问算法流程图进正文（对应 D-10）**
方案：把已到件的 `图48-F05 物质坐标变换示意`（Q4 用）保留在正文，同时将 **Q1 求解原理示意（F-07／图49）**与 **Q3 求解流程（F-51／图51）** 从支撑材料提升到正文 5.1／5.3 对应小节，使四问各自有 1 张流程/示意图。**推荐**：只提升 Q1、Q3 两张（Q2 已有 IMEX 流程图在支撑件、Q4 已有图 5-17）；**代价 +0.5～1 页**，且需绘图/排版侧配合投放。

> **✅ 已执行（本轮，比原方案多做 2 张）**：正文图 **9 → 13 张**，补入 **4 张流程图**并在各处配"如图 X 所示"＋"这意味着…"句（合机检 R-15）：
> · **图 5-3　问题一求解原理示意图**（F-07／图49）→ §5.1.3 末
> · **图 5-10　问题二双向耦合关系图**（F-21／图50）→ §5.2.1 末
> · **图 5-11　交替推进流程图**（F-22／图19）→ §5.2.3 末
> · **图 5-15　问题三求解流程图（事件驱动与子步级二分）**（F-51／图51）→ §5.3.3 末
> **原位补号、无需重排**（正是 减法轮留下的空号 5-3／5-10／5-11／5-15）；正文图引用 `\ref{fig:}` **34 → 42**（C3 甲项目标 47，达成 89%）；`图占位框 = 0`（4 图全部解析嵌入成功）。同步更新：A_08 元信息"呈现图 9 → 13 张"、占位清单 4 行、"不呈现"统计（11 → 7 张）；派生表 `A_图表编号对照.md` 已重跑刷新。
> **★ 图件尺寸校正（非排版调优）**：实测各图纵横比后发现 **图19（h/w=1.17）与图51（h/w=1.21）在 0.66 宽下高度达 12.4／12.8 cm，超出 P-A"流程图高度 ≤9 cm"红线**（并触发 `Overfull \vbox 301pt`）⟹ 在 `gen_latex.py::FIG_WIDTH` 按图定宽：**5-11 → 0.45（8.4 cm）、5-15 → 0.45（8.7 cm）、5-17 → 0.48（6.7 cm）**，全部收回红线内。
> **编译实测**：**131 页／正文 29 页（≤30 ✅）／0 错误／图占位框 0**；遗留 3 处 Overfull 见 **§三 排版遗留登记**（按用户指令**本轮不再处理**）。

**F-7 · 补 8 篇近 5 年文献（对应 D-4）**
方案：现 18 条中近 5 年仅 3 条（17%），需补 **8 篇左右**（2022 年以后）并在正文真实引用，使占比 ≥60%；**英文占比 56% 已达标，无需再补英文**。**执行约束**：项目纪律是"**文献必须真实可检索、禁止编造**"，故本项须在**逐条核验题名/作者/年份/卷期**后进行。

> **🔎 文献检索结果（本轮 · 依用户要求先查 `kb/source/联网搜索`）**
> 该目录共 **42 件**（含 PDF 原件与摘要 md）。**近 5 年（2022 之后）且题相关**的候选：
> | 候选（源文件名） | 年份/出处 | 是否已引 |
> |---|---|---|
> | `摘要_大红袍花椒热风干燥动力学与品质_食品与发酵工业2023.md` | 食品与发酵工业 2023 | **已引 [6]** |
> | `摘要_苹果丁联合干燥体积收缩机制_农业工程学报2024.md` | 农业工程学报 2024 | **已引 [7]** |
> | `IMEX与BDF格式族_抛物型方程_SIAM2024.md` | SIAM 2024 | 疑与 **[10]** 同源 |
> | `扩散方程反问题的正则化方法_应用数学进展2024.md` | 应用数学进展 2024 | 新 |
> | `热传导方程两类参数的统计反演方法_应用数学进展2025.md` | 应用数学进展 2025, 14(3):203-215，DOI 10.12677/aam.2025.143106（胥文博） | 新 |
> | `摘要_IMEX_BDF2后验误差估计与自适应时间步_2025.md` | J. Comput. Appl. Math. 2025（DOI 10.1016/j.cam.2024.116318）／Calcolo 2025（DOI 10.1007/s10092-025-00638-z）／arXiv:2506.18809 | 新（3 条） |
> | `IMEX_RB自适应隐显格式一阶无条件稳定_arXiv2025.md`、`自适应隐式时间步最优性_arXiv2025.md` | arXiv 2025 | 新（预印本，引用效力较弱） |
>
> ⟹ **实测可用新增量 ≈ 4–6 篇**（近 5 年、真实可核、与本题"反演／IMEX-BDF／自适应步长"直接相关）。
> **⚠ 关键结论**：**"近 5 年 ≥60%" 在本项目不可达**——现 18 条中近 5 年仅 3 条；即使把上列候选**全部补入**（+6），总 24 条中近 5 年 9 条 ＝ **37.5%**，仍低于 60%。硬达 60% 须**替换掉承载方法学依据的经典文献**（Crank 1957／Murray–Landis 1959／Gibson 1959 等），**得不偿失**。
> **建议**：补入 **4–6 篇**（近 5 年占比升到 ~30–35%）并保留全部经典；**若你要硬达 60%，需明确授权"删经典"**。

> **✅ 用户裁定（本轮）**：**"只能尽力到这里了，不能删掉经典文献"** ⟹ **F-7 关闭为"维持现状"**：**不删任何经典文献**、**不再新补**（近 5 年维持 3 条／18 条）；该项在评估中按**已知口径分歧**记录（不据此扣分）。如后续仍要补 4–6 篇，随时可执行（候选清单已备）。

**F-8 · 计算复杂度与求解效率（对应 D-12／S3 5.4、5.10）**
方案：在 5.0.2 补一段"成本—精度口径"：说明空间离散后为**三对角／五对角稀疏系统**、单步求解代价为 $O(N)$、总代价为 $O(N\cdot N_t)$，并给出**实测墙钟时间与硬件环境**。**执行约束**：墙钟时间须**真实测量**（不可估写），而重跑求解脚本属 `00_执行要点.md §四-7` 的**升门禁**动作（须先出六栏申请单）。**推荐**：若 `logs/` 中已留运行时长则直接引用（零门禁）；否则申请单后再测。

**F-9 · 腾页（F-4／F-5／F-6 的**前置条件**）**
方案：正文现 **29 页**，上限 30 页（S3 13.10：>30 页 → ❌；P-A：25–30 页红线）。F-4+F-5+F-6 合计 **+3.5～5 页** ⟹ 必须先从 `20_交付包/13_论文可删减部分` 启用 **≥4 页**的删减。**推荐**：优先删"与题面表重复的呈现段"与"第五章可合并的推导过渡句"，**不动任何数值、结论与检验**；并按项目纪律在删减留痕中逐条登记。

**F-10 · 摘要压缩（对应 D-3／C2）——✅ 本轮**已按你的裁定执行完毕****
**裁定**：C2 取**交集**＝「**≤1000 字符（不计空格、含关键词）** 且 **≥800 字**」。**执行**：七段逐段**同义紧凑化 ＋ 删冗余修饰**，并卸去三处非数值性从句（"（约 2.40 天）"／"两场特征时间相差约 34 倍，是长时程耗时的根源"／"四组对照量化了简化与取法的影响"——**三句在正文 5.3／5.5 与第六章均仍在，摘要只是卸冗余**）。**实测结果：正文 962 ＋ 关键词 37 ＝ 999 字符（不计空格）、汉字 640，落 [800, 1000] 区间 ✅；29 处精确数值一个未动；七段结构与"针对问题 X"段首未变**。已在 `A_02_摘要.md` 回填：元信息 ★字数段、自查第 9 项、独立核验第 16 项与**变更记录 v5**。编译复核：**正文 29 页／Overfull 0／错误 0**。

---

## 三、③ 受阻（1 项）＋ **排版遗留登记**（按用户指令停手，待专门处理）

> **⚠ 排版遗留登记（按用户指令"页数暂不设限、后续专门检测处理"）**
>
> | # | 位置 | 告警 | 量级 | 状态 |
> |---|---|---|---|---|
> | T-1 | p12（`sections/05_模型建立与求解.tex`） | `Overfull \vbox` | ~~301.01 pt~~ | ✅ **已消失**（行距下调后页内可容 ⟹ vbox 归零） |
> | T-2 | p16 | `Overfull \vbox` | ~~95.61 pt~~ | ✅ **已消失** |
> | T-3 | p10 | `Overfull \hbox` | ~~6.80 pt~~ | ✅ **已消失** |
> | T-4 | 附录 B 代码注释 | `Missing character` ×44 | 43×`⟹`(U+27F9) ＋ 1×`✅`(U+2705) | ✅ **已消失**（v7 修：`newunicodechar` 映射到已有字体 ＋ `literate` 二次保险，**未改 `code/` 任何字节**） |
>
> **当前实测（正式 `main.pdf`，本轮 v19 后）**：全文 **126 页**｜**正文 30 页（＝红线 ≤30 ✅）**｜LaTeX 错误 **0**｜`\hbox` **0**｜`\vbox` **0**｜`Float too large` **0**｜图占位 **0**｜`??`（未定义引用）**0**｜`Missing character` **0**｜`LaTeX Warning` **0**｜**全文疑似重叠 0**（环绕图 5 处）。
> **已试无效并已回退的两项**（依"无证据不留改动"纪律）：① 前言注入浮动放置容限（`\topfraction 0.92`／`\floatpagefraction 0.75`／`totalnumber 6`）——位置与 vbox **均无变化**，已删；② 把 图5-15（Q3 求解流程图）由 0.45 缩到 0.40——落位**无改善**，已回退到 0.45（仍守"流程图 ≤9 cm"红线）。⟹ 结论：**位置漂移源于正文流量与页面断点，不是 TeX 拒绝落位**。

### ★ 版式三要素的**官方依据核查**（本轮 · 依 `kb` 内官方规范原文）

| 要素 | 官方条款（原文） | 能否调小 | 本轮处置 |
|---|---|---|---|
| **页边距** | **第一条**："论文用白色 A4 纸打印；**上下左右各留出至少 2.5 厘米的页边距**；从左侧装订" | **❌ 不可调小** —— 本件 `\geometry{...=2.5cm}` **恰好压在下限**，再小即违规 | **未动**（保持 2.5 / 2.5 / 2.5 / 2.5 cm） |
| **字号 / 字体** | **第八条**："本规范中未作规定的，如论文的**字号、字体、行距、颜色**等**不做统一要求**" | ✅ 可调 | **未动**（正文 12 pt＝小四，国赛惯例口径；如需再省页可一键改五号 10.5 pt） |
| **行距** | 同上（第八条，**不做统一要求**） | ✅ 可调 | **1.06 → 1.0（单倍）**：全文 **132 → 126 页**、正文 **31 → 30 页**，并**顺带消除 T-1 的 301 pt vbox** |

> 依据源：`kb/source/第二批提取后/A157 …/2026国赛官方通知/全国大学生数学建模竞赛论文格式规范.md`（第一条 L11–12、第八条 L27）；另见 `00_规则/建模作业规范.md` §6（"字号/字体/行距/颜色官方**不做统一要求**；官方硬性仅 A4、页边距 ≥2.5 cm、页码页脚居中自摘要页起"）。
> 交付件同步：`preamble.tex`（L10 `linespread=1.0` ＋ 上方注释留证）与生成器模板 `gen_latex.py` **同步修改**（`preamble.tex` 带"保留"门禁，须两处同改）。

**F-11 · 查重率（S3 15.1 的剩余部分）**
本轮已用可自测的方式处置了其中一半：**论文与题面的最长公共片段 <20 字 ⟹ 无复制原题、无大段抄袭迹象**（`temp/_crosscheck4.txt`）。剩余的"查重率 ≤15%–20%"须用**外部查重工具**（知网/维普等）判定，本区无此能力 ⟹ 该项保持 ⬜ 待外部取证。

---

## 四、判据冲突 C1–C4 —— **状态一览**（源名与原文见 §五）

| # | 冲突双方 | 状态 | 处置 |
|---|---|---|---|
| **C1** | 编号公式 **≥30**（升级提示词 模块七）vs **≥15**（同文件 工作量硬指标 #1） | **✅ 已裁定** | 用户裁「**取交集、最严格**」⟹ **按 ≥30 执行**（＝F-4，本清单**下一执行动作**） |
| **C2** | 摘要 **≤1000 字符**（升级提示词 模块五 🔴）vs **800–1200 字**（进阶版 1.1） | **✅ 已裁定 ＋ 已执行** | 取交集 ⟹ **≤1000 字符（不计空格、含关键词）且 ≥800 字**；**实测 999 字符**（正文 962 ＋ 关键词 37）、汉字 640 ✅ |
| **C3** | 图表计数：**引用次数**（进阶版 6.6）vs **全文件数**（升级提示词 工作量 #2／#3） | **✅ 已裁定（本轮）** | 用户裁：「**按甲（引用次数），但最好都满足**」⟹ 目标 **图引用 ≥47 次**（现 34）**且同时满足乙的件数门槛**（图 ≥10 ✓／全文表 ≥18 ✓）。增收方式＝**补图（F-6）＋在相关结果段增"如图 X 所示"式引用**，不作无内容支撑的堆砌 |
| **C4** | 「独立灵敏度分析章」：**字面**（进阶版 7.1／1.9）vs **意图**（同文件 §一·1.3） | **✅ 已裁定（本轮）** | 用户裁：**「暂不裁决，保留现状」** ⟹ **不动** 6.6／6.7 的结构与标题层级；该项在评估中记为**已知口径分歧**（不据此扣分） |

---

## 五、本轮裁定记录 ＋ **C3／C4 源清单**（源名与原文**留档**）

### 5.1 已裁定（**依用户本轮指令**）

**C1 编号公式数量**：**取交集、满足最严格** ⟹ **≥30**（不取 ≥15）。按此执行 **F-4**。
**C2 摘要篇幅**：**取交集、满足最严格** ⟹ **≤1000 字符（不计空格、含关键词）且 ≥800 字**。**已执行**（实测 **999** 字符）。
**C3 图表计数**：**按甲（引用次数）**，**并力争两口径都满足**（甲：图引用 ≥47／表引用 ≥27.6；乙：图 ≥10／全文表 ≥18）。
**C4 独立灵敏度章**：**保留现状、暂不裁决**（不动 6.6／6.7 结构）。

### 5.2 C3「图表计数口径：正文 vs 全文」—— **两方源名与原文**

| 方 | **源名称**（kb 内路径） | 条款 | 原文（关键句） |
|---|---|---|---|
| **甲 · 按"引用次数"** | `AI提示词合集/02_检测自查与优化/AI论文检查与获奖预测！数学建模论文全维度自查与获奖预测提示词！（进阶版）.md` | §一·1.4「图表」行；§二 维度六 **6.6 图表数量充足【国奖级】**；§附录A | 6.6 判据："图表数量是否充足（国奖级论文**平均图引用 47 次、表引用 27.6 次**）"；§一·1.4 同款"平均图引用 47 次、表引用 27.6 次" |
| **乙 · 按"全文件数"** | `AI提示词合集/02_检测自查与优化/2026国赛国奖级论文AI自动优化升级提示词.md` | §六·1 工作量 10 项硬指标 **#2／#3**；模块六·3 | #2："**表格数量 ≥18 个**（建议 20–25）｜全部三线表，**含中间结果表**"；#3："**图片数量 ≥10 张**（12–15）"；模块六·3："**全文**图表总数建议 15–25 个（含表格）" |

**我方实测**：正文 **图 15／表 13**；全文**表 29**；引用 `\ref{fig:}` **34** 次、`\ref{tab:}` **34** 次。
⟹ **按甲**（引用次数）：图 34 ＜ 47 **偏低**；表 34 ＞ 27.6 达标。
⟹ **按乙**（件数）：图 15 ✓、全文表 29 ✓ **达标**（但正文口径的表 13 ＜ 18 则不达）。
**请裁定**：口径取甲（引用次数）／乙（全文件数）／乙′（正文件数）。

### 5.3 C4「独立灵敏度分析章」—— **两方源名与原文**

| 方 | **源名称**（kb 内路径） | 条款 | 原文（关键句） |
|---|---|---|---|
| **甲 · 要求"独立章"（字面）** | `AI提示词合集/02_检测自查与优化/AI论文检查与获奖预测！数学建模论文全维度自查与获奖预测提示词！（进阶版）.md` | §二 维度七 **7.1 独立灵敏度章节【国奖级】**（及维度一 **1.9** 同款） | "是否有独立的'灵敏度分析'或'敏感性分析'章节｜**无独立章节→❌**" |
| **乙 · "独立"的目的＝不与结果混写** | **同一文件**（同上） | §一·1.3「模型检验共性特点 · 独立章节」行的**国奖级标准列** | "**检验内容独立成章，不是藏在结果分析里**" |
| **旁证 · 只要求"齐全"与"有数据"** | ①`…/2026国赛国奖级论文AI自动优化升级提示词.md` 模块四；②`第一批提取后/老哥讲义！2026数学建模国赛冲奖要点全解析！.md:688` 潜规则 4 | 模块四首句；潜规则 4 | ①"三类检验必须齐全，每类必须有量化指标＋可视化图表＋明确结论。**缺少任意一项→最高省二**"（未提"章"的粒度）；②"**检验是加分项**……做了灵敏度分析、误差分析、稳健性检验的论文，明显比没做的高一个档次"（同样只讲"做了"） |

**我方现状**：`sections/06_模型检验.tex` 为**独立一章（11 个小节）**；其中 **6.6「参数灵敏度（局部）」与 6.7「参数灵敏度（全局）」为独立小节**。
**请裁定**：按甲（须另立"灵敏度分析"章，可零页数代价把 6.6／6.7 升格为章）／按乙（现结构即满足）。

---

## 六、变更记录

| 版次 | 变更 |
|---|---|

| 版次 | 变更 |
|---|---|
| **v22（用户 10 条图排整改）** | **先建"印刷图号 → 论文内索引号"映射**（实测：图 5-1←5-1／5-2←5-3／**5-3、5-4←5-5、5-6**／5-5←**5-10**／5-6←5-11／5-7←**5-12**／5-8、5-9←**5-15、5-16**／5-10←**5-17**／5-11←**5-20**／6-1←**A-1**／6-2、6-3←**A-10、A-2**／6-4←**A-20**／6-5←**A-9**）。逐条处置（尺寸＝PDF 实测，单位 cm）：① 图5-3/5-4 间隔 **1.44 → 0.48**、图 **7.3 → 7.8**（新 `PAIR_W_BY` 逐图宽度）；② 图5-5 **10.4×4.3 → 12.6×5.2**（`FIG_WIDTH["5-10"]` 0.66→0.80）；③④ 图5-7 **由环绕改回居中嵌入**并放大到 **9.6×7.7**（`AW_NO_WRAP` 记明理由：环绕致"往左下偏"＋第 16 页预留空位溢出到下一页）；⑤ 图5-8/5-9 **改为左侧竖直排列的环绕式**（同一 `wrapfigure{l}`、各占 `\linewidth` ⟹ **宽度必然一致**；实测 6.3×7.7 与 6.4×4.3、整体靠左、正文绕右，**已目视确认**）；⑥ 图5-10 **7.6×6.6 → 8.8×7.7**（5-17：0.48→0.56）；⑦ 图5-11 **改环绕**（`WRAP_FIGS["5-20"]=(0.50,"r")`）——实测 0.58 宽时与紧随其后的 **表 5-6 相撞**（表格不能绕排）⟹ **把表 5-6 提到该图之前** ＋ 宽收到 0.50，复核重叠 0；⑧ 图6-1 **已整版宽 16.0**、两幅面板**内部白边实测 0.0%** ⟹ 无放大余量（唯一途径是把 a/b 改上下排列，将新增约 0.3 页与 12.6 高的块）——**本轮保持**并留待裁定；⑨ 图6-2/6-3：**A-10 0.50→0.52、A-2 0.50→0.44**（**6-2 多放大**，两幅高各≈5.3、视觉等高）；⑩ 图6-4/6-5 **10.6×6.3 → 12.8×7.7**、**10.6×4.9 → 12.8×6.0**。**终态：126 页／正文 30 页／错误 0／hbox 0／vbox 0／Float 0／图占位 0／`??` 0／缺字 0／Warning 0**；"疑似重叠 2"经查为**图内自身标签**（放大后字高越过 10pt 触发检测阈值），非真重叠。**未改动任何数值、结论与检验** |
| **v21（用户问"有没有办法自动处理环绕图表，是否必须手动调参数"）** | ① **先回答事实**：`wrapfig` **本身不能自动定宽**（TeX 层没有"量图再定栏宽"的机制）⟹ **纯 LaTeX 做不到自动**；但本工程是「md → LaTeX 生成器」结构，生成器**在生成时能读到每张成品图的真实尺寸** ⟹ 于是"要不要环绕、环绕多宽"变成**可计算量**，不必人工试错（**原先每张环绕图的宽度都是手填常数 0.40–0.45，正是"很多图很小"的根因**，因常规嵌入宽是 0.50–1.00）。② **已实现自动判定引擎**（`gen_latex.py::auto_wrap` ＋ `_fig_size_cm`，用 PyMuPDF 实测成品图单页尺寸）：四条判据＝**①** 图侧正文栏 ≥0.42 版心（图宽 ≤0.58）；**②** 图高 ≤8.0 cm（由该图**实测纵横比**反算宽度上限）；**③** ★**环绕不得把图缩小**（算出的宽必须 ≥ 该图常规嵌入宽，否则一律嵌入）；**④** 纵横比 ∈[0.30, 1.60]。取满足四条的最大宽，无解则不环绕；另设**例外名单** `AW_NO_WRAP`（现仅 **图5-17**，附实测理由：其后紧邻展示式 (5-24)、侧栏容不下）。预演输出**逐图打印判定依据**，可复核。③ **实测效果**：15 张图中 **13 张"不环绕"（保住 0.45–1.00 的嵌入大图）**，仅 **2 张环绕且均比嵌入更大** —— 图5-11 **0.519**（嵌入 0.36）、图5-12 **0.580**（嵌入 0.50）⟹ **直接回应"很多图很小"**；`wrapfigure` 由 5 处降为 **2 处**。④ 终态：**126 页／正文 30 页／错误 0／hbox 0／vbox 0／Float 0／图占位 0／`??` 0／缺字 0／Warning 0／全文疑似重叠 0**。**未改动任何数值、结论与检验** |
| **v19（同轮 · 排版两项用户指令）** | ① **标题断行**（用户指令"把'耦合模型的'移到第一行"）：第 1 行改为以"……传热传质耦合模型的"结尾、第 2 行为"建立、达标时间反演与收缩效应分析"；改在生成器 `TITLE_BREAK_BEFORE`（`"耦合模型的建立"` → `"建立、达标时间反演与收缩效应分析"`）**并同改**摘要页文件；**PDF 首页实测**：第 1 行"药材的烘干问题：圆柱非稳态传热传质耦合模型的"✅、第 2 行"建立、达标时间反演与收缩效应分析"✅。② **批量环绕**（用户指令"批量把嵌入式图表改为环绕式，缓解页数超限"）：`WRAP_FIGS` 新增 **图5-3（0.40）／图5-10／图A-9／图A-20（各 0.45）**，连同既有 **图5-11** 共 **5 处 `wrapfigure`**；入选判据＝① 图后连续正文足够长（≥图高）② 非并置图 ③ 非整宽图。③ **图5-17 撤出环绕（实测）**：环绕后其右侧栏（0.55 版心）容不下 §5.4.2 的展示式 (5-24)（收缩律插值式）⟹ 出现 `Overfull \hbox` **98.5pt** 与"公式压图"重叠（p19 实测命中）⟹ 已撤出，**复测重叠 0**。④ 终态：**126 页／正文 30 页／错误 0／hbox 0／vbox 0／Float 0／图占位 0／`??` 0／缺字 0／Warning 0／全文疑似重叠 0**。**未改动任何数值、结论与检验** |
| **v18（内容轮 · 执行全部二类 ＋ 新问题一并解决）** | ① **F-4 编号公式 21 → 31（≥30 达成 ✅）**：新增 (5-18) 元体平衡通用通量平衡式／(5-19) 总代价式／(5-20) 中心半格通量平衡式／(5-21) 物性逐子步更新式／(5-22) 判据等价性式／(5-23) 物料坐标变换式（升格）／(5-24) 收缩律插值式／(5-25) 守恒与非守恒形式之差／(6-9) 理查森外推与 GCI 式／(6-10) 判据敏感度弹性式；**全部由正文已有结论与已登记数值形式化而来，不新增结论**。② **F-5 正文补 4 张中间结果表**（表 5-3 Q1 网格加密与对拍／表 5-4 Q2 耦合推进设置与收敛／表 5-5 Q3 长时程收敛与达标定位／表 5-6 Q4 网格·时间·界面取法），**数据全部取自正文与《数值口径总表》登记值**。③ **F-8 求解复杂度与效率段**（§5.0.2）：$O(N\cdot N_t)$ ＋**实测墙钟**（问题一约 45 s／问题二 9.9 s（原 47.6 s，4.80×）／问题三 248.2 s／问题四 487 s），**墙钟均取自口径表登记值**（零门禁）。④ **F-9 腾页（只许内容手段）**：删 表 5-2（三套物性对照 → 一句文字）、图 5-8（灵敏度龙卷风图，改由 6.6／6.7 承载）、图 5-9（变物性演化 → 文字），并压缩 §5.1.1b／§5.1.4／§5.2.4／§5.3.4／§5.4.5／§5.5 与 §6.1／§6.3／§6.8／§6.9／§6.11 的冗余表述 ⟹ **正文 31 → 30 页（红线 ≤30 ✅）**。⑤ **C3甲 图引用 42 → 47（≥47 达成 ✅）**；表引用 40 → 49。⑥ **新问题一并解决（依用户"不必再请求授权"）**：**正文直引号 82 → 4 处**（改在生成器 `inline()`，**只对成对引号生效**，数学片段与行内代码受保护）；**摘要压缩**（只删冗余从句；(5-22) 句"两段式"等数值全留）。⑦ **人味判断句** 3 处（"本文认为"×2／"值得指出的是"×1）。⑧ 生成器另扩**表引用正则**（`5-1`→`5-\d+`、`A-1`→`A-\d+`），使新增补充表的正文引用一律走 `\ref`。⑨ **终态**：见 v19 行。**未改动任何数值、结论与检验** |
| **v17（本轮 · 修 AI 说明文档分页处「细线＋粗线并存」）** | ① **现象**（用户截图）：AI 工具使用详情说明中，**未结束表分页的前一页页脚**出现**细线＋粗线并存**，用户要求**只保留粗实线**。② **取证**（PyMuPDF 量线宽与墨迹区间）：6 处分页点均为「行间 `\midrule`（**0.5978pt**，全宽 439.9pt）＋ `\endfoot` 的 `\bottomrule`（**0.9564pt**）」，两条墨迹相距 **2.7–2.85pt**。③ **根因**：行间线写在「下一行之前」，分页时它是**独立于行的 chunk**，longtable 判「放得下」就把它**留在上一页页脚**（实测把 `\midrule` 换成 longtable 的 `\hline` **同样被留在页脚** ⟹ 无法靠"让线跟着下一行走"解决）。④ **修法**（改生成器 `root_temp/_mkaitool.py`，只作用本文档）：`\endfoot` 改为**裸 `\specialrule{\heavyrulewidth}{\dimexpr-\aboverulesep+\lightrulewidth\relax}{0pt}`** —— 粗线相对常规位置**上移**，**正好压在**那条孤立细线上 ⟹ **6 处页脚全部合成一条粗实线**（墨迹区间**相交**，缝 = **−0.09pt**，无缝隙；线宽全宽 439.9pt 未退化）。⑤ **已踩过的两个坑（勿回退，均实测取证）**：`\vspace{-…}` 会**破坏 `\bottomrule` 内部的 `\noalign`** ⟹ 整条规则退化成 **70pt 短粗线**（已渲染截图）；`\setlength{\aboverulesep}{-\aboverulesep}` 会**撑坏 `\endfoot` 模板** ⟹ 页数 **7 → 22 页**。⑥ 副产物修正：曾删掉续页表头 `\endhead` 的 `\midrule`（误以为会与下一行自带线叠成双线）——实测分页那条线**必然留在上一页**、不会跟过来 ⟹ 表头下会**无线**（已渲染取证），**已恢复**。⑦ **顺带体检正文 `main.pdf`（125 页）**：**无此缺陷** —— 三线表数据行之间不放线（只有分组行的 `\cmidrule`，0.40pt），实测 7 处「并存」皆为**两条等宽细线夹一短行**＝正常分组线；**唯需留意**：日后若给正文表格加行间线，会出现同类页脚缺陷。⑧ 终态：AI 文档 **7 页**、页脚 6 处全合并、续页表头线齐全 ✓。**未改动任何数值、结论与检验** |
| **v16（本轮 · 环绕图放大至 1.5× ＋ 编译前置清障）** | ① **编译卡住的真因（用户指出）**：多数是**阅读器开着 `main.pdf` 未关**（实测 `Get-Process` 命中「main.pdf - Adobe Acrobat (64-bit)」）⟹ 已固定为**编译前置步骤**：先 `CloseMainWindow`／必要时 `Kill` 掉 `Acrobat／AcroCEF／acrotray／SumatraPDF／Foxit／Nitro` 等**阅读器**进程（**浏览器不动**），再编译。**实测有效**：清障后 gen＋两遍编译一次通过（此前多次"日志止于第 2 页"的中断不复现）。② **图5-8（环绕）等比放大到约 1.5×**（用户指令）：`WRAP_FIGS` 中 `5-11` 由 **0.30 → 0.45**（0.30×1.5）⟹ 图区实测 **201×222pt**（原 136pt 宽，1.5× ✓）；**正文全部排在图的左侧**（环绕行右界 x=310，图起于 x=322 ✓）。③ **放大后复测：全文疑似重叠 = 0** ✓（先前"环不足则溢出"的风险未发生）；图区内容元素字号由 0.30 时的 **≤4.1pt** 提升到 **2.5／3.8／6.1／7.8／8.9pt**（主体标注已可读；**最小的"开始"节点与个别上标仍 2.5–3.8pt**，若要彻底可读需**绘图侧**把该两处字号调大）。④ 终态：**125 页／正文 29 页／错误 0／hbox 0／vbox 0／图占位 0／缺字 0／Warning 0**（残留 `Underfull \hbox` ×1，观感级）。**未改动任何数值、结论与检验** |
| **v13（本轮 · 表4-1 分组行 ＋ 环绕重叠修复 ＋ AI 文档表格细线）** | ① **表 4-1 分组行**（用户看诊断副本后确认"不是对齐、是四个小标题没合并单元格并居中"）：改为 **`\multicolumn{3}{c}{\bfseries …}`（合并整行＋居中＋加粗）＋ 其下 `\cmidrule(lr){1-3}`（细实线）**，longtable 与浮动表两个分支同步。② **图5-8（环绕）重叠**：取证＝该图原声明在 §5.2.3 **末尾**、其后仅约 45pt 正文 < 图高 171pt ⟹ 环绕溢出进 §5.2.4，**与表5-5 叠在一起**（实测表题注伸到 x=442、表头伸到 x=488，而图占 x 410→524）。修法＝**图移到本节公式之后**（其后连续 15 行正文≈189pt > 图高 160pt）＋宽度 0.34→**0.30**。**实测全文重叠 0 处 ✅、表5-5 表头恢复正常（"时间/h…2.0"横跨 x95→488 无干扰）**。③ ⚠ **同时测得**：图5-8 在 0.30 宽下图内文字仅 **1.7–4.1pt（不可读）**——该 IMEX 流程图（原图 401×469pt、文字密集）本质需 ~0.85 版心宽；**环绕与可读性在此图不可兼得**，已列三选项待用户裁定（保持环绕＋绘图侧重渲染／改 0.85 大图／折中 0.45–0.55）。④ **AI 文档表格**：判断取**行间细实线**（单元格常跨 3–5 行，"行距"无法界定行边界）⟹ 8 张表插入 **57 条 `\midrule`**，**只作用于该文档**（正文表格样式未动）；重编译 **7 页／0 错误／0 缺字／0 告警**。⑤ 交付件终态：**125 页／正文 29 页／错误 0／hbox 0／vbox 0／图占位 0／缺字 0／Warning 0**。**未改动任何数值、结论与检验** |
| **v12（本轮 · 支撑材料《AI工具使用详情》转排）** | 用户交付 `50_论文/04_交排版成图方/支撑材料/AI工具使用详情.md`，要求**按正文同款样式**转 LaTeX 并编译。**做法（保证"同款"的机制）**：新文档 `支撑材料/AI工具使用详情.tex` 内 **`\input{../交付排版方论文/preamble}`**（正文版式件）＋ 转换器 `root_temp/_mkaitool.py` **复用正文生成器的 `inline()` 与 `table_block()`** 处理正文与表格 ⟹ 标题（黑体三号/四号、一级居中）、正文（宋体小四、单倍行距、首行缩进 2 字符）、表格（三线表＋`\settabw` 列宽加权＋**md 对齐行驱动**＋`m{}` 竖直居中）、西文/数字（Times New Roman）**与正文逐项同源**，非"照着调参数"。转换范围：题目 1 ＋ 一级节 4（`\section*` 保留"一、二、三、四"，不另加 LaTeX 序号）＋ 二级节 2 ＋ **表格 8**（含 3 个"典型交互示例"表）。**过程中修掉一处真实缺陷**：带圈数字 **①②③④⑤⑥ 共 9 处 `Missing character`**——根因＝U+2460–U+24FF **不在 xeCJK 默认 CJK 区间**，被送进 `Times New Roman` 而 Times 无此字形；修法＝**`\xeCJKDeclareCharClass{CJK}{"2460 -> "24FF}`**（只改字形路由、不动任何样式），并**同步写入交付论文 `preamble.tex` 与生成器模板**。实测：新文档 **6 页／错误 0／hbox 0／vbox 0／缺字 0／Warning 0**，字体 SimSun 12＋TimesNewRomanPSMT 12＋SimHei 16/12＝与正文一致；交付论文同步重编 **125 页／正文 29 页／0 告警（未变）**。**未改动任何数值、结论与检验** |
| **v11（本轮 · 环绕排版 ＋ 题目换行 ＋ 表4-1 线框副本）** | ① **图5-8 改"环绕"**（用户指令"类似 Word 环绕、直接在原文件改"）：`wrapfig` 实现，新增生成器配置 **`WRAP_FIGS = {"5-11": (0.34, "r")}`**（键＝论文内索引号，值＝图宽占比／靠边），并在 `fig_block` 内分支发射 `wrapfigure`；题注用 **`\captionof{figure}`** ⟹ **编号与 `\label` 语义不变、正文 `\ref` 一字不改**，且不再被推成"整页只有一张图"。实测 p14：**正文在图左侧环绕**（正文 x 191→408，图高约 181pt）、0 vbox／0 hbox。② **题目换行**：实测原断行落在 **"达标时／间"**（**断在词中间**，两行 448／160pt 极不平衡）；新增生成器配置 **`TITLE_BREAK_BEFORE = "耦合模型的建立"`** 给显式换行 ⟹ 上行 272pt／下行 336pt **两行均衡、无断词、无行首标点**（换行点可一行调走）。③ **表 4-1 线框诊断副本**（用户要求"建副本改成普通线边，我检查到底怎么回事"）：复制整工程至 `50_论文/04_交排版成图方/**\_诊断_表41线框**/`（**与交付件同深度**，故 `../../05_成品图/` 相对路径仍可解析），并**只对副本**把表 4-1 由三线表改为**普通线框**（列规格两端与列间加 `\|` ＋ **逐行 `\hline`，共 24 条**）⟹ 编译产出 `main.pdf`（表 4-1 在 **p6**，横线 x 77→518）。**交付件与生成器一律未受影响**；补丁脚本幂等（始终读交付件、只写副本）。④ 交付件终态：**125 页／正文 29 页／错误 0／hbox 0／vbox 0／图占位 0／缺字 0／Warning 0**。**未改动任何数值、结论与检验** |
| **v10（本轮 · 六项图表排版专项）** | 依用户点名的六项逐一取证并修复（编号映射取自 `main.aux` 的 `\newlabel`，确保与其所见**印刷号**一致）：① **表 4-1（`tab:0-1`）"重灾区"**：实测被 `longtable` 在 p5 页底切出"题注＋表头＋仅 1 行"、余 17 行续 p6 ⟹ **题注与表体跨页分离**。修法＝新增 `FLOAT_TABLES` 白名单 + `long = label not in FLOAT_TABLES`，**该表改用浮动 `table`**（既有分支，自带 `\centering`），并补浮动分支的**分组行 `\multicolumn`**；列宽 `0.30/0.39/0.31 → 0.26/0.58/0.16`（原"含义"列仅 155pt 行行折行、"单位"列右侧**空 44pt**）。**实测：p5 表线 0 条、整表含题注完整落 p6 ✅**。② **图5-8（交替推进流程图）／图5-9（重叠段差异归因）**：原各高约 8.5 cm 同落一页（合计占版心高 69%）⟹ `FIG_WIDTH` 调 **5-11→0.36（6.7cm）／新增 5-12→0.50（6.4cm）**。③ **图5-10＋图5-11 并排**：`FIG_PAIRS` 增 `("5-15","5-16")` ✅ 实测图注**同在 p18**。④ **图6-2＋图6-3 并排**：`FIG_PAIRS` 增 `("A-10","A-2")` ✅ 实测图注**同在 p25**。`PAIR_W` 0.485→**0.455**（合计 0.91 版心宽）：因新并入的图5-15 为高瘦流程图（h/w≈1.22），0.485 宽时高 9.5 cm **越"流程图 ≤9 cm"红线**。⑤ **表5-9（`tab:5-3` "严重问题"）**：上版把首列压到 `0.09`（≈40pt）**过了头** ⟹ "物性体系"被硬折两行；改为 **`0.12/0.22×4`**（首列 53pt 单行 ✓，四问列 97pt ≥ 单元格 `50.6523±0.0249 h` 所需 93pt ✓）。⑥ **表5-10（`tab:5-4`）"未居中"**：表规则线实测 78→518 与版心 71→524 **本已居中**；故按"列内容居中"执行——md 对齐行 `\|:---\|:---:\|:---\|` → **`\|:---:\|:---:\|:---:\|`**（三列全部居中）。⑦ 顺带：`frontmatter/00_摘要页.tex` 的格式改动**已同步进生成器模板**（此前仅交付件有效，重生成会退回旧样式）。⑧ **终态**：**125 页／正文 29 页（≤30 ✅）／错误 0／hbox 0／vbox 0／图占位 0／缺字 0／Warning 0**（残留 `Underfull \hbox` ×1，观感级）。**未改动任何数值、结论与检验** |
| **v9（本轮 · 按 kb 口径落地字体/字号）** | ① **正标题（论文题目）有明确口径且 6 处来源一致**：**16 号、加粗、居中**（`三天极限速成` L1589／`挑战一小时速通` L1987、L2037／`挑战3天零基础速成` L2017、L2034／`2025国赛备战最强资料` L808）。⟹ 已落地为 `\zihao{3}\bfseries\centering`（PDF 实测 **SimHei 16.0**）。② **按用户优先级【字体 ＞ 字号 ＞ 行间距】落地**：**字体**＝标题黑体（SimHei）＋正文宋体（SimSun）＋**西文/数字/公式 Times New Roman**（`\setmainfont{Times New Roman}` ＋ `newtxmath`；PDF 实测 TimesNewRomanPSMT 2063 处、公式主体 NewTXMI/txsys/txmiaX，**残留少量 CM 符号**）；**字号**＝题目/一级 **16pt(三号)**、摘要标题/二级 **14pt(四号)**、三级/关键词 **12pt(小四)**、正文 **12pt(小四)**；首行缩进 2 字符 ✓。③ **摘要格式**（用户追问）：官方第三条**只**规定"摘要专用页、含标题与关键词、≤1 页、页码自本页起"，**无字体字号规定**；kb《2025国赛备战最强资料》§6.2 给出＝**题目 16 号加粗居中／摘要标题 14 号加粗居中／摘要内容 12 号单倍行距／关键词 12 号加粗居左** ⟹ 已逐条落地（摘要标题改为独立居中行、题目 16pt、关键词保持加粗居左、摘要内容行距 `1.0` → **`1.25`**（★用户指令：摘要页内容较松散可放宽、**正文仍保持单倍紧凑**））。④ **行距**：`linespread=1.5`（要点48 口径）**实测正文 39 页／全文 181 页 ⟹ 超官方第四条红线 9 页** ✗；而《2025国赛备战最强资料》两处明写"**单倍行距**" ⟹ 依优先级（行间距最低）**维持单倍**，正文回到 **30 页** ✓。⑤ **段前段后（12/6/3pt）已弃用**：实测该间距使两处长表分块超出 ⟹ 2 处 `Overfull \vbox`（p9 142pt／p11 296pt）；**去掉即 0 vbox**（归因确认）—— 属最低优先的间距项，为布局洁净让路。⑥ **终态**：**126 页／正文 30 页／错误 0／hbox 0／vbox 0／图占位 0／缺字 0**；残留 `Underfull \hbox` ×1（居中长标题所致，观感级）。**未改动任何数值、结论与检验** |
| **v8（本轮 · 标题字号字体核查）** | ① **官方**：全文十三条**无任何标题字号/字体条款**，**第八条**明文"字号、字体、行距、颜色等**不做统一要求**" ✓。② **kb 内有口径且三源一致**：`必看！2026数学建模国赛获奖要点解析！.md` 要点48＝"正文宋体小四(12pt)、**一级标题黑体三号、二级黑体四号、三级黑体小四**、1.5 倍行距、首行缩进 2 字符"；`2026国赛零基础获奖速成课讲义.md`＝"标题1 黑体三号(16pt) 居中加粗／标题2 黑体四号(14pt) 左对齐／标题3 黑体小四(12pt) 左对齐"；`AI论文全维度自查表 13.2`＝"正文宋体小四，**标题黑体**，公式 Times New Roman；**字体混乱→❌**"。③ **我方 PDF 字体级实测对照**：正文 **SimSun 12**（✅ 一致）／一级 **SimHei 17.2**（kb 三号＝16 ⟹ **偏大 1.2pt**）／二级 **SimHei 14.3**（✅ ≈四号）／三级 **SimHei 12**（✅ 小四）／标题族＝**SimHei**（✅ 黑体）／公式变量＝**CM 族**（⚠ kb 期望 Times New Roman）／行距 **1.0**（⚠ 民间 1.5，**受 ≤30 页红线约束不可直接就改**）。④ 另实测到两处**字体分工**（非混用）：**页眉**用 **KaiTi 12pt**（125 页，y=35.7 页边区）；**附录 B.1 表格备注列**用 **FangSong 12pt**（p39／43／44）。⑤ **三项待裁定**：一级标题 17.2→16pt（零风险，建议做）／公式字体 CM→Times（需评估缺字）／行距 1.5（与页数红线冲突）。**未改动任何数值、结论与检验** |
| **v7（本轮 · 图表留白＋缺字归零＋文献入文）** | ① **图表留白（用户提出"调节插入图表的边距"）**：先**实测证伪**——19 张成品图"页面盒≈内容盒"、白边率 **0–2.6%**（>12% 者 0 张）；全篇 126 页仅 p1／p38 空隙 >60pt ⟹ 白边**不在图件内、也不在页面**，而在**插入图表时的默认间距**。处置：`\abovecaptionskip` 10→**5pt**、`\belowcaptionskip`→**0**；**实测把 `\textfloatsep`／`\intextsep`／`\floatsep` 收紧到 8–9pt 会引发 2 处长表分块 `Overfull \vbox`（254／260pt）** ⟹ 依"无证据不留改动"**回退为默认浮动体间距**（归因实验：停用即 0 vbox）。② **`Missing character` 44 → 0**（附录 B 代码注释的 `⟹`×43＋`✅`×1）：用 `newunicodechar` 把两字形映射到已有字体（`\Longrightarrow`／`\ding{51}`，附 `pifont`），**不改 `code/` 任何字节**（哈希不变）；`literate` 作二次保险。③ **文献入文 3 条**（`bibitem` 18→**21**，近 5 年 3→**6** 条）：[19] 胥文博 2025（应用数学进展 14(3):203-215）、[20] 王清艳 2024（理论数学 14(4):98-106）、[21] arXiv:2506.18809；引用点＝§5.3.2「反演」口径界定句（[19][20]）与 §5.3.4 误差带合成口径句（[21]），**无"列而不引"**。**上一轮联网检索的 3 条候选因缺作者／完整题名未引**（禁编造；kb 内无可引元数据）。④ **终态（正式 `main.pdf`）**：**126 页／正文 30 页／错误 0／`\hbox` 0／`\vbox` 0／图占位 0／缺字 0／LaTeX Warning 0**。**未改动任何数值、结论与检验** |
| **v6（本轮 · 版式官方依据核查 ＋ 行距调整）** | ① **核查版式三要素的官方依据**（取 `kb` 内官方规范原文）：**页边距**＝**第一条硬红线**"上下左右各留出至少 2.5 厘米"⟹ 本件恰好压在下限、**不可调小（未动）**；**字号／字体／行距**＝**第八条明确"不做统一要求"**⟹ 可调。② **仅调行距**：`linespread` **1.06 → 1.0**（交付件 `preamble.tex` L10 与生成器模板**同改**），实测 **全文 132 → 126 页、正文 31 → 30 页（回到红线内 ✅）**，并**顺带消除 §三 的 T-1 `Overfull \vbox` 301 pt** ⟹ 现 **`\hbox` 0／`\vbox` 0／错误 0／图占位 0**。③ 字号**保持 12 pt（小四）**未动：改五号属"改观感"级别动作，超出"适量调小"范围，留待你的指令（一行可改，预计再省 ~5–6 页）。④ **说明书未纳入任何文献**（`bibitem` 仍 18 条、近 5 年 3 条 ⟹ F-7 已按裁定"维持现状"关闭），此前检索到的候选**一条都未加入**。**未改动任何数值、结论与检验** |
| **v5（本轮 · 图占位修复 ＋ 位置诊断 ＋ 文献定案）** | ① **修复「正文图占位」（用户实测至少 3 处 → 实为 7 处）**：根因＝`gen_latex.py::load_figmap()` 只读 `A_图表编号对照.json`，而该 JSON 的 `paper_index` **只填 12/51**（正文在用的 图5-3／5-6／5-10／5-11／5-12／5-15／5-20 全缺）⟹ 候选链退化为「论文内索引号」⟹ `\fgphchain` 未命中 ⟹ 打印 `\fbox{图占位}`。**修法**＝新增 `_draft_figmap()`：**以成文稿「占位清单」为准补全映射**（只在图链接这一条路径生效，不动其余三张派生表）。**实测：补全 7 个号、对照表命中 18/18、无比对行 0、`main.pdf` 文本中「图占位」＝0**。② **查明你为何看到占位**：上一次正式编译**被超时中断**（`main.log` 止于第 2 页）⟹ `main.pdf` 停留在 15:13 的旧件（含 7 处占位）；已单独重跑两遍正式编译，**`main.pdf` 已刷新为 132 页／1586 KB／占位 0**。③ **图表位置诊断**（自洽判据）：图 5-10 比首次引用**晚 2 页**、图 5-3／5-4 **早 1 页**、其余同页（正常）；**两项试修均无改善并已回退**（浮动容限／缩图）⟹ 结论为内容流量所致。④ **文献定案**：依用户裁定 **F-7 关闭＝维持现状（不删经典、不再补）**。⑤ 排版遗留更新：`\hbox` 0、`\vbox` 1（p12 纯文本页，301pt）、缺字 44 待裁定；**正文 31 页超红线 1 页**（待腾页）。**未改动任何数值、结论与检验** |
| **v4（本轮 · 执行 F-6 ＋ 联网检索 ＋ 排版停手登记）** | ① **执行 F-6**：正文图 **9 → 13 张**（补入图 5-3／5-10／5-11／5-15 四张流程图，原位补号不重排），图引用 **34 → 42**；同步更新 A_08 元信息／占位清单／"不呈现"统计与派生表 `A_图表编号对照.md`。② **图件尺寸校正**：实测纵横比后发现图19／图51 超"流程图 ≤9 cm"红线（12.4／12.8 cm，并触发 vbox 301pt）⟹ `FIG_WIDTH` 按图定宽（5-11／5-15 = 0.45、5-17 = 0.48），全部收回红线内。③ **联网检索完成**（排除全部开赛后资料）：新增可核候选 **3 条**（J. Food Eng. 2023 收缩机理；Numer. Algorithms 2024 IMEX-BDF2 变步长收敛；J. Comput. Math. 2023 IMEX-BDF2 误差估计），连同 kb 内候选**合计可用 ≈8 条** ⟹ 近 5 年占比可达 **~42%**，仍**不达 60%**。④ **排版停手登记**：新增 **§三 排版遗留登记 T-1…T-4**（2 处 vbox＋1 处 hbox＋44 缺字），按用户指令**本区不再执行排版动作**。**未改动任何数值、结论与检验** |
| **v3（本轮 · 裁定 C3／C4 ＋ 执行 F-4 第一批 ＋ 文献检索）** | ① **记录 C3 裁定**（按甲／引用次数，力争两口径都满足 ⟹ 图引用 ≥47 且件数达标）与 **C4 裁定**（保留现状、暂不裁决）；§四 状态表与 §5.1 同步更新。② **执行 F-4 第一批**：编号式 **16 → 21**（新增 (5-14) 毕奥数／(5-15) 欠松弛更新／(5-16) 两场特征时间／(5-17) 扩散长度／(6-8) 归一化灵敏度），**全部由已有数值与已述方法形式化、不新增结论**；A_08"式号口径"注与 A_10 成文修正表 #2 同步更新；编译 **131 页／正文 29 页／Overfull 0／错误 0**。③ **F-7 文献检索完成**：查 `kb/source/联网搜索`（42 件）⟹ 实测可用新增量 **4–6 篇**，并**实证"近 5 年 ≥60%" 不可达**（补满亦仅 37.5%），附两方案待裁定。④ F-4 余 9 式已列计划。**未改动任何数值、结论与检验** |
| **v2（同轮 · 裁定回填 ＋ 自检优化）** | ① **记录用户裁定**：**C1＝取交集（≥30）**、**C2＝取交集（≤1000 字符 且 ≥800 字）**；② **C2 已执行完毕**：摘要压缩至 **999 字符（不计空格、含关键词）**、29 处数值全留、`A_02_摘要.md` 已回填元信息／自查／核验第 16 项／变更记录 v5，编译复核 **正文 29 页／Overfull 0／错误 0**；③ **新增 §五**（裁定记录 ＋ **C3／C4 两方源名与原文**，供用户判断）并**重写 §四为状态一览表**（原"推荐口径"段已作废，避免与裁定冲突）；④ §二 标题由"待裁定"改为"已裁定／待执行"并注明三类约束；⑤ F-4 补"执行要点"（优先把行内式升格为编号式以压低页数代价）。**未改动任何数值、结论与检验** |
| **v1** | 首版：前置核查 7 项全部闭环（`temp/_crosscheck4.txt`）；产出 **11 项修复方案**（**① 本轮已改 3 项**：F-1 选型排除式／F-2 工程启示段／F-3 灵敏度档位判定句；**② 待裁定 6 项**；**③ 受阻 1 项**）＋ **4 组判据冲突的推荐口径**。**未改动任何数值、结论与检验** |
