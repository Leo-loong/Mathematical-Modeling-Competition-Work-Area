# 数学建模竞赛 AI 协作约定（Claude Code 自动加载）

> 本工作区用于全国大学生数学建模竞赛（CUMCM，3 天赛程）。
> 规则：**AI 跑腿，人把关；AI 出稿，人改写。** 任何时候先读 `README.md`。

## 工作区结构

- `00_知识库/` — 静态资料（赛前备好，比赛期间**只读**，AI 回答问题以这里为准）
- `01_当前赛题/` — 动态工作区（赛题原文、数据分析、建模过程、论文素材）
- `02_输出产物/` — 最终交付物（论文 PDF、支撑材料、AI 声明）

## 每次会话开始先做

1. 读 `00_知识库/00_知识库使用说明.md` 和 `README.md`，了解全貌。
2. 读 `01_当前赛题/` 下所有模板与记录（用户赛前已填写的内容优先于模板）。
3. 用户给出赛题后：提炼问题清单 → 让用户确认 → 按 `01_当前赛题/README_使用说明.md` 的三天节奏推进。

## 回答问题的优先级

1. 用户已有记录/结论（`01_当前赛题/`）> 知识库（`00_知识库/`）> 通用知识。
2. 知识库中已有的模型/代码/格式规范，直接引用，不另起炉灶。
3. 知识库没有的方法，先说明理由再补充，不要假装知识库里有。

## 红线（AI 绝不能做）

- ❌ 替用户定模型、定假设（只给候选 + 理由，由团队拍板）
- ❌ 编造结果数据（所有数字必须来自用户跑出的程序输出）
- ❌ 替用户写摘要定稿（只能给修改建议）
- ❌ 修改 `00_知识库/` 内容（比赛期间只读；赛后用户另行安排）

## 代码规范（代码要进论文附录）

- 每个问题一个脚本 `solve_qx.py`，放 `01_当前赛题/02_建模过程/代码脚本/Qx/`
- 中文注释；开头写用途/输入/输出/依赖；结果存文件，不做任何手动修改
- 画图统一用 `00_知识库/画图模板/matplotlib_中文画图模板.py`（已处理中文字体）
- 给代码前先想清楚边界情况（缺失值/除零/数据长度）；给完代码提醒用户"必须自己跑一遍"

## 论文写作约定

- 格式按 2026 年修订稿：正文 ≤ 30 页、摘要 ≤ 1 页、附录含全部可运行代码
- 输出文本要"去 AI 味"：具体数值 > 空话；讲过程（试过什么/为什么换）> "首先其次"；不留模板痕迹
- 所有建议附理由或来源，禁止只给结论
